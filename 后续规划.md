# 后续规划

> ## 现在停在哪（2026-08-29 交接）
>
> ### 分支状态：干净的，没有待合并的东西
>
> `master` 已包含所有已验收的改动，最后并进去的是这一批（用户真机逐项验过）：
>
> - **短语模块**（第十二节）+ 它的两处返工（第十三节：三条线不再重叠、
>   右侧栏按正文顺序排）
> - **复习页点词/点句朗读**（第十节）
> - **API Key 那格改成明文文本框 + 眼睛**（第十四节，手机上粘不了的毛病）
> - **AI 角标不再多占一行**（第十五节）
> - **改了卡片角标不消失**的两个 bug（第十六节：只在失焦时才保存、
>   短语的改动根本存不进库）
>
> 旧分支（`speak-cards` / `phrase-fixes` 等）留着当存档，可以不管。
>
> ### 当前状态
>
> - 测试 295 项全过；`npx tsc -b --force`、`npm run build` 都通过
> - APK 打得出来（三步见 [发布到手机.md](发布到手机.md)）
>
> ### 下一步：一键划词（决定已拍板，未动手）
>
> 阅读页放一个按钮，AI 通读全文挑出重点词与短语并填好。
> 三个决定和那个真正的难点（把 AI 说的词**定位回原文坐标**）
> 写在第十二节末尾。**前置的短语模块已经就绪并验收**。
>
> 其余候选见第八节（意向，都还需要先定细节）和第九节（不依赖前置的零散项）。
>
> ### 一件到期要做的收尾
>
> 标注模型换过之后，旧数据和旧代码都还留着没删（`data.notes`、旧的
> `reconcilePage` 及其测试、storage 里那几个已无人调用的旧写入函数 ——
> 文件里都加了标记）。**在手机上正常用几天确认无误后，做一次清理。**
>
> ### 这个项目的工作方式
>
> 每次改动**开一条分支** → 打包 APK 给用户在手机上验 → 确认没问题才并回 `master`。
> 已并完的旧分支留着当存档，可以不管。
>
> 观感类的问题（「不好看」「没对齐」）**不要凭印象改** —— 去浏览器里把数字量出来，
> 量完再谈。这个项目里已经三次靠量数字发现真正的原因（喇叭图标偏高 2px、
> 长网址 302px 撑破 275px 的卡片、两条线都画在基线下 4px 处所以必然重叠）。
>
> 还有一条同样贵的教训：**「浏览器里能跑」不等于装进 App 还能跑**。
> WebView 不是 Chrome —— 朗读（第十节）和 Key 粘不了（第十四节）栽的是同一件事。
> 凡涉及系统能力（朗读、相机、剪贴板、文件、分享），先在真机上验一个最小版本。

这份文档记录**结构上还有哪些短板**、**想加的功能各自需要什么前置条件**，以及**建议的推进顺序**。

想做某项功能时先翻到对应条目，看一眼「要先做什么」和「注意事项」，避免又踩进「改一个地方带出三个问题」的循环。

---

## 一、先看这个：结构上的三个短板

这三块是目前所有麻烦的来源。不必现在就修，但加功能前最好知道它们在哪。

### 短板 1：笔记没有稳定身份，位置就是身份 ⚠️ 最关键

**现状**

单词笔记存成这样，**键就是位置**（第 0 行第 2 个词）：

```
{ "L0W2": { word: "stood", definition: "站立" } }
```

句摘则是完全另一套系统：单独的数组、自己的 id，而且存在**另一个地方**（浏览器的 localStorage，不在主数据库里）。

**为什么是问题**

- 位置一变，身份就变。所以正文一编辑，笔记就会错位 —— 这是 2026-08-28 那批 bug 的总根源。目前是靠「对账」在事后补救，不是从设计上就不会错。
- 两套并行的系统，同样的逻辑要写两遍。再加个「短语」就得写第三遍。
- 备份要从两个地方拼数据（主库 + localStorage），容易漏。

**改法**

给每条笔记一个**永不改变的 id**，把位置降级成一个普通属性：

```
标注 {
  id          ← 稳定不变，这才是身份
  docId       ← 属于哪篇文档
  type        ← 'word' | 'phrase' | 'sentence'
  范围        ← 位置只是属性，会变很正常
  原文        ← 用来对账
  内容        ← 音标 / 词性 / 释义 / 句型 / 翻译
  排序        ← 卡片排序用
  orphaned    ← 原文已删除
}
```

**这一次改动能同时解锁：** 短语归类、卡片排序、自动划生词、有声阅读的位置映射、以及安全地修改分词规则。详见下面各条。

**代价**：要迁移你现有的笔记数据，是风险最高的一次改动。建议等确定要做上面某个功能时再动，别为改而改。

---

### ~~短板 2：滚动时整个界面重画~~ ✅ 2026-08-29 已完成

滚动时每一帧都把阅读进度写进最外层状态，导致整棵树重渲染（含上千个单词节点）。
当初试过的五个 CSS 偏方一个都没打到点子上 —— 卡的不是绘制，是 JS。

改法（三处）：
- 阅读进度取整到百分点：进度条本来只显示到这个精度，不取整则每帧都是新数值
- 两个内联回调提成稳定引用（否则 memo 会被破坏）
- 四个大组件用 memo 包起来做渲染隔离

实测（1400 词文档，20 次滚动）：

| 组件 | 改前 | 改后 |
|---|---|---|
| LyricEditor | 40 次 | **0 次** |
| LeftSidebar | 40 次 | **0 次** |
| RightSidebar | 40 次 | 12 次 |
| App | 40 次 | 24 次 |

实际工作耗时 688ms → 460ms。

### ~~短板 3：导入功能写死在界面里~~ ✅ 2026-08-29 已完成

txt 导入的 110 行代码原本直接写在 `App.tsx` 的回调里（嵌套四层 Promise），
每加一种格式都得去改界面代码。

现已抽出 `src/importers/`：

```
任何输入 -> { 书名, 章节: [{ 标题, 正文 }] }
```

- `types.ts` 定义约定，`txt.ts` 实现纯文本，`index.ts` 是登记处
- **加新格式只需写一个文件、在登记处加一行**，界面一个字都不用动 ——
  连文件选择器的 accept 都会自动跟着变
- 章节切分变成纯函数，第一次有了单独的测试（13 项）
- App.tsx 里那段从 4011 字符缩到 581 字符

## 二、升级清单逐项

### ~~epub 格式导入~~ ✅ 2026-08-29 已完成

`src/importers/epub.ts` + `epubParts.ts`。用 fflate 解压，按 epub 的固定约定读：
container.xml -> OPF（书名、阅读顺序、目录位置）-> 目录（章节名）-> 各章 XHTML。

- 一个 epub = 一个文库，一个正文文件 = 一篇文档
- 章节名优先取自目录（epub2 的 ncx 与 epub3 的 nav 都支持），
  没有目录则退回正文里的 h1/h2，再不行按「第 N 章」编号
- 封面、空白页（没有正文的）自动跳过
- HTML 转纯文本保留段落换行：阅读器每行是一个块，不空行段落会挤在一起

**这一项是导入层的第一次检验：界面代码一个字都没改，
只在登记处加了一行，文件选择器就自动开始接受 .epub 了。**

### ~~一键填充（AI 补全空白笔记）~~ ✅ 2026-08-29 已完成

复习页顶部按钮，范围跟着当前打开的文档或文库走。只填**完全空白**的笔记，
已经写过的内容绝不覆盖。

- `src/enrich/` —— 和导入层同一个思路：`types.ts` 定约定、`deepseek.ts` 是实现、
  `runner.ts` 管分批与中断、`index.ts` 是入口。**换供应商不用动界面**
- 单词填音标 / 词性 / 中文释义（非原形会在释义末尾补上原形，如「飞行；飞翔（fly 现在分词）」）；
  句子填句型说明 + 翻译
- 一次打包多条发送（单词 40 条一批、句子 15 条一批）—— 逐条发会贵十几倍
- 每批完成立即落库，中途停掉已填的都保留
- 填出来的内容标一个灰色单星角标；**用户手动改过就自动清除**，
  所以它实际是一份「待复核清单」，复核完自然清空

**API Key 的事**：用的是用户自己的 DeepSeek key，存在本机 localStorage
（键名 `lyric-vocab-ai-key`），不上传、也不写进导出的备份。
第一次点填充按钮时会要，之后可在同一个对话框里「换 Key」。

**想换别家 AI**：已经做好了，见下面「多 AI 供应商」一条 ——
对话框里选「自定义」，填服务地址和模型名即可，不用改代码。

---

### ~~短语归类~~ ✅ 2026-08-29 已完成

见第十二节。当初判断「标注模型统一之后它只是 `type: 'phrase'`，不需要新逻辑」——
数据层确实如此（存储、备份、对账、迁移全都免费继承），
但**界面是从零做的**：正文画线、复习卡片、抽屉表单、右侧栏、AI 填充，一处都不能少。

---

### 单词卡 / 句子卡可调整位置

| | |
|---|---|
| 需要先做 | **短板 1（标注模型）** |
| 难度 | 做完前置后：低 |

排序需要两样东西：稳定的 id（知道谁是谁）和排序字段（记住顺序）。现在两样都没有 —— 笔记的「身份」就是它的位置，而位置是会被正文编辑改变的，没法拿来排序。

左侧栏的文库拖拽排序已经有现成实现（dnd-kit），可以照搬。

---

### ~~划句子时标点与数字的处理~~ ✅ 2026-08-29 已完成

分词规则已修正：统一两种撇号、缩写后缀拆成标点、数字纳入单词、落单的连字符不再算词。
`she's` / `she’s` 现在都能单独选中 `she`，`COVID-19`、`1990s` 是完整的词，纯数字不再算单词。

配套做了一次性数据迁移（`migrateTokenizer.ts`）：新旧规则扫的是同一串文字，
按**字符位置**把旧词编号精确对应到新编号，不是猜。迁移只跑一次，用 localStorage 标记守住。

---

### 单篇文章过长

| | |
|---|---|
| 需要先做 | 无（独立） |
| 难度 | 中高 |

**现状**：每个英文词都是一个独立的网页元素。一章小说几千个词 = 几千个元素，一次性全部渲染。

**缓解措施已有**：导入 txt 时会自动按「第 N 章 / Chapter / Session」分章，所以一般不会真的把整本书塞进一篇。

**真要解决**：做虚拟滚动（只渲染屏幕上看得见的部分）。建议**等真被卡到再说**。

---

### 导出为 Word 文档

| | |
|---|---|
| 需要先做 | 无（独立） |
| 难度 | 看选哪条路 |

- **纯前端生成 .docx**：能做，但要引入挺重的库。
- **导出 HTML 或 Markdown**：成本低得多，一样能打印、能发给别人、能用 Word 打开。

如果目的只是「有个能拿出去的文件」，建议走第二条。**这条要你先定方向。**

---

### 拍照识别文字（OCR）

| | |
|---|---|
| 需要先做 | ~~短板 3（导入层）~~ 已就绪 |
| 难度 | 中 |

本质是接一个安卓的文字识别插件，拿到文本后走导入流程。**风险不在结构，在识别质量** —— 尤其是中英混排、排版复杂的书页。建议先小范围试效果再决定要不要正式做。

---

### 自动检索考试词典划生词

| | |
|---|---|
| 需要先做 | **短板 1（标注模型）** |
| 难度 | 代码不难，**难在数据** |

这项主要不是编程问题：得先有一份可用的词表（四六级 / 考研 / 雅思等），最好还带释义和音标。找到合适的数据之后，代码部分很短 —— 扫一遍正文，命中词表的批量生成标注。

**要你先解决的是数据从哪来。**

---

### 导入视频字幕

| | |
|---|---|
| 需要先做 | ~~短板 3（导入层）~~ 已就绪 |
| 难度 | 低到中 |

字幕文件（.srt 等）结构简单，解析容易。要想清楚的是**时间轴要不要留着** —— 如果留，以后可以和「有声阅读」联动；如果只要文字，那就是最简单的一种导入格式。

---

### 有声阅读

| | |
|---|---|
| 需要先做 | 想要「读到哪高亮到哪」→ ~~短板 1~~ 已就绪 |
| 难度 | 分两步，差别很大 |

- ~~**第一步「能读」**~~ ✅ 2026-08-29 已完成，见第十节。
  做的**不是**阅读页整篇朗读（机械音通读没什么好听的），而是
  **复习页点单词卡的词、点句摘卡的句子就读出来** —— 用户要的是查发音，不是听书。
- **第二步「读到哪里高亮到哪里」**：需要把声音进度对应到文中位置。
  地基已经有了（标注有稳定 id 和 start/end），什么时候想做都行。

---

## 三、建议顺序

1. ~~**渲染隔离**（短板 2）~~ ✅ 已完成
2. ~~**导入层抽出来**（短板 3）~~ ✅ 已完成
3. ~~**统一标注模型**（短板 1）~~ ✅ 已完成，见第七节
4. **虚拟滚动** —— 等真被长文卡到再说

**4 先放着。**

零散的、不依赖任何前置的：导出文档、有声阅读第一步、拍照识别试水。想到哪做到哪都行。

---

## 四、目前已知的小问题

- **目录（左侧栏）拖拽排序偶有异常**。左侧栏自己存了一份文档列表，和数据库传下来的那份是**两份**，拖动时改本地那份、松手后存库、存完新数据又灌回来覆盖本地 —— 两份数据来回拉扯。2026-08-28 修掉了其中一半（排序保存以前是「发出去就不管」，拿不到结果，容易被旧数据覆盖）。如果还有问题，需要具体现象才好定位。

---

## 五、背景：2026-08-28 已完成的部分

搁置约半年后的一次集中整理，从根上处理掉了几类问题。了解这些有助于理解现在的代码为什么长这样。

| 做了什么 | 解决了什么 |
|---|---|
| 加入 git 版本管理 | 之前没有存档，每次改动都得赌 |
| 改为纯触摸交互（长按取词），删掉桌面端并行逻辑 | 双击手势时灵时不灵；手机上看不出哪些词可点；净删 156 行 |
| 存储层收口：内存一份权威数据 + 排队落盘 | 偶发丢笔记；编辑长文卡顿（打字时数据库写入 40 次 → 1 次） |
| 编辑框改用本地草稿 | 编辑正文时光标被弹到文末 |
| 正文编辑后自动对账笔记位置 | 划线转移到别的词、划线消失、以及「改上文导致所有笔记整体错位」 |
| 孤儿笔记标记 +「原文已删除」+ 删除入口 | 笔记与正文对不上时无从处理 |
| 系统弹窗改为 app 内弹窗 | 弹窗样式突兀；弹出后画面被切成两半（输入法相关） |
| 引入测试（24 项） | 最绕的对账逻辑现在有回归保护 |

## 六、2026-08-29 已完成的部分

| 做了什么 | 解决了什么 |
|---|---|
| 底部避让系统导航栏 | 三键导航（后台/主页/返回）会压住左侧栏底部那排图标 |
| 长按选中的单词保持高亮 | 抽屉弹出后看不出选中的是哪个词 |
| 回收站两个弹窗改用传送门 | 它们长在带 transform 的侧栏里，遮罩只盖住 250px，点外面关不掉、还会跟着侧栏被收走 |
| 接管安卓返回键，逐层关闭 | 开着弹窗按返回会直接退出 App |
| 分词规则修正 + 一次性数据迁移 | `she's` 无法单独选中 `she`；两种撇号行为不一致；`COVID-19` 被切坏 |
| 句摘原文改为按位置截取 | 拼词还原是有损的，句中的数字和标点会被吞掉 |
| 整理模式隐藏「更多」按钮 | 拖拽时碍事、易误触 |
| 文库拖拽不再回弹 | 文库没有本地副本，重排只在松手时发生，动画只能往回飞 |
| **渲染隔离**（结构优化 1） | 滚动时整棵树重渲染 —— 这才是当初卡顿的真正原因 |
| **抽出导入层**（结构优化 2） | 加新格式要改界面代码 |
| **epub 导入** | 导入层的第一次检验：界面一字未动 |
| **一键填充（AI）** | 空白笔记只能一条条手工查 |
| 复习页工具栏瘦身 + AI 角标 | 按钮换行；标记太扎眼；改过之后标记不消失 |
| **卡片可拖拽排序** | 复习顺序只能按添加先后 |
| **卡片改为就地编辑** | 编辑模式把排好版的卡片变成一张表单：三个字段各套一个灰框，卡片还猛地长高 31% |
| **取词抽屉重做** | 输入框 32px 太小、音标与词性平分宽度、白底压白底浮不起来、顶部一条骗人的「可拖拽」横杠 |
| 整理模式随侧栏收起自动退出 | 收起来它还留着，下次呼出仍在整理态 |
| 导出的备份改名「文库备份-日期」 | 手机上写死叫 backup.json，攒几个就分不清谁是谁 |
| 右侧栏只点单词才跳转 | 想看释义却被弹回正文 |
| 右侧栏在手机上去掉关闭键 | 与左侧栏一致；宽屏保留，见下方说明 |
| **多 AI 供应商**（见第七之二节） | 只能用 DeepSeek 一家；换一家要改代码 |

这批做完时测试是 117 项；第七、七之二两节又各有增加，现在共 230 项。

---

## 七、~~统一标注模型（短板 1）~~ ✅ 2026-08-29 已完成

**核心变化**：笔记原来的键就是它的坐标（`L0W2` = 第 0 行第 2 个词），
位置即身份，所以正文一编辑身份就变，只能靠事后「对账」把笔记一条条搬回去。
现在每条标注有一个**永不改变的 id**，坐标降级成它身上的一个属性。

单词、短语、句子从此是同一种东西，只差范围长短 ——
单词的首尾是同一个坐标，短语和句子的首尾是两个坐标。

| 从前 | 现在 |
|---|---|
| 单词笔记以坐标为键，住在主库 | 一张标注表，每条有稳定 id |
| 句摘另一套系统，住在浏览器 localStorage | 同一张表里，`type: 'sentence'` |
| 备份要从两个地方拼数据 | 只从一处取 |
| 孤儿要编一个假坐标 `orphan:xxx` 才不撞车 | 孤儿就是「位置为 null」 |
| 加「短语」要把逻辑写第三遍 | 已经是同一种东西，不用写 |

**你能看见的行为一条没变**：正文下划线、句摘虚线、长按取词、
「原文已删除」弹窗与角标、删除入口、原文改回来自动复活 —— 全部照旧。

### 唯一一处看得出来的变化

复习页的卡片顺序。从前没有排序，卡片按「你当初添加的先后」排；
现在按**正文顺序**（跟着文章从头排到尾）。内容一条不差，只是排列变了一次。

### 顺手修掉的一个旧 bug

从右侧栏点进去修改一条已有句摘，保存后会多出一条一模一样的
（旧实现在那里一律新增，不看有没有）。现在同范围已有就改它。

### 安全网

- 旧的 notes 与 localStorage 里的句摘**原样保留没删**，代码也留着并加了标记。
  等真机上用一阵子确认无误，再连同代码一起清理
- 「迁移过没有」的标记存在数据里，不是 localStorage —— 后者会脱节：
  清掉之后迁移会重跑，而重跑是拿旧数据重建整张表，会冲掉迁移后新加的标注
- 恢复迁移前导出的老备份会自动补跑迁移，否则恢复回来的笔记一条都不显示
- 导出的备份里仍额外写一份旧形状的 sentences，万一要退回旧版本 APK 认得

### 验证

**迁移预演**（拿真实备份跑，只读不写，不碰任何数据）：

```bash
node scripts/preview-migration.ts "文库备份-2026-08-29.json" --dump
```

用真实备份跑过：17 条（15 单词 + 2 句摘）进、17 条出，逐篇核对全对得上；
再做了一遍逐字段核对 —— 音标 / 词性 / 释义 / 原形 / AI 标记 / 句型 / 翻译
一个字都没变。

**真机数据跑界面**：把那份备份灌进浏览器、模拟手机首次启动新版本，逐项验过 ——
迁移到位、句摘 id 不变、阅读页 15 条下划线与 2 段句摘虚线位置正确、
复习页 15 张卡片按正文顺序；长按新建 / 修改（id 不变、不产生重复）/ 删除、
编辑正文触发「原文已删除」弹窗、选保留后位置置空且顶上来的词不被误画线、
把词打回去后自动复活且 id 不变。

测试增至 188 项。

### 这一步解锁了什么

短语归类、卡片可拖拽排序、自动检索词典划生词、有声阅读的「读到哪高亮到哪」——
这四项从前都卡在「笔记没有稳定身份」上，现在前置条件都齐了。

---

## 七之二、~~多 AI 供应商~~ ✅ 2026-08-29 已完成

**一句话**：填充不再只能用 DeepSeek，任何一家「OpenAI 兼容」的服务都能接上。

在复习页点「一键填充」→「AI 设置」，里面多了一排选择：

- **DeepSeek** —— 和从前一样，粘个 Key 就能用，地址和模型都是填好的
- **自定义** —— 自己填**服务地址**和**模型名**，通义千问 / 智谱 / Kimi / OpenAI，
  以及自己电脑上跑的 Ollama，都是这一种

**为什么不是一家一家写**：这些服务对外是同一套接口（`/chat/completions` +
`Bearer` 认证），差别只有地址和模型名两样。所以做法是把这两样从代码里挪出来
变成配置 —— 一份实现接所有家，以后再出新的一家也不用改代码。

### 用起来的几个细节

- **地址随便粘**：结尾多个斜杠、不写 `https://`、把文档里那条完整的
  `.../chat/completions` 整个粘进来 —— 都认，保存时自动规整成正确的样子
- **「测试连接」按钮**：拿 `stood` 这个词真跑一次，当场看到模型给的释义。
  填错地址或模型名不必等填充跑一半才发现
- **各家的 Key 分开记**：从 DeepSeek 切到别家再切回来，不用重新粘
- **错误说人话**：连不上会说「连不上 https://xxx，请检查网络和服务地址」，
  404 会指向「地址或模型名不对」，而不是甩一个状态码
- **兼容性兜底**：个别服务不认「强制 JSON 输出」这个参数，遇到就自动去掉重试一次，
  并记住这件事，后面几批不再白试

### 窄屏上的一次返工（同日）

第一版在手机上「自定义」那屏**显示不全、还能左右轻微滑动**。量出来才知道不是「字多」，
是**一整串没有空格的网址**：提示语里那条示例网址要 302px，而卡片里文字的可用宽度只有 275px，
断不下来就把卡片顶宽，整页跟着能横滑。输入框里的提示「服务地址，如 https://...」同理，
264px 塞进 257px 的框，尾巴被切还没法滑动去看。

三处一起改：

- 提示语删掉那条长网址，示例挪到输入框下面一行小字（`例：api.moonshot.cn/v1 · moonshot-v1-8k`）,
  省掉 `https://` 是有底气的 —— 保存时会自动补上
- 输入框提示词缩成「服务地址」「模型名」
- **根上的那道兜底**：卡片里的文字一律允许在长串中间断行。
  这条比前两条要紧 —— 连不上时错误信息里必然带着用户自己填的那串网址，
  不断行的话换个网址又会犯

顺手还修了错误信息：从前把服务商返回的**整坨 JSON** 原样倒在界面上，
现在只取里面那句话（不是 JSON 的仍原样截断，信息不丢）。

窄到 320px 实测：页面不横滑、卡片内不横滚、三个按钮仍在一行。

### 再一次返工：安全键盘弹出来关不掉（同日）

手机上**第一次**点填充按钮，安全键盘会跟着弹出来，而且关不掉。

根因是一帧闪烁：弹窗的配置原本给的是空初值，等 effect 跑完才从本机读出真正的 Key。
于是第一次打开先按「还没设过 AI」渲染了一帧，那屏上的**密码输入框**被代码自动聚焦
（本意是方便直接粘 Key）—— 安卓的安全键盘就是这么被叫出来的；紧接着 Key 读到了，
界面切走，输入框消失，键盘却还开着，**没有对应的输入框可关**。

两处一起治：

- 配置改成**在初始化时当场读出来**（`useState(loadConfig)`），不再有那一帧
- **取消自动聚焦**。手机上自动弹键盘本来就烦人，用户自己点更可控；
  也就再不会有「键盘弹出来而框已经没了」这种状态

顺带按用户意见调了那段提示：示例不再和提示之间空一行（它们是同一句话的两半），
示例改成两行「地址 …」「模型 …」，并且**把 `https://` 加回来** ——
不写的话看不出这是个网址（真填时不写也行，保存时会自动补上）。

### 「待填充」的标准改成「缺哪格补哪格」（同日）

用户反馈「填充按钮好像只数单词不数句子」。查下来**计数本来就是两者相加**
（造一条全空的句摘，按钮当场从 2 变 3），真正的原因是另一条规矩：

**从前只有「整条全空」才算待填充。** 单词卡有音标 / 词性 / 释义三格，
句摘卡有句型 / 翻译两格 —— 只要有一格写过字，这条就既不计数、也填不上。
于是「写了一半」的卡片没人管：句摘写了句型没写翻译，AI 帮不上忙，
数字里也看不见它。

现在改成：

- **有一格空着就算待填充**（`isWordNoteIncomplete` / `isSentenceIncomplete`）
- 填充时**只往空着的格子里写**（`mergeWordFill` / `mergeSentenceFill`）——
  「绝不覆盖用户写过的内容」这条底线收在这两个函数里，只此一处，
  别处不再各判一遍
- 每批回写前会重新读一遍最新数据：这中间用户自己补了几格的，也不会被盖掉

实测（把某个词的音标、某条句摘的翻译单独清空）：按钮从「填充 2」变成「填充 4」，
弹窗写「待填充：3 个单词·1 个句子」—— 正是用户说的那种数法。测试增至 237 项。

界面上的说法跟着改了：按钮提示「还有 N 条笔记没填全」，
弹窗里说明「只补空着的那几格，你写过的一个字都不动」。

### 填充按钮改为常驻（同日）

从前只有「还有没填全的笔记」才显示，全填完就消失。改成一直在：

- 有得可填：实心琥珀 + 报数（「填充 4」），和从前一样
- 没得可填：收成一枚安静的图标（只剩「填充」二字），与旁边「词/句」同一等级

除了位置固定、不会今天在明天不在，还顺手解决了一个死角：
**「AI 设置」只有这条路进得去** —— 从前全填完之后按钮消失，就再也改不了 Key 了。
没得可填时点开，弹窗写「待填充：都填全了」，「开始填充」是灰的，但「AI 设置」能进。

实测（375px）：两种状态按钮都是 30px 高，工具条 57px 不变，不换行。

### 老用户不受影响

从前那个只存一个 Key 的老格式会被自动认成「DeepSeek 的 Key」，
打开就是能用的状态，不用重新填。保存时也仍往老位置写一份 ——
万一要退回旧版本 APK，那边还认得。

代码上 `enrich/deepseek.ts` 改名成了 `openaiCompatible.ts`，
新增 `enrich/config.ts` 管供应商配置。测试增至 230 项。

---

## 八、待办清单（意向，未拍板）

2026-08-29 记录的一批想法。已做完的见第六节（含「多 AI 供应商」），剩下这些**都还需要先定细节**。

### 要先决定「做成什么样」

| 想法 | 卡在哪 |
|---|---|
| **同一文档内的重复单词也标为重点** | 文库复习是把同名词**合并成一张卡**记频次；单篇复习是一条笔记一张卡。单篇若也合并，**刚做的拖拽排序在那里就用不了**（合并出来的卡没有对应的一条笔记可写回顺序）。两条路：合并（放弃单篇排序），或每张卡都留着、重复的加个「×3」角标（排序还能用） |
| **主题色切换** | 现在的「主题」是三种纸色（标准/青灰/暖白），**全是浅色**。要的是换强调色（现在通体琥珀），还是**深色模式**？后者是大工程，每个颜色都要重配 |
| **设置功能更直观** | 设置现在散在三处：外观（左侧栏左下角浮层）、AI Key（藏在填充对话框里）、备份恢复（左侧栏）。大概是要收进一个设置页 |
| **AI 复核已写的笔记** | 现在的「一键填充」只填空白、绝不覆盖。复核是反过来：自己写的让 AI 挑错。关键是**结果怎么交付** —— 直接改掉（危险），还是并排列出「你写的 / AI 的建议」逐条选（推荐，但界面工作量实在） |

### 方向清楚，动手前确认一下即可

| 想法 | 说明 |
|---|---|
| **侧栏左右滑动开关** | 风险在误触：正文要长按取词，横向也可能有滚动。建议**只认从屏幕边缘起手**的滑动 |

### 一条已知的不对称，别当成 bug

手机上右侧栏没有关闭键（与左侧栏一致，靠点遮罩或安卓返回键关闭），
但**宽屏上保留着** —— 那边遮罩是隐藏的，浮动的「笔记」按钮在打开状态下也不显示，
删掉就再没有关掉它的办法了。

---

## 九、下一步从哪接

三个结构短板**全部做完了**，只剩虚拟滚动 —— 等真被长文卡到再说。

**地基解锁的四项**（从前都卡在「笔记没有稳定身份」上）：

- ~~**卡片可拖拽排序**~~ ✅ 2026-08-29 已完成，见下
- **短语归类** —— 已经是同一种东西，只是 `type: 'phrase'`，主要是界面活。
  要定的是交互：长按连起两个词时，怎么告诉 App「这是短语不是句子」
- **自动检索考试词典划生词** —— 代码不难，**难在先找到一份可用的词表**
- **有声阅读的「读到哪高亮到哪」** —— 声音进度对应到 start/end 就行

### ~~卡片可拖拽排序~~ ✅ 2026-08-29

复习页右上角那支笔（编辑模式）里，卡片左上角会出现拖拽手柄，按住它拖。
不新增按钮，词和句各排各的。

**只在「单篇文档」的复习里开放。** 文库复习的条目是按拼写合并出来的
（同一个词在三篇文档里各标过一次，那里只显示一条、频次记 3），
拖了没有一条标注可以写回去，所以那边不给手柄。

不依赖任何前置、想做就能做的：**有声阅读第一步（能读）**、
**字幕导入**、**导出 HTML/Markdown**、**拍照识别试水**。

已知待观察：
- **标注模型刚换，先在手机上正常用几天**。旧数据和旧代码都还留着，
  确认无误之后再做一次清理（删掉 data.notes、旧的 reconcilePage 及其测试、
  storage 里那几个已无人调用的旧写入函数 —— 文件里都加了标记）
- epub 只用自造的标准包测过，**真实电子书千奇百怪**，遇到怪的再针对性适配
- AI 填的音标准不准还没有实际数据。若错得多，就补一份词典 +「变形还原」
  （`stood -> stand` 那一层）；错得少就省了

**跑测试**：

```bash
npm test
```

**打包到手机**：见 [发布到手机.md](发布到手机.md)

---

## 十、~~有声阅读第一步：点词/点句读出来~~ ✅ 2026-08-29 已完成

**做成什么样**：复习页里，单词卡上的那个英文词、句摘卡上的那句英文，
本身就是按钮 —— 点一下读出来，后面跟一枚小喇叭点明它可以点。

- 正在念的那个字变亮，念完自动复原
- **点正在念的那个 = 停下来**；念着的时候点另一个 = 直接换成后一个，不排队
- 单词放慢到 0.85 倍速（一个词一闪而过听不清），整句用正常语速
- 卡片别处照旧是「点一下翻开答案」，两件事不打架
  （外层那个点击本来就跳过按钮）

**没有做阅读页的整篇朗读** —— 用户明确说机械音通读没什么好听的，
要的是查发音。这也让第一步的范围小了很多。

### 结构

又是同一个路子（`src/speech/`）：`types.ts` 定约定、`webSpeech.ts` 是实现、
`index.ts` 是入口。现在用的是 WebView 自带的 Web Speech API ——
它背后接的就是手机系统的「文字转语音」引擎，不引任何库、不联网。

**要换成安卓原生插件，只改 `index.ts` 一个文件。**

几处防身的细节：

- **挑嗓子**：优先本地合成（离线可用、开口快），其次 en-US；
  一个英文嗓子都没有就交给系统按 `lang` 自己看着办，绝不拿中文嗓子念英文
- **兜底时长**：安卓 WebView 上 `onend` 偶尔不来，真发生了高亮会一直挂着像卡死，
  所以按字数估一个上限，到点就当念完
- **读不了的手机**：`speechSynthesis` 都没有的话，喇叭根本不画 ——
  不摆一个按了没反应的按钮
- **读不出来时**：工具条下面出一条黄色提示，直说「去系统设置里装一个英文语音包」

### 实测

浏览器里拦下真正交给系统的内容逐项核对：

| 验的东西 | 结果 |
|---|---|
| 单词发出去的内容 | `stumble`，`en-US`，语速 0.85 |
| 句子发出去的内容 | 整句原文，`en-US`，语速 1 |
| 念时高亮 / 念完复原 | 100ms 高亮，500ms 已复原 |
| 点正在念的那个 | 停下 |
| 念着时点另一个 | 前一个熄灭，后一个亮起 |
| 点词会不会连带翻开答案 | 不会 |
| 375px 排版 | 卡片、按钮尺寸正常，页面与卡片都不横滚 |
| 编辑模式 | 拖拽把手与朗读按钮共存，互不影响 |

**真发不发得出声，只有在手机上才知道** —— 浏览器这边是把发声拦下来验参数的。

测试增至 247 项。

### 后来改成了原生（同日，第一版在手机上不出声）

**现象**：手机上点词点句毫无反应，**连喇叭图标都没有**。

**根因**：安卓的 WebView **没有实现网页版朗读接口**（`speechSynthesis`）——
手机 Chrome 有，WebView 没有，这是它的老毛病。代码检测到没有就把喇叭藏了，
所以用户连图标都看不见。**电脑浏览器里测不出这个问题**，
因为电脑 Chrome 是有的。

**改法**：加一个原生实现，绕开 WebView。

- 装了 `@capacitor-community/text-to-speech@8.0.2`（正好配 Capacitor 8），
  直接调系统的 TextToSpeech
- `src/speech/nativeSpeech.ts` 是新实现；`index.ts` 里分路：
  **手机上走原生，电脑浏览器里仍走网页版**（开发时要在浏览器里调界面）
- 约定层（`types.ts`）和界面**一个字没改** —— 当初留的口子这次真用上了
- 错误话术收进 `errors.ts`（纯函数、好单测），两条路共用
- 缺英文语音时，提示条上多一个「**去安装**」按钮，直接跳系统的语音包安装界面
  （`openInstall()`，安卓独有）

**这次学到的**：凡是「浏览器里能跑」的能力，装进 App 之后未必还在 ——
WebView 不等于 Chrome。以后凡涉及系统能力（朗读、相机、文件、分享），
**先在真机上验一个最小版本，再往上盖界面**。

测试增至 250 项。

### 喇叭图标：调了两版，最后删了

原本在文字后面跟一枚小喇叭，点明「这里可以点」。用户觉得偏高，
量出来确实：图标落在**大写高度**的正中，而卡片上的词多是小写，
相对小写中线高了 2px。先改成按小写中线（`align-middle`），
又按用户提议改成「**看结尾那个字母的大小写**」分别对齐 ——
两版都量到了 0.4px 以内。

**但用户看着还是别扭，最后决定删掉。**
文字本身就是按钮、点了会变色，反馈已经够了。
`utils/speakIconAlign.ts` 连同它的测试一并删除。

留个教训：**这种「加个小图标点明可点性」的补丁，在触摸端未必值**。
真正让人看出可点的是「点下去有反应」，不是多一枚图标。

### 踩到的一个坑（记下来免得再犯）### 踩到的一个坑（记下来免得再犯）

`useSpeak()` 一开始被放在了「没选文档就提前 return」的**后面**，
React 的钩子数量因此忽多忽少，整页白屏。**钩子必须在所有提前 return 之前调用。**
这错在浏览器里一眼就看见了，单测看不出来 —— 又一次说明光跑单测不算验过。

---

## 十一、~~新标注按正文顺序插入~~ ✅ 2026-08-29 已完成

**问题**：文中 `apple and ear`，先标了 apple 和 ear，回头再标 and ——
它会跑到复习页卡片列表的**末尾**，而不是待在 apple 和 ear 中间。

**根因**：新建标注给的 order 是 `nextAnnotationOrder`，也就是
「同文档同类型里的最大值 + 1」= 永远排最后。这是当初做拖拽排序时定的，
只考虑了「加到末尾」这一种情况。

**改法**：`utils/annotationOrder.ts`（纯函数、13 项单测）——
按坐标（`L第几行W第几个词`）找到**正文里紧挨着它前面的那一条**，排在它后面；
它要是正文里最靠前的，就排到最前面。

**为什么不干脆整篇按正文重排**：那会把用户拖出来的顺序冲掉。
只认新来的这一条该插在哪，别人的相对次序一概不动 —— 这一点专门写了测试。

取值可能是小数（两条之间取中点）。排序只看大小，够用；
用户下次拖动时 `reorderAnnotations` 会重新编成整数。

孤儿标注（原文已删除、没有坐标）不参与比较，也不会被挤掉。

### 顺序已经被拖乱时，新的落在哪

规则只有一条：**紧跟「正文里它前面那条」，插在那条后面** —— 那条现在排在哪，
它就去哪。正文里它前面没有别人，就插到「正文里它后面那条」的前面。

三种情形都有单测钉住（正文顺序都是 a b c d）：

| 现在的排列 | 新词在正文里的位置 | 结果 |
|---|---|---|
| `d b a c` | 夹在 b 与 c 之间 | `d b 新 a c`（跟着 b） |
| `d b c` | 比谁都靠前 | `d 新 b c`（插在正文后继 b 前面） |
| `d b c` | 比谁都靠后 | `d 新 b c`（跟着 d，而 d 被拖在第一位） |

所以**乱过之后，新卡片不一定出现在列表末尾，也不一定"看着像正文顺序"** ——
它只保证挨着自己在正文里的邻居。这是有意的：列表已经被手动排过，
再谈「正文位置」就没有一致的参照了，「挨着邻居放」是唯一说得通的做法。

要想让某篇文档重新变回正文顺序，现在只能手动拖。
将来若需要，可以加一个「按正文顺序重排」的按钮 —— 那是**用户主动要求**的重排，
和这里「不擅自冲掉手动顺序」并不矛盾。

### 实测（浏览器里走真实流程）

在示例文档里长按 `or`（坐标 L38W2，正文中夹在 Flying `L38W0` 与 below `L38W4` 之间）
保存，然后看库里的顺序：

```
Flying(L38W0 · 6)
or    (L38W2 · 6.5)   <- 新标的，落在正中间
below (L38W4 · 7)
```

复习页卡片顺序同样如此。**其余 15 条的 order 一个没动** ——
这份测试数据本身就不是正文顺序（stood 在正文最前，却排在第 3 位），
正好验证了「不冲掉已有排列」。验完把测试笔记删掉了。

---

## 十二、~~短语模块~~ ✅ 2026-08-29 已完成

**一句话**：长按连选两个词之后，抽屉顶部多了「短语 | 句子」两个标签 ——
从前一段范围只能存成句摘，现在可以存成短语。

### 三个当初卡着的决定（用户拍板）

| 决定的事 | 结果 |
|---|---|
| 怎么告诉 App「这是短语不是句子」 | **抽屉里加一个切换**，不新增手势。默认按长度猜：**四个词以内当短语** |
| 短语卡放复习页哪里 | **并到「词」页签**。短语本质也是词汇，和单词一起背最自然，工具条也不必变三态循环 |
| 短语卡填什么 | **释义 + 用法说明**。释义存 `definition`、用法存 `grammar`（和句子共用同一批字段，备份与对账不必为短语再开一路） |

### 动手时才发现的一件事：排序得并组

`order` 原本是按「同文档 + **同类型**」编号的。短语要和单词显示在同一列卡片里，
可两者各有一套编号（词 0..14、短语 0..2），混在一起按数值排就会乱插。

所以引入了**排序分组**：`vocab`（单词 + 短语）和 `sentence` 各一条队。
`nextAnnotationOrder` / `orderForNewAnnotation` / `reorderAnnotations` 都改成按组。
实测新存的短语落在 6.5，正好排在它首词 Flying（6）之后、below（7）之前。

### 都改到了哪儿

一条短语要同时在五个地方露面，漏一处就是「存进去了但看不见」：

- **正文**：一条**连续实线**（穿过词与词之间的空格，看得出是一个整体）。
  句摘仍是虚线，单词仍是词底下的下划线 —— 三者一眼可分。
  同一段既是句摘又含短语时，实线在内、虚线在外，两条都画
- **抽屉**：「短语 | 句子」切换；短语两格（释义 / 用法），句子两格（句型 / 翻译）。
  重新选中一段已存过的，会自动回到它当初的类型并把内容填回来，还给删除按钮
- **复习页**：「词」页签里，带一枚「短语」标签，不显示音标和词性，
  释义下面多一行用法。拖拽排序照旧可用（和单词同一条队）
- **右侧生词板**：也在「生词」那一栏，带「短语」标签
- **一键填充**：短语有自己的提示词（要的是搭配用法，不是音标词性），
  25 条一批。仍然只补空着的格子

**对账（正文编辑后搬位置）是免费继承的** —— 它本来就按「单词 vs 范围」分支处理，
短语走的是和句摘同一条「范围收缩」的路。

### 实测（浏览器里走真实流程）

长按 `Flying` 再长按 `or` → 抽屉默认选中「短语」（三个词）→ 填两格 → 保存：

| 验的东西 | 结果 |
|---|---|
| 存进去的形状 | `Flying high or`，L38W0–L38W2，释义/用法各就各位 |
| 排序 | order = 6.5，落在 Flying(6) 与 below(7) 之间 |
| 正文 | 一条 2px 连续实线盖住三个词；两条句摘虚线不受影响 |
| 复习页 | 卡片在 Flying 与 below 之间，带「短语」标签、无音标行 |
| 右侧生词板 | 有这一条，带「短语」标签 |
| 重新选中同一段 | 自动回到「短语」、内容填回、出现删除按钮 |
| 六个词的范围 | 默认落在「句子」，字段是句型/翻译 |
| 删除 | 标注没了、实线没了、两条句摘虚线还在 |

测试增至 285 项（新增短语填充 11 项、读模型分路 8 项）。

### 下一步：一键划词（决定已拍板，未动手）

阅读页放一个按钮，AI 通读全文、挑出重点词与短语并直接填好。三个决定：

1. **依据**：AI 判断，**按难度等级**，用户自己选档（四级 / 六级 / 考研 / 雅思）。
   不走考试词表 —— 难在弄到带释义的数据，而且词表基本覆盖不了短语
2. **重复词**：同一个词在一篇里出现多次，**只划第一次**
3. **短语**：模块已经就绪，可以一起划

**真正的难点是「定位」不是「挑词」**：AI 返回的是文字，得对应回坐标。
风险是 AI 给原形（take off）而正文是变形（took off）、或者干脆编一个文里没有的词。
解法：要求 AI 返回**原文里的确切写法 + 所在行号**，在那一行里精确查找，
**找不到就跳过并如实报数**（「挑了 42 个，划上 38 个，4 个没对上」）。

还要配一个**整批撤销** —— 一次划出几十条，不能撤销的话用户不敢按。

---

## 十三、~~短语模块的两处返工~~ ✅ 2026-08-29

用户在手机上试完短语模块，说「问题挺多」。问清楚是**两件事**，一件是观感、
一件是真 bug。

### 一、三条线撞在一起

**用户的原话**：「划短语的那条实线和划句子那条虚线重叠了，而且划词已经也是实线了。」

两个毛病同时存在：

- **短语和单词长得一样** —— 都是 2px 琥珀实线，只有长短之分
- **短语的实线把句摘的虚线整条盖住**

**根因（量出来的，不是猜的）**：短语用 `border-b-2`、句摘用 `border-b`，
两层 span 套着，各自 `padding-bottom: 1px`。行内元素的内边距**不撑开高度**，
所以两层的内容盒底部在同一个位置，两条边框必然画在同一条水平线上。

**改法**：三条线**换成同一套画法**（`text-decoration`），都从基线往下量，
才谈得上「错开」。位置一律用 `em` —— 正文字号是用户可调的。

线型也各不相同，不必靠长短去分辨：

| | 线型 | 粗细 | 离基线 |
|---|---|---|---|
| 单词 | 直实线 | 2px | +2px |
| 短语 | **波浪线** | 1px | +0.36em |
| 句摘 | 虚线 | 1px | +0.6em |

**纵向预算是量出来的**（18px 字号、行距 1.8）：基线往下 4px 是内容盒底部
（从前两条线都落在这儿），9.7px 是行盒底部，再往下还有 5.7px 行间空气
才碰到下一行的字顶 —— 也就是基线以下约 15px 都是安全的。

**为什么没走「底色」这条路**：淡琥珀底色已经被三个状态占着了
（按住中 `amber-300/70`、当前选中的词 `amber-200/80`、正在框选句摘范围
`amber-100/70`）。短语再用底色，会和「正在框选」撞脸。

三个类名收在 `LyricEditor.tsx` 顶部（`WORD_LINE_CLASS` / `PHRASE_LINE_CLASS` /
`SENTENCE_LINE_CLASS`），三条线摆在一处才好对着调。

### 二、右侧生词板里短语总排在最后

**用户的原话**：「新的短语会被直接放进最后，而不是像划新词那样找到文中的位置
参与排序，但是复习模式下短语卡片的位置似乎又没有问题。」

**根因**：右侧栏那份清单是**分两步拼**的 —— 先把单词按顺序排好塞进去，
再用第二个循环把所有短语一股脑追加在末尾，从头到尾没让两者比过顺序。
数据本身没错（短语的 `order` 存的就是 6.5 这种正确值），纯粹是拼清单的方式不对。

而复习页走的是**另一条路**：直接从标注表里把单词和短语一起捞出来、一起按 order 排。
所以「复习页对、右侧栏不对」—— 同一份数据，两条路。

**改法**：新增读模型 `buildVocabList()`（`utils/annotationViews.ts`），
右侧栏改走它，和复习页同一个算法。App.tsx 里那段 44 行的拼装代码缩成 4 行。

顺手统一的两件小事：短语不再往清单里塞音标/词性（卡片上本来就不画那两格）；
键撞车时退回用 id 的规则和 `buildNotesIndex` 保持一致。

### 实测（浏览器里走真实流程）

造了一段三线叠加的极端情况：句摘 `Is to wish my life away` 里套一条短语
`wish my`，短语里再套一条单词笔记 `wish` —— 三条线同时画在 `wish` 这一个词上。

| 验的东西 | 结果 |
|---|---|
| 单词线 | solid / 2px / 离基线 2px |
| 短语线 | **wavy** / 1px / 离基线 6.48px |
| 句摘线 | dashed / 1px / 离基线 10.8px |
| 三条线的间距 | 彼此隔开 1.5px 以上；最深的一条离下一行字顶还有 3.6px |
| 嵌套关系 | 句摘 ⊃ 短语 ⊃ 单词，三条同时画得出来 |
| 旧的 border 画法 | 页面上一处不剩 |
| 右侧栏顺序 | `wish` → **wish my** → `dream`；`Flying` → **Flying high or** → `below` |
| 复习页顺序 | 与右侧栏**逐条一致** |

验完把测试标注删干净了（20 → 17 条，无残留）。

新增 `utils/buildVocabList.test.ts`（8 项）钉住排序那个 bug，
测试增至 293 项。

### 留个教训

**同一份数据被两处界面用，就要走同一条路。** 这次的 bug 不是算错了顺序，
是右侧栏压根没算 —— 它自己拼了一份。读模型（`annotationViews.ts`）当初就是
为这个立的规矩，右侧栏是唯一一处还在自己拼的，现在收回来了。


---

## 十四、API Key 那格粘贴不了（2026-08-29，第一轮）

**用户的原话**：「填充功能那里不是要先输入 deepseek 的 api 吗？这个输入 api 的框
居然没法使用粘贴功能。」

### 诊断：代码里没有任何东西挡着粘贴

翻了一遍可能拦路的地方，全都干净：

- 全局样式里没有「禁止选中文字」（`user-select: none`）那类规则
- 弹窗和它的外层没有任何拦截长按的代码 ——
  正文里那个「长按取词」确实会吃掉长按，但只挂在单词 span 上，跟弹窗不沾边
- 安卓那边是 Capacitor 的默认壳子，`MainActivity` 是空的，没做过任何定制

那个输入框就是一个最普通的 `<input>`。所以问题在 **WebView 本身**，
两个已知原因都对得上：

1. **安卓 WebView 长按「空的」输入框，经常根本不弹「粘贴」气泡**。
   手机 Chrome 会弹，WebView 不弹。而 API Key 这格第一次用时永远是空的
2. 那格是 `type="password"`，部分 WebView 版本对密码框不给选择/粘贴菜单

**这两个在电脑上分辨不出来** —— 电脑 Chrome 两种情况都正常。
又是那条老教训：**WebView 不等于 Chrome**（朗读那次栽的是同一件事）。

### 第一轮：先试不装插件的做法（用户拍板）

只改一处：**Key 那格默认改成明文文本框**（`type="text"`），
右边加一枚眼睛可以遮起来。同时关掉自动大写/自动纠错/自动填充
（`autoCapitalize` / `autoCorrect` / `autoComplete` / `spellCheck`）——
Key 是一串随机字符，被输入法「纠正」过就废了。

这一改同时办两件事：

- **验证假设 2**：如果换成普通文本框长按就能粘了，那原因就是密码框
- **本来就该有**：Key 是一长串，粘成一串圆点的话粘错、粘漏都看不出来，
  只能等填充跑起来报错才知道。现在粘完能当场核对

默认明文而不是默认遮住 —— 自己的手机、自己的 Key，没什么可藏的；
真要遮就点那枚眼睛。

### 浏览器里验过的

| 验的东西 | 结果 |
|---|---|
| 那格的类型 | `text`（从前是 `password`） |
| 输入法干扰 | autocomplete/autocapitalize 都是 off，spellcheck 关 |
| 眼睛按钮 | 36×36，点一下变 `password`、标签变「显示 Key」，再点回来值不丢 |
| 文字会不会被眼睛压住 | 输入框右内边距 36px，正好等于按钮宽度，不会 |
| 375px 窄屏 | 输入框 275px，页面不横滑、卡片内不横滚 |

### 如果这一轮还是粘不了

那就是原因 1（WebView 对空输入框不弹粘贴菜单），跟密码框无关。下一步：

- **先试不用插件的粘贴按钮**：Capacitor 是从 `https://localhost` 提供页面的，
  属于「安全上下文」，`navigator.clipboard.readText()` 有可能直接能用
- 不行再装 `@capacitor/clipboard`，走原生 —— 和朗读那次同一个路子

**系统能力这种事只有真机说了算**，浏览器里测不出来。


---

## 十五、~~AI 角标在句子卡上多占一行~~ ✅ 2026-08-29

**用户的原话**：「句子卡的四芒星角标不知怎的会加到下一行制造一行空白。」

**用户观察到的那个不对称就是线索**：单词卡没这毛病，只有句子卡有。

### 根因：inline-block 是一个整块

角标从前跟在朗读按钮的**后面**：

```
<p>
  <button>整句英文</button>
  <角标 />
</p>
```

`<button>` 是 `inline-block`。inline-block 是一个**整块** ——
里面的文字换行之后，这个块的宽度就变成**整行宽**（不是最后一行的宽度）。
后面再没有位置放角标，角标只好掉到下一行，独自占一行。

- **句子长了才会换行**，所以只有句子卡看得出来
- 单词就一个词，块永远很窄，角标挤得下 —— 这就是那个不对称的由来

**量出来的**（375px 窄屏，一条三行的句子）：按钮 66px 高、外层容器 97px 高
—— 凭空多出 **31px** 的空白。

**试过但行不通的改法**：把按钮改成 `display: inline`。
浏览器**强制**把按钮变成 inline-block，连 `!important` 都压不动
（算出来仍是 `inline-block`）。这条路直接堵死。

### 改法：角标搬进按钮里面

```
<button>整句英文<角标 /></button>
```

角标成了按钮内部行内流的一部分，跟着最后一个词走，自然待在最后一行。
单词卡也一并搬进去，两处保持一致。

顺手治了第二个毛病：角标那个 span 从前会**撑到一整行的行高**
（10px 的图标占出 24px 的盒子），上标又是抬高的，于是每条带角标的卡片
行高都被顶高 8px。加 `leading-[0]` 之后盒子就是 10×10。

代价：点那枚 10px 的角标现在会触发朗读（从前是翻开答案）。
它紧挨着本来就点了会朗读的文字，这个改变可以忽略。

### 实测（375px 窄屏）

| 验的东西 | 改前 | 改后 |
|---|---|---|
| 三行长句：容器比按钮高出 | **31px**（多一整行） | **1px** |
| 单行短句：容器比按钮高出 | 8px | 2px |
| 角标盒子 | 10×24 | 10×10 |
| 角标离最后一个词 | （掉到下一行） | 2px，同一行 |
| 单词卡 | 正常 | 正常（多出 0px） |
| 右侧栏生词条目 | 正常 | 正常（那里本来就在按钮里面） |
| 右侧栏句摘条目 | 正常 | 正常（纯文本，不经过按钮） |

右侧栏的句摘**从来没坏过** —— 它的句子是直接写在 `<p>` 里的纯文本，
不经过按钮。这一点正好反证了根因就是那个 inline-block 按钮。

验完把测试数据清干净了（17 条标注，无残留）。

### 留个教训

**inline-block 里的文字一换行，这个块就占满整行宽** ——
凡是「文字后面跟一个小标记」的排版，标记必须放在那个 inline-block 的**里面**，
放外面迟早会掉下去。而且这个坑**只有文字够长、真换行时才现形**，
短文字上怎么试都是好的。


---

## 十六、~~改了卡片，AI 角标却不消失~~ ✅ 2026-08-29

**用户的原话**：「设计的初衷是当我修改了卡片的内容后四芒星角标就消失，现在修改
单词卡片好像是必须清空一个框才会触发消失……而对于短语卡，问题不止于此，
你修改了退出编辑模式结果删掉的东西又回来了，它好像并没有被记忆。句子好像没问题。」

查下来是**两个各自独立的 bug**，凑巧症状看着像一个。

### bug 一：改动只在「失焦」时才保存

角标不消失只是表象 —— **真正没发生的是保存**。

那两格输入框**只有 `onBlur` 一个保存触发点**。手机上改完直接点铅笔退出编辑模式时，
输入框往往还没来得及失焦就被换成阅读态了，那次改动就这么悄悄没了：
数据没存，角标自然也不会消失。要是中间先点了别处（输入框失了焦），
它就存得上 —— 这就是「有时灵有时不灵」的由来。

**浏览器里复现过**：把释义从「飞鸟」改成「鸟」，不触发失焦、直接点铅笔退出，
落盘的仍是「飞鸟」，`auto` 还在。

**改法**：进编辑模式时登记一个清理函数，**离开编辑模式（或卡片被卸掉）时兜底保存一次**。
不管失焦事件来不来得及，改动都保得住。单词卡、短语卡、句子卡三处都加了 ——
句子卡有同样的隐患，只是还没撞上。

### bug 二：短语的改动**根本存不进去**

`updateAnnotationsByWord()` 里写死了一句 `if (a.type !== 'word') return a` ——
**短语被静静跳过**：不报错、也不写库。所以短语卡改完退出就打回原形，
连带角标也永远清不掉。这跟 bug 一无关，给足正常的失焦也一样存不进去。

**根子在名字**：这个函数叫 `updateAnnotationsByWord`，「word」既指
「按拼写找」又被读成「只管单词」。后来加短语时，数据层免费继承了一大半，
唯独这一处的类型判断没人想起来改。

**改法**：函数改名 `updateVocabByText`，判断改成「**除了句摘都算**」。
单词和短语在复习页本来就是同一列卡片，改法也该是同一条路。
短语的原文必然带空格，和单个词撞不了车。

### 实测（浏览器里走真实流程）

| 场景 | 改前 | 改后 |
|---|---|---|
| 单词卡改一个字 → 直接点铅笔退出 | 改动丢失、角标还在 | 存进去了、角标消失 |
| 短语卡改释义 → 正常失焦 | **完全没存**、角标还在 | 存进去了、角标消失 |
| 短语卡改用法 → 正常失焦 | 完全没存 | 存进去了，释义不受影响 |
| 短语卡改完 → 直接点铅笔退出 | 完全没存 | 存进去了、角标消失 |
| 句子卡改完 → 直接点铅笔退出 | 改动丢失 | 存进去了、角标消失 |

新增 2 项单测钉住「短语也要改到」和「短语与单词按原文各认各的」，
测试增至 295 项。

### 留个教训

**函数名里的类型词会变成陷阱。** `updateAnnotationsByWord` 里的「Word」
本意是「按拼写」，读起来却像「只管单词」—— 加短语时全项目都改到了，
唯独这一处的 `type !== 'word'` 没人想起来。以后凡是「按某个键找一批标注」的函数，
名字里别带类型词，判断也宁可写成「排除谁」而不是「只要谁」。

**还有：只有一个保存触发点是危险的。** 输入框的 `onBlur` 在手机上不保证来得及 ——
组件先被卸掉，事件就永远不来了。凡是「边改边存」的界面，
除了失焦还要有一条卸载时的兜底。
