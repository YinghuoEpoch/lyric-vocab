# 后续规划

> ## 现在停在哪（2026-09-04 交接·第十六次）
>
> **文档太长了，先看下面那份「目录与索引」，别通读。**
>
> ### 分支状态：`sidebar-follow` 等真机验收
>
> **在分支上（第七十节）**：笔记栏跟着正文滚，当前那条左边一道细竖线。
> 浏览器里量全了（见那一节的表），**唯一没走通的是「刚亲手划完一条就跟过去」**
> —— 要模拟长按取词那套手势。真机上留意这一条。
>
> 第六十九节（设置子屏）他已经验过并进 `master` 了。
> 没有别的待合并分支。测试 **657 项**全过；
> `npx tsc -b --force`、`npm run build` 都通过。
>
> **⚠️ 第六十六～六十八节是用户让直接并的，没走「真机验过才并」那条老规矩。**
> 这一批动的是**同步的存取结构**：索引里摘掉了旧 `notes` 和阅读进度，
> 云端多了一个 `progress.json`。**他两台真机上第一次同步值得多留意。**
>
> 他要是报「笔记没了 / 进度不对 / 同步报错」，先看这三个提交，
> 它们是**分开的**，可以单独 revert：
>
> | 提交 | 做了什么 | 出事的样子 |
> |---|---|---|
> | `ca45e6c` 记真账 | 累计流量，读数屏显示 | 最无害，几乎不可能是它 |
> | `be12aeb` 旧 notes 不上云 | 索引里摘掉 `notes` | 「恢复很老的备份恢复不出来」 |
> | `82e5e84` 进度单独走 | 索引里摘掉进度，新增 `progress.json` | 「阅读位置乱跳／回到开头」 |
>
> 另外 `aa96c5f`（同步被闸拦下来时补一次）也在这一批里。
>
> ### ⚠️ 这一天我算错过两次账，别再犯
>
> 用户问「笔记多了体积涨得快吗」「会不会用完额度」，我估了两次、**两次都错**：
>
> 1. 拿第六十二节里**另一个数据集**的数（1760 条笔记 17 KB）当成他的，
>    算出「一条约 10 字节、两万条以内不用管」。他一句
>    「我才划了 300 多条，每次上下就已经 30 KB」推翻 —— 真数大了五到八倍
> 2. 改口按「一天同步 180 次」估，算出一个月 157 MB。**又高了约五倍** ——
>    只有真有改动才上传
>
> **治法已经落地：不再估，改成记真账**（第六十八节）。
> 设置 → 开发者 → 同步数据，最上面那栏就是本月实际用掉多少。
> **下次他再问额度，去看那一屏，别自己算。**
>
> ### 手上的真数（2026-09-04，他的库）
>
> 14 个文库 / 201 篇文档 / 322 条笔记（单词 229、短语 51、句摘 42）/ 正文 4.29 MB。
> `data.json` **28 KB**（优化前 30 KB）：笔记 67%、文档壳 31%、文库 3%。
> 一条笔记平均：**单词 47 字节、短语 85、句摘 103**。
>
> ### 最新一批：笔记栏跟着正文滚（第七十节，在分支上）
>
> 他提的：文档一长、笔记一多，在侧栏里找那条笔记很麻烦。现在读到哪儿，
> 侧栏就落在哪条笔记上，句摘栏同理。三个决定都是他拍的板 ——
> **看不见才滚**（另两种是永远贴顶 / 永远居中，做成网页给他拖着选的）、
> **「最近」取前面那条**（不回跳）、**当前那条左边一道细竖线**。
>
> ⚠️ **这一轮连中三个环境骗局**（过渡不推进、改 scrollTop 不发 scroll 事件、
> 后台 setTimeout 被钳到 1 秒），三次都让我以为是 bug，**代码一次都没错**。
> 第七十节末尾专门记了这一段，比功能本身值钱。
>
> ### 上一批：设置的子屏漏登记（第六十九节，已验收并入）
>
> 用户报的两件，**他自己就猜到是同一个根子**：「同步数据」点进去停在主列表那个位置，
> 手机返回键一按把整个设置都关了。都是因为第六十七节加那一屏时，
> 只在五处手写清单里登记了三处（漏了 `inSub` 和返回键那两处）。
>
> 第六十节修的是症状（滚动位置），没动成因（五份平行清单）。现在收成
> **一个 `sub` 状态 + 一张 `Record<SubScreen, string>`**，加一屏编译器会点名。
> 他那句「**以后记住在设置里新增窗口，滚动条应该单独计算**」——
> 现在不用记了，忘了就编译不过。
>
> ### 上一批：同步这一摊（第六十六～六十八节，2026-09-04 下半天）
>
> 起点是他问「同步到底什么时候触发」。**触发点只有三个，没有定时轮询**：
> app 启动／回到前台、改完东西 8 秒后、设置页里手动那颗。
>
> 查触发时查出一处会**悄悄丢改动**的地方（六十六）：每分钟至多一次那道限流闸，
> 从前是 `return` 直接放弃 —— 现象是「我明明改了，另一台没更新」。
> 改成**约到闸开的那一刻再来**，上限一点没松。
>
> 接着他追问体积和额度，我算错两次（见上面那条），于是有了
> **读数屏**（六十七）和**记真账**（六十八）。
>
> ⚠️ **同步的真身（网络、坚果云）我验不了。** 能钉的都抽成纯函数钉住了：
> `decideAutoSync`（走不走）、`progress.ts`（进度怎么合）、`usage.ts`（记账）。
>
> 他提的「只对齐当前文档的笔记」**现在不做** ——
> 只能省一半多一点（文档壳那 29% 拆不掉），而代价压在三方合并上。
> **门槛：`data.json` 到 100 KB 左右再说**，方案已经想清楚，写在第六十七节。
>
> ### 上一批：重叠的短语跳不到正文（第六十五节）
>
> 用户报的：右侧栏点某些短语没反应，**偏偏是和单词重叠的那些**（all + all right）。
> 根子是 `anchorId` 一个字段干了两件事 —— 既是身份又是门牌号，
> 撞车时后来的退回用记录 id 当身份，拿它去正文里找当然找不到。
> 现在门牌号单独一格。**⚠️ 这个 bug 一半是第六十四节带出来的**：
> 从前谁当输家看先划哪个，新排序规则让短语成了固定的输家。
>
> ### 再上一批：去掉手动排序（第六十四节）
>
> 用户提的：「把手动排序的功能给删掉，完全由规则排好就不能再自定义移动位置了。」
>
> 现在笔记的次序是**算出来的**（按正文坐标），不再是存下来的。
> 复习页的拖拽手柄没了，`insertionOrder` 那一百行、`reorderAnnotations`、
> 同步里的 `mergeCardOrder` 一并删掉 —— **净减 445 行**。
> `order` 字段留在老数据里当死字段，谁也不再读写它。
>
> ⚠️ **他拖出来的旧顺序全部消失，这是他自己拍板接受的**（重排本身零数据改动）。
>
> ### 上一批：云端同步（第六十一～六十二节）
>
> 用户提的：「两个设备还要来回备份迁移」。现在手机和平板经**坚果云**共用一份数据。
>
> 心脏是 `src/sync/merge.ts` 的**三方合并** —— 靠一份「上次同步完是什么样」的底本
> 把「谁改了什么」**算出来**，于是删除不需要墓碑、数据格式一个字没改。
> 那一节值得完整读一遍，尤其是冲突规则那张表。
>
> ⚠️ **两条别再试的**：CapacitorHttp 不支持 MKCOL，所以 app 建不出文件夹
> （让用户用坚果云默认就有的「我的坚果云」）；坚果云在文件夹不存在时回 **409 不是 404**。
>
> ⚠️ **流量踩过两次坑，都记在六十一、六十二节**：一是阅读进度
> （一路往下读，滚动位置一直在存，每次都算「数据变了」）；二是**划词**
> —— 那是真改动、本来就该传，治法是把**正文和索引拆开存**，
> 云端一个 `data.json`（索引 + 全部笔记，17 KB）加每篇一个 `page-xxx.json`。
> 真机实测一次同步 **30 KB**。
>
> ⚠️ **拆开之后，先传正文再写索引，顺序不能反** —— 反了会把另一台的正文冲掉。
> 第六十二节里那四条都是「判错就丢正文」，动这块之前务必读一遍。
>
> ⚠️ **这个 app 里有两种「排序」，对同步是相反的两件事**（第六十三节）：
> 文库和文档靠**数组位置**（三方合并天然看不见，得单独合一遍），
> 复习页卡片靠 `order` **字段**（看得见，但两边都重排时逐条合会交错成乱序）。
> 再加任何「顺序」之前，先问一句它是哪一种。
>
> ### 上一批：朗读走出设备（第五十七～五十九节）
>
> 起因是用户在平板上「点了没声音」，一路查下来是**设备缺件，不是代码**：
> 他那台鸿蒙平板用**卓易通**跑这个 app，那层兼容环境里**一个朗读引擎都没有**
> （装 Google 文字转语音也接不上）。真人录音是下载 mp3 放出来的，所以还能响；
> 凡是过机器音的一概哑。
>
> 定案靠的是**加一屏读数**（设置 → 开发者 → 朗读引擎参数）——
> 和第四十九节同一招，前面猜了好几轮，读数一出当场结案。**这一屏留着了。**
>
> 出路是**云端朗读**（第五十九节）：真人录音 → 云端合成 → 系统引擎，
> 用户开的火山引擎，**手机平板一套逻辑，不做区分**（他的原话）。
> 顺带补上第五十七节查出的那个 bug：**朗读失败时在你点的那个地方说人话**
> （从前只有复习页会说，阅读页整个把错误吞了，这正是多绕好几轮的直接原因）。
>
> 还有第六十节：设置页的滚动位置（一个根子两个症状）。
>
> ### ⚠️ 这一段里我犯的两个错，都值得记住
>
> 1. **我下过一次错误的结论**：用户说设置里找不到「文字转语音」，我就断定
>    「不用查了」；他接着说**屏幕朗读能念英文**，把这个结论推翻了 ——
>    华为那套是私有的，第三方 app 借不到。**新证据来了就该重新算，别护着上一个判断。**
> 2. **我提的一个改法被用户否掉了，他是对的**：我想「引擎不可用就别画喇叭图标」，
>    他回「本来就没有喇叭图标，点词就是发音」。而且那么改会把**还好使的真人录音
>    那一半也砍掉**。见第五十八节。
>
> ### 上一批：沉浸阅读（第五十五节） —— 「像看视频那样」，只剩正文。
> 两条顶栏、两条系统栏、「笔记」键一起收。**宽窄两套触发方式，是用户分两次定的**：
> 平板横屏自动（两侧栏都收起来就进，点空白露 3 秒又自己收），
> 手机和平板竖屏手动（点一下空白收起，**不会自己回来**，再点一下才回来）。
> 做了三轮，第二轮把状态栏和顶栏绑成一体才算顺 —— 那一节值得读。
>
> 同一天并的还有四件：**启动屏**换掉 Capacitor 的默认蓝 X（第五十二节，
> 和图标一样是漏掉的默认货）、**划词线与短语线互不干涉**（第五十三节，
> 短语的波浪线改用背景图）、**波浪线的裂缝**（第五十四节，砖缝挪到波谷）、
> **「更多」菜单按最长那条撑开**（第五十六节）。
>
> ### ⚠️ 平板的系统字号比手机大，这是一整类问题的来源
>
> 第五十一、五十三、五十六节都栽在这上面：短语线断在 g 上、
> 「移至回收站」折成两行，全是「他那儿排版不对、我这儿正常」。
> **往后遇到这种，先把浏览器的根字号调大再看**
> （`document.documentElement.style.fontSize='24px'`，等价于系统字体放大），
> 别急着改数字 —— 写死的宽高只是把门槛往后挪，他再调一档又会犯。
>
> **上一批：平板上的六条（第五十一节）** —— 键盘不再把整个窗口往上挤
> （改成原生只报高度 `--kb`，弹窗/编辑全文/正在打字的那一栏各自让）、
> 「笔记」键不再抢在侧栏收完之前跳出来、短语波浪线不再断在 g 上、
> **两侧栏都能拖着改宽度了**（`usePanelWidth`，只在宽屏）、
> 长按小窗会自己躲开键盘。
>
> **再上一批：宽屏点中间正文即可收起笔记栏（第五十节）**，笔记栏右上角那颗关闭键删了。
> 手机上那颗键本来就不显示，窄屏没变。
>
> **再上一批：沉浸式系统栏（第四十九节）——** 第四十八节撤回的那件事重做，通过了。
> 网页自己长到屏幕边缘，每一栏的底色和竖线一路铺到最顶最底，
> 时钟电量和导航键浮在 app 自己的颜色上，弹窗压暗时两条系统栏也跟着暗。
> **底部让位改了四版才对**，教训和真机数值都记在那一节。
> 顺带：装成 app 不再用 PWA 离线缓存（那是「装了新包还跑旧网页」的根源），
> 设置里多了「开发者 → 系统栏参数」一屏。
>
> 更早的三批（第四十五～四十七节）：
>
> - **六条小优化**：返回键改成退到后台（从前是真退出）、离开复习自动退出编辑模式、
>   排序手柄对齐小写字母的中线、句摘虚线改成背景条纹、「文件夹」统一成「文库」
> - **左滑删除重做**：不再露常驻红按钮，划过 72px 松手弹确认框。
>   **前面三轮修参数全没打中，根子是结构** —— 见第四十六节
> - **平板**：布局分界线从 768 挪到 1024（新断点 `wide`），左栏在宽屏也能收，
>   宽屏长按取词改回贴着词的小窗
>
> ### ⚠️ 用户定下的一条规矩（2026-09-03）
>
> **「改平板就是改平板，不要影响到手机那边。」**
>
> 起因是沉浸式系统栏那次（第四十八节）：为了修平板上的两条系统栏问题，
> 我动了主题、窗口模式、原生注入和全局 CSS 四处 —— 这些是**两边共用的地基**，
> 手机也跟着一起变，真机一装就废了，当场撤回。
>
> 往后：平板的问题**一律锁在 `wide`（1024）那条线之内**，
> 或者用 `useIsWide()` 判断；地基类的改动单独提、单独验、单独一条分支。
>
> ### 更早的几批
>
>
> 上一轮做的四件事（第三十八～四十一节）：
>
> - **纸色设置延伸到复习模式**（第三十八节）—— 用户报的：设置里换了主题，
>   复习页纹丝不动。配色表原本写在阅读页组件内部，搬进
>   `src/components/theme.ts` 给两边共用。**这一节我返工了两次，
>   教训比改动本身重要 —— 见那一节的「⚠️」**
>
> - **强调色五种可选**（第三十九节）—— 从前通体琥珀、写死在 119 处。
>   现在类名统一成 `accent-N`，色值走 CSS 变量，切换只改 `<html data-accent>`。
>   琥珀 / 墨蓝 / 松绿 / 朱红 / 石墨，设置里一排色点。默认仍是琥珀
>
> - **两个 AI 弹窗点外面能关 + Key 默认遮住**（第四十节）—— 眼睛图标从前是反的
>   （跟动作走），改成跟状态走。Key 按「框里有没有东西」决定默不默认遮 ——
>   一律遮住会撞上「安卓密码框不给粘贴」那个老坑
>
> - **换掉 App 图标**（第四十一节）—— 手机桌面上那个蓝色的 X 一直是 Capacitor
>   打包自带的默认图标，从来没换过。现在是「衬线 A + 琥珀下划线」
>
> 更早的几批见第二十五～三十七节。旧分支全部留着当存档，可以不管。
>
> ### 当前状态
>
> - 测试 **646 项**全过；`npx tsc -b --force`、`npm run build` 都通过
> - **手机上「好像没有变化」时先看设置 →「开发者 → 系统栏参数」里那行「网页打包于」**：
>   对不上刚装的那一版，就是跑着旧网页（第四十九节堵过一次，但这一招留着好用）
> - APK 打得出来（三步见 [发布到手机.md](发布到手机.md)）。
>   **交给用户前先改名**，别直接送 `app-debug.apk`，名字用英文
>   （中文名传过去会乱码）：`lyric-vocab-<改了什么>-<日期>.apk`
> - **改完直接打包送过去，不要问「要不要打个 APK」**（用户 2026-09-02 明确说的）
> - 仓库里还有两份**非代码**的东西：`宣传图/`（八张 1080×1440 的介绍图，
>   小红书/朋友圈直接能发）和 `截图/`（用户的九张真机截图，是前者的素材）。
>   生成脚本不在仓库里，见第三十九、四十一节的说明
>
> ### 下一步：等他继续提，尤其是平板上的

> 2026-09-02 他先说「感觉也没什么想要添加的功能了」，转头又说
> **「但是还有很多需要优化的地方」**，从那以后一条条提到现在。
> 2026-09-03 换到平板上用，又提了一批（第四十七～五十六节，那一天做了十节）。
> 2026-09-04 是朗读那一摊（第五十七～六十节）和**云端同步**（第六十一节）。
>
> 所以**不要自己找活干，等他提**。他提的都很具体、也都在点子上。
>
> **手上没有欠着的东西。** 分支都并了，`master` 干净。
>
> 唯一悬着的是上面那条：**第六十六～六十八节没经真机验证就并了**，
> 那一批动了同步的存取结构。他两台机器上第一次同步之后，
> 值得问一句「同步还正常吗、阅读位置对不对」。
>
> 云端同步这一摊（第六十一～六十三节）是 2026-09-04 一天做完并全部验过的，
> 他两台真机上跑着。**在他报出问题之前，别去动它** —— 那块里每一处判错
> 都是「不报错、过几天才发现笔记没了」。
>
> 平板上那条深灰的状态栏**已经解决**（第四十九节）。
> 平板上**没声音也已经解决**（第五十九节，走云端）—— 那台机器上的系统朗读引擎
> 是指望不上的，**别再往「装个引擎」那个方向想**，第五十七节记着为什么。
>
> **云端朗读的剩余额度用的是本地计数**（app 自己数发出去多少次），
> 是用户在三条路里选的。想接官方那个真实额度得要账号级 AK/SK，
> 他知道代价、选了不接 —— 别自作主张去接。
>
> **沉浸阅读那边没有待办。** 从前这里写着「他自己提过两处可能还要收」，
> 那是记错了 —— 第五十五节里查不到用户说过这两句，是我把自己的观察写成了他的意见。
> 其中「进沉浸时正文往上跳 117px」2026-09-03 当面问过他，
> **他的原话：「顶栏没了正文自然要往上顶，这不是很正常吗」** —— 结案，不改。
> （量过：确实是 117px、瞬间、没有动画，但滚动位置不变、退出来一个像素不差，
> 而且沉浸态里再点空白叫顶栏时正文完全不动 —— 那条栏是浮着的，本来就不占位置。）
>
> 手里攒着的（都不急，等他开口或等用得着）：
>
> - 书库四条小的（第四十三节末尾）：那 4118 本网页版（查过是通俗小说，
>   不划算）、中文书名搜不了、下过的书不记得、目录不会更新
> - **一键划词的音标释义准不准还没有实际数据** —— 用几篇下来若错得多，
>   就补一份词典兜底
> - **英音/美音开关** —— 地址里 `type=1/2`，代码早留好了，半天能出活。
>   现在云端那条路也进来了，火山有专门的英音音色（Anna `BV040_streaming`）
>   和美音的（Ariana `BV503`、Jackson `BV504`），两条路可以一起接
> - **云端那个音色可以换**（第五十九节）：现在用的是他那个服务给的通用音色，
>   火山另有专门的英文发音人 —— 等他觉得念得不好听再说
> - 导出 HTML/Markdown、字幕导入（.srt）—— 都是独立的小活
> - **深色模式** —— 换强调色那一半已经做了（第三十九节），
>   剩下的是大工程，每个颜色都要重配，**要他先拍板**
>
> **拍照识别（OCR）已经聊过，结论是往后放。** 不是技术难 ——
> AI 那套已经接好了，识图是同一套东西。是这件事本身不划算：
> 拍书页一次只能进一页，反光、弯页、要一页页拍，
> 想弄一章进来是所有办法里最费劲的。只有「纸质书且没有电子版」才轮得到它。
>
> **其它动手就能做的**：**导出 HTML/Markdown**（半天能出活）、**字幕导入（.srt）**、
> **英音/美音开关**（地址里 `type=1/2`，代码早留好了）。
>
> **要用户先拿主意的**：**深色模式**（换强调色那一半已经做了，见第三十九节；
> 剩下的深色是大工程，每个颜色都要重配）、**同一篇里重复的词怎么处理**
> （合并成一张卡就用不了拖拽排序）、**AI 复核已写的笔记**（结果怎么交付是关键）。
>
> **聊过但用户说再想想的**：**句摘用云端朗读**。词典只收词条、整句没有录音，
> 而拼接单词录音行不通 —— 量过：八个词拼出来 10.3 秒，正常人读只要 2.5～3 秒。
> 发音缓存那套现成的正好能接云端 TTS，详见第三十节前后的讨论记录。
>
> ### 两件到期要做的收尾
>
> 1. **一键划词的音标与释义准不准，还没有实际数据**。用几篇下来若错得多，
>    就补一份词典兜底；错得少就省了（和「一键填充」同一个悬而未决的问题）
> 2. **`data.notes` 删不掉**（交接说明里从前写错过）—— 恢复换代之前导出的老备份
>    还得靠它。第二十节删掉的是旧的写入函数和旧的 `reconcilePage`
>
> ### 一件救不回来的旧账
>
> 第二十一节那个 bug 修复**之前**就已经被改坏的句摘救不回来 ——
> 原句当时根本没存下来。从那版起新发生的不会再坏。
>
> ### 这个项目的工作方式
>
> 1. **每次改动开一条分支** → 打包 APK 给用户在手机／平板上验 → 确认了才并回 `master`。
>    **改完直接打包送过去，不要问「要不要打个 APK」。** 旧分支留着当存档
> 2. **观感问题（「不好看」「没对齐」「太挤」）先去浏览器把数字量出来**，量完再谈，
>    量到的当场用上；说不清就做成网页给他看真实像素（要**整屏对照**）。
>    **观感的改动单独提、让他拍板**，别顺手打包做掉 —— 他的判断一直很准
> 3. **⚠️「顺手」是这个项目最贵的两个字。** 只做他要的那一件；
>    要退回就退干净（第三十八节：多改三样全部撤回重做）
> 4. **平板的问题锁在 `wide`（1024）之内**；系统栏、窗口模式这类两边共用的地基
>    单独提、单独一条分支（第四十八节）
> 5. **我验不了的东西要说清楚**（提示词准不准、朗读引擎、真机手感、`:active` 的观感），
>    而且**不要一次改一大片** —— 真机一挂会分不清是哪一处的错。
>    能留个**手工入口**就留（`window.__onNativeInsets` 在浏览器里也挂着，
>    敲一句就能走一遍真机那条通路），比盲改强
> 6. **改到第二轮还不对，就别再调参数了**：换依据、换设计，或者先做一个诊断手段。
>    第四十九节加了一屏读数之后，前两轮的猜测一轮就定案。
>    同理，**别张口就下结论** —— 那条深灰我说成「跟随系统深色模式」，
>    用户回「我没开深色模式」，真因在别处
> 7. **⚠️ 报数字之前，先问这个数是从哪儿量出来的。** 2026-09-04 我连错两次，
>    都是拿一个来路不明的数当他的（详见第六十七节）。
>    **他手上有真数据而你只有二手数时，先要数据、或者先加一屏读数，别先给结论。**
>    额度和体积现在有真账了：设置 → 开发者 → 同步数据
> 8. **写完测试要反过来验它会不会红** —— 把修复拿掉跑一遍，确认它真的挡得住，
>    再还原。不然你不知道那条测试在挡什么（第六十八节验了三处）
>
> 光跑单测不算验过：界面改完去浏览器走一遍真实流程；涉及系统能力
> （朗读、相机、剪贴板、文件、分享、系统栏）先在真机上验一个最小版本 ——
> WebView 不是 Chrome。
>
> 正文各节里的「留个教训」是**病历，按需翻**，不必开工先通读一遍。
>
> ### ⚠️ 在这个环境里验证，什么能信什么不能信
>
> 这张表是踩出来的，查一眼比再栽一次便宜：
>
> | 想验什么 | **能信** | **不能信** |
> |---|---|---|
> | 样式、选中态 | `className` | `getComputedStyle`、截图 |
> | 动画、位移、回弹 | `getAnimations()`、inline style、React 状态 | 截图、`getBoundingClientRect()`（面板隐藏时动画根本不推进，读到的是冻住那一帧）|
> | 手势 | 打在**手指真正会碰到的那个元素**上的真实指针事件 | 打在外层容器上（会绕开所有子元素）|
> | 网络 | 响应的**内容** | `res.ok` / 状态码 200 |
> | 控制台报错 | 插一条自己的标记，看红字在标记前还是后 | 直接看有没有红字 |
> | 布局断点 | `matchMedia(...).matches`、`className` | 「改了视口尺寸就会收到事件」（模拟器不发 `resize`，手工补一个）|
>
> 另外三条环境上的事：
>
> - **改了 `vite.config.ts` / `tailwind.config.js` 开发服务器不会自动生效，得重启**；
>   `npm run build` 当场生效
> - 要读被过渡挡住的真值，先注一条 `*{transition:none!important}`；
>   要等一帧用 `setTimeout`，`requestAnimationFrame` 在面板隐藏时不触发
> - **检查脚本自己也会写错。对不上时先怀疑脚本**，再怀疑代码

这份文档记录**结构上还有哪些短板**、**想加的功能各自需要什么前置条件**，以及**建议的推进顺序**。

想做某项功能时先翻到对应条目，看一眼「要先做什么」和「注意事项」，避免又踩进「改一个地方带出三个问题」的循环。

---

# 目录与索引

> **这份文档怎么用**（写给接手的人，也写给 AI）
>
> 全文四千多行、六十节，**不要通读**。正文各节是**病历，按需翻**。
> 顺序是这样的：
>
> 0. **新窗口接手的第一件事**：读「现在停在哪」里那张
>    「哪个提交出事长什么样」的表 —— 第六十六～六十八节**没经真机验证就并了**，
>    动的是同步的存取结构。他要是一上来就报同步的问题，答案多半在那张表里
> 1. 先读上面那段「**现在停在哪**」—— 分支状态、最近做了什么、下一步等什么
> 2. 再读「**这个项目的工作方式**」和「**在这个环境里验证，什么能信什么不能信**」
>    —— 这两块是规矩，违反了会返工
> 3. 动手前，按下面「**按主题找**」翻一眼相关的那几节，看有没有踩过的坑
> 4. 「**血的教训索引**」是跨节的，值得单独扫一遍

## 血的教训索引（跨节，最值得先看）

| 教训 | 出处 |
|---|---|
| **「顺手」是这个项目最贵的两个字** —— 只做他要的那一件；要退回就退干净 | 三十八 |
| **改平板就是改平板，别碰两边共用的地基**（主题、窗口模式、原生注入、全局 CSS） | 四十八 |
| **改到第二轮还不对，就别再调参数了** —— 换依据、换设计，或者先做一个诊断手段 | 四十六、四十九 |
| **加一屏读数，胜过猜三轮**。第四十九节加了读数之后一轮定案；第五十七节同样 | 四十九、五十七 |
| **别张口就下结论** —— 「跟随系统深色模式」被用户一句「我没开深色模式」推翻 | 四十八 |
| **别信状态码，看内容** —— 200 的网页被当成录音存进缓存；401 的正文里才是真原因 | 四十三、五十九 |
| **平板的系统字号比手机大**：写死的宽高在他那儿会折行。先把根字号调大再看 | 五十一、五十三、五十六 |
| **我验不了的东西要说清楚**，而且留一个手工入口，比盲改强 | 五十一 |
| **一次别改一大片** —— 真机一挂会分不清是哪一处的错 | 四十八 |
| **观感的改动单独提、让用户拍板** —— 他的判断一直很准，我提的改法被他否掉过 | 五十八 |
| **验证环境会骗人**：模拟器不发 resize；面板隐藏时过渡不推进 | 五十、五十五 |
| **用户先入为主的判断，值得当场去测** —— 三句直觉两句错，而对的那句比他以为的更严重 | 六十一 |
| **别记状态，去算状态** —— 同步靠「底本」算出谁改了什么，省掉了改数据格式和二十个写入口 | 六十一 |
| **用户问「那……会怎么样」时，当真去查一遍**，别凭结构推理回「应该没问题」 | 六十三 |
| **两种排序机制别混为一谈**：数组位置 vs `order` 字段，对同步是相反的两件事 | 六十三 |
| **去掉一个功能，往往能顺手拆掉一整串为它服务的复杂** —— 净减 445 行 | 六十四 |
| **先问「这个字段是非存不可，还是能算出来」** —— 算出来的东西同步弄不乱 | 六十四 |
| **改排序规则前先想：有没有别处正靠着「谁排在前面」做决定** | 六十五 |
| **一个字段别拿去干两件事** —— 身份和门牌号撞车，点了静悄悄地什么也不发生 | 六十五 |
| **这个环境里验滚动，先把平滑关掉**（rAF 冻着，CSS 压不住 JS 传的 smooth） | 六十五 |
| **限流的正确形状是「推迟」不是「丢弃」** —— 一个 return 别兼办两件事 | 六十六 |
| **检查脚本自己也会写错**，对不上时先怀疑脚本（六十六、六十七连中两次） | 六十六、六十七 |
| ⚠️ **引用一个数之前先问它从哪儿量的** —— 我拿别的数据集的数当用户的，错了五到八倍 | 六十七 |
| **用户有真数据而我只有二手数时，先要数据／先加读数，别先给结论** | 六十七 |
| **加一屏读数胜过猜三轮**（第三次应验） | 四十九、五十七、六十七 |
| **顺口说的结论和写进文档的结论，标准要一样** —— 「顺带查明」也得有证据 | 六十七 |
| **限流／省流量都别用「丢弃」实现** —— 该推迟、该拆小，不该把东西丢了 | 六十六、六十八 |
| **危险的遗漏用类型系统挡，别靠记性** —— 参数做成必填，逼编译器找出每个调用点 | 六十八 |
| **写完测试要反过来验它会不会红**，否则你不知道它在挡什么 | 六十八 |
| **「新增一屏要在 N 个地方登记」—— N 大于 1 就已经是 bug 了**。修症状不动成因，七节后同一个坑又踩一次 | 六十、六十九 |
| **算得清的那一半抽成纯函数钉住，算不清的那一半交给浏览器**（`scrollIntoView({block:'nearest'})` 一行顶一屏像素计算） | 七十 |
| ⚠️ **一轮里连中三个环境骗局**：过渡不推进、改 scrollTop 不发 scroll 事件、后台 setTimeout 被钳到 1 秒。**「这是个 bug」之前先问这个数是怎么量的** | 七十 |
| **判「用户滚了侧栏」要听 wheel/touchmove，不能听 scroll** —— 后者分不清是谁滚的，自己滚的会把自己暂停掉 | 七十 |

## 按主题找

| 你在碰什么 | 相关的节 |
|---|---|
| **朗读 / 发音** | 十（起点）、二十五～二十七（真人录音、缓存、弯撇号）、二十九、三十一、五十七（平板没声音的诊断）、五十八（失败要说人话）、五十九（云端朗读） |
| **平板 / 宽屏布局** | 四十七（断点挪到 1024）、五十、五十一、五十五（沉浸阅读） |
| **系统栏 / 沉浸式 / 原生** | 四十八（撤回）、四十九（成了）、五十二（启动屏）、五十五 |
| **标注模型（词 / 短语 / 句摘）** | 七（统一模型）、十一、十二、十三、二十、二十一（句摘失真） |
| **AI 填充 / 划词** | 七之二（多供应商）、十七、二十四、二十八、四十 |
| **书库 / 导入** | 四十二（古登堡）、四十三（澳洲站） |
| **设置页** | 十九（收进一页）、二十三、三十九（强调色）、四十、五十九（云端朗读那一栏）、六十（滚动位置）、**六十九（子屏收成一个状态，滚动 + 返回键）** |
| **手势 / 滑动删除** | 三十二、四十六（重做，前三轮全没打中） |
| **排版 / 对齐 / 观感** | 十五、三十六、三十七、四十五、五十三、五十四、五十六 |
| **回收站 / 数据** | 四十四、二十（清旧代码） |
| **云端同步 / 多设备** | 六十一（三方合并、坚果云的脾气）、六十二（拆开存、流量）、六十三（排序）、六十六（触发时机）、六十七（真实体积、读数屏）、**六十八（记真账、旧残留不上云、进度单独走）** |
| **笔记的排列次序** | 十一、十二（词与短语同一条队）、六十三（同步）、**六十四（改成一律按正文顺序，手动排序去掉了）** |
| **右侧生词板 / 点笔记跳转** | 十三（短语排最后）、六十五（重叠的短语跳不动）、**七十（跟着正文滚）** |

## 按顺序（全部小节）

**早期：结构与升级清单（一～九）** —— 统一标注模型、多 AI 供应商这两件大改的由来。

- 一～四：结构短板、升级清单、建议顺序、已知小问题
- 五、六：2026-08-28 / 08-29 已完成的部分
- 七：统一标注模型 ✅ ｜ 七之二：多 AI 供应商 ✅
- 八、九：待办意向、下一步从哪接

**有声阅读与标注（十～二十四）**

- 十：点词/点句读出来 ✅ ｜ 十一：新标注按正文顺序插入 ✅
- 十二、十三：短语模块及其两处返工 ✅
- 十四：API Key 那格粘贴不了（安卓密码框的老坑）
- 十五、十六：AI 角标占行 / 改了卡片角标不消失 ✅
- 十七：一键划词 ✅ ｜ 十八：生词卡点一下展开 ✅
- 十九：设置收进一个页面 ✅ ｜ 二十：清掉换代留下的旧代码 ✅
- 二十一：**句摘被改短就永久失真** ✅（有一笔救不回来的旧账）
- 二十二～二十四：魔杖加文字、使用说明、「AI 填充」改名 ✅

**真人发音这一批（二十五～三十一）**

- 二十五：单词改用词典真人录音 ✅ ｜ 二十六：发音存下来 + 短语也用真人 ✅
- 二十七：**弯撇号丢录音** + 冠词 a 读成字母名 ✅
- 二十八：AI 把 fetters 填成名词 ✅
- 二十九～三十一：生词板点词、句摘箭头只跳转、长按取词时读出来 ✅

**手势与排版（三十二～四十一）**

- 三十二：复习页左滑删除 ✅ ｜ 三十三：划句子带不进引号 ✅
- 三十四：点「编辑全文」跳回文首 ✅ ｜ 三十五：「新建文库」挪进列表 ✅
- 三十六、三十七：三列顶栏对不齐、复习页那行竖着挤 ✅
- 三十八：**主题只染阅读页**（返工两次，教训比改动重要）✅
- 三十九：强调色五种可选 ✅ ｜ 四十：弹窗点外面关不掉 + Key 默认明文 ✅
- 四十一：换掉 Capacitor 的默认图标 ✅

**书库与回收站（四十二～四十四）**

- 四十二：App 里搜古登堡直接下载 ✅ ｜ 四十三：接上澳洲站（**安卓不许明文 HTTP**）✅
- 四十四：回收站一键清空 ✅

**平板那一天（四十五～五十六，2026-09-03 做了十节）**

- 四十五：六处小优化 ✅ ｜ 四十六：**左滑删除重做**（前三轮全没打中，根子是结构）✅
- 四十七：布局分界线 768 → 1024 ✅
- 四十八：⚠️ **沉浸式系统栏做了又撤** —— 「改平板别碰手机」那条规矩的由来
- 四十九：沉浸式系统栏（这次成了）✅ —— **加一屏读数一轮定案**
- 五十：宽屏点中间收起笔记栏 ✅ ｜ 五十一：平板这一批（键盘、抢跑、断线、可拖宽）✅
- 五十二：启动屏还是默认蓝 X ✅ ｜ 五十三、五十四：划词线与短语线、波浪线裂缝 ✅
- 五十五：**沉浸阅读**（三轮，宽窄两套触发方式）✅
- 五十六：「移至回收站」折成两行（**根子是系统字号**）✅

**朗读走出设备（五十七～五十九，2026-09-04）**

- 五十七：**平板上机器音一概没声音** —— 查明是卓易通里没有朗读引擎，不是代码
- 五十八：朗读失败时说人话，而且在你点的那个地方说 ✅
- 五十九：**云端朗读**（真人录音 → 云端合成 → 系统引擎，火山引擎）✅

**其它**

- 六十：设置页三处小问题（**滚动位置**：一个根子两个症状）✅
- 六十一：**云端同步**（坚果云 WebDAV，三方合并）✅
- 六十二：**正文和索引拆开存** ✅ —— 划词从传 1.44 MB 变成 17 KB，省 84 倍
- 六十三：⚠️ **排序被同步悄悄还原**（文档拖动 + 复习卡片两处）✅
- 六十四：**去掉手动排序**，笔记一律按正文顺序（净减 445 行）✅
- 六十五：右侧栏点**和单词重叠的短语**跳不到正文（一个字段干了两件事）✅
- 六十六：自动同步**被闸拦下来时补一次**（从前就地放弃 = 悄悄丢改动）✅
  ⚠️ 这一节里「两笔账」那一段**数字是错的**，正确的在六十七、六十八
- 六十七：**「同步数据」读数屏** ⚠️ 附一次我算错的账（体积估小了五到八倍）✅
- 六十八：**同步三件优化**（记真账 / 旧残留不上云 / 进度单独走小文件）✅
- 六十九：设置的子屏漏登记（进去不在顶部 + 返回键关掉整个设置）✅ —— 五份手写清单收成一个 `sub` 状态
- 七十：**笔记栏跟着正文滚** ✅ —— 竖线标当前那条；⚠️ 这一节里三个环境骗局值得单独读

---

## 一、先看这个：结构上的三个短板

**三块现在都做完了**（2026-08-29 那一轮）。这一节留着当病历 —— 后面几条功能的「要先做什么」指的就是它们，翻到对应条目时能对上号。

### ~~短板 1：笔记没有稳定身份，位置就是身份~~ ✅ 2026-08-29 已完成

**做完了，详见第七节。** 这里留着当初的病历 —— 后面好几条功能的「要先做什么」
指的都是它，而且知道从前难在哪，才看得懂现在的代码为什么长这样。

**当初的毛病**：单词笔记的键就是它的位置（`L0W2` = 第 0 行第 2 个词）：

```
{ "L0W2": { word: "stood", definition: "站立" } }
```

位置即身份，所以正文一编辑身份就跟着变，笔记就错位 —— 这是 2026-08-28
那批 bug 的总根源。句摘还是完全另一套系统：单独的数组、自己的 id，
而且存在浏览器的 localStorage 里，备份要从两个地方拼。

**现在**：每条标注有一个永不改变的 id，位置降级成它身上的两个普通属性
（`src/types.ts` 里的 `Annotation`，`start` / `end`）。单词、短语、句子是
同一种东西，只差范围长短；句摘搬进了同一张表，备份只从一处取；
孤儿就是「位置为 null」，不用再编假坐标。

对账（`reconcileAnnotations`）没有消失，也不该消失 —— 正文改了位置本来就得跟着变。
区别在于它现在只更新位置属性，不再搬家：万一失手，位置暂时不准，
身份和内容一个字都不会少。

**当初被它挡着的四项，前置条件现在全齐了**：短语归类（已做，第十二节）、
卡片排序（已做，第九节）、自动划生词、有声阅读的位置映射。

**旧数据没删，是故意的**：`data.notes` 和旧句摘原样留着 ——
恢复一份换代之前导出的老备份还得靠它。见文档开头「两件到期要做的收尾」。

---

### ~~短板 2：滚动时整个界面重画~~ ✅ 2026-08-29 已完成

滚动时每一帧都把阅读进度写进最外层状态，导致整棵树重渲染（含上千个单词节点）。
当初试过的五个 CSS 偏方一个都没打到点子上 —— 卡的不是绘制，是 JS。

改法（三处）：
- 阅读进度取整到百分点：进度条本来只显示到这个精度，不取整则每帧都是新数值
- 两个内联回调提成稳定引用（否则 memo 会被破坏）
- 四个大组件用 memo 包起来做渲染隔离

实测（1400 词文档，20 次滚动）：

| 组件 | 改前 | 改后 |
|---|---|---|
| LyricEditor | 40 次 | **0 次** |
| LeftSidebar | 40 次 | **0 次** |
| RightSidebar | 40 次 | 12 次 |
| App | 40 次 | 24 次 |

实际工作耗时 688ms → 460ms。

### ~~短板 3：导入功能写死在界面里~~ ✅ 2026-08-29 已完成

txt 导入的 110 行代码原本直接写在 `App.tsx` 的回调里（嵌套四层 Promise），
每加一种格式都得去改界面代码。

现已抽出 `src/importers/`：

```
任何输入 -> { 书名, 章节: [{ 标题, 正文 }] }
```

- `types.ts` 定义约定，`txt.ts` 实现纯文本，`index.ts` 是登记处
- **加新格式只需写一个文件、在登记处加一行**，界面一个字都不用动 ——
  连文件选择器的 accept 都会自动跟着变
- 章节切分变成纯函数，第一次有了单独的测试（13 项）
- App.tsx 里那段从 4011 字符缩到 581 字符

## 二、升级清单逐项

### ~~epub 格式导入~~ ✅ 2026-08-29 已完成

`src/importers/epub.ts` + `epubParts.ts`。用 fflate 解压，按 epub 的固定约定读：
container.xml -> OPF（书名、阅读顺序、目录位置）-> 目录（章节名）-> 各章 XHTML。

- 一个 epub = 一个文库，一个正文文件 = 一篇文档
- 章节名优先取自目录（epub2 的 ncx 与 epub3 的 nav 都支持），
  没有目录则退回正文里的 h1/h2，再不行按「第 N 章」编号
- 封面、空白页（没有正文的）自动跳过
- HTML 转纯文本保留段落换行：阅读器每行是一个块，不空行段落会挤在一起

**这一项是导入层的第一次检验：界面代码一个字都没改，
只在登记处加了一行，文件选择器就自动开始接受 .epub 了。**

### ~~一键填充（AI 补全空白笔记）~~ ✅ 2026-08-29 已完成

复习页顶部按钮，范围跟着当前打开的文档或文库走。只填**完全空白**的笔记，
已经写过的内容绝不覆盖。

- `src/enrich/` —— 和导入层同一个思路：`types.ts` 定约定、`deepseek.ts` 是实现、
  `runner.ts` 管分批与中断、`index.ts` 是入口。**换供应商不用动界面**
- 单词填音标 / 词性 / 中文释义（非原形会在释义末尾补上原形，如「飞行；飞翔（fly 现在分词）」）；
  句子填句型说明 + 翻译
- 一次打包多条发送（单词 40 条一批、句子 15 条一批）—— 逐条发会贵十几倍
- 每批完成立即落库，中途停掉已填的都保留
- 填出来的内容标一个灰色单星角标；**用户手动改过就自动清除**，
  所以它实际是一份「待复核清单」，复核完自然清空

**API Key 的事**：用的是用户自己的 DeepSeek key，存在本机 localStorage
（键名 `lyric-vocab-ai-key`），不上传、也不写进导出的备份。
第一次点填充按钮时会要，之后可在同一个对话框里「换 Key」。

**想换别家 AI**：已经做好了，见下面「多 AI 供应商」一条 ——
对话框里选「自定义」，填服务地址和模型名即可，不用改代码。

---

### ~~短语归类~~ ✅ 2026-08-29 已完成

见第十二节。当初判断「标注模型统一之后它只是 `type: 'phrase'`，不需要新逻辑」——
数据层确实如此（存储、备份、对账、迁移全都免费继承），
但**界面是从零做的**：正文画线、复习卡片、抽屉表单、右侧栏、AI 填充，一处都不能少。

---

### ~~单词卡 / 句子卡可调整位置~~ ✅ 2026-08-29 已完成

见第九节。复习页右上角那支笔（编辑模式）里，卡片左上角会出现拖拽手柄，按住它拖，
词和句各排各的。

**只在「单篇文档」的复习里开放。** 文库复习的条目是按拼写合并出来的，
拖了没有一条标注可以写回顺序，所以那边不给手柄。

---

### ~~划句子时标点与数字的处理~~ ✅ 2026-08-29 已完成

分词规则已修正：统一两种撇号、缩写后缀拆成标点、数字纳入单词、落单的连字符不再算词。
`she's` / `she’s` 现在都能单独选中 `she`，`COVID-19`、`1990s` 是完整的词，纯数字不再算单词。

配套做了一次性数据迁移（`migrateTokenizer.ts`）：新旧规则扫的是同一串文字，
按**字符位置**把旧词编号精确对应到新编号，不是猜。迁移只跑一次，用 localStorage 标记守住。

---

### 单篇文章过长

| | |
|---|---|
| 需要先做 | 无（独立） |
| 难度 | 中高 |

**现状**：每个英文词都是一个独立的网页元素。一章小说几千个词 = 几千个元素，一次性全部渲染。

**缓解措施已有**：导入 txt 时会自动按「第 N 章 / Chapter / Session」分章，所以一般不会真的把整本书塞进一篇。

**真要解决**：做虚拟滚动（只渲染屏幕上看得见的部分）。建议**等真被卡到再说**。

---

### 导出为 Word 文档

| | |
|---|---|
| 需要先做 | 无（独立） |
| 难度 | 看选哪条路 |

- **纯前端生成 .docx**：能做，但要引入挺重的库。
- **导出 HTML 或 Markdown**：成本低得多，一样能打印、能发给别人、能用 Word 打开。

如果目的只是「有个能拿出去的文件」，建议走第二条。**这条要你先定方向。**

---

### 拍照识别文字（OCR）

| | |
|---|---|
| 需要先做 | ~~短板 3（导入层）~~ 已就绪 |
| 难度 | 中 |

本质是接一个安卓的文字识别插件，拿到文本后走导入流程。**风险不在结构，在识别质量** —— 尤其是中英混排、排版复杂的书页。建议先小范围试效果再决定要不要正式做。

---

### 自动检索考试词典划生词

| | |
|---|---|
| 需要先做 | ~~短板 1（标注模型）~~ 已就绪 |
| 难度 | 代码不难，**难在数据** |

这项主要不是编程问题：得先有一份可用的词表（四六级 / 考研 / 雅思等），最好还带释义和音标。找到合适的数据之后，代码部分很短 —— 扫一遍正文，命中词表的批量生成标注。

**要你先解决的是数据从哪来。**

---

### 导入视频字幕

| | |
|---|---|
| 需要先做 | ~~短板 3（导入层）~~ 已就绪 |
| 难度 | 低到中 |

字幕文件（.srt 等）结构简单，解析容易。要想清楚的是**时间轴要不要留着** —— 如果留，以后可以和「有声阅读」联动；如果只要文字，那就是最简单的一种导入格式。

---

### 有声阅读

| | |
|---|---|
| 需要先做 | 想要「读到哪高亮到哪」→ ~~短板 1~~ 已就绪 |
| 难度 | 分两步，差别很大 |

- ~~**第一步「能读」**~~ ✅ 2026-08-29 已完成，见第十节。
  做的**不是**阅读页整篇朗读（机械音通读没什么好听的），而是
  **复习页点单词卡的词、点句摘卡的句子就读出来** —— 用户要的是查发音，不是听书。
- **第二步「读到哪里高亮到哪里」**：需要把声音进度对应到文中位置。
  地基已经有了（标注有稳定 id 和 start/end），什么时候想做都行。

---

## 三、建议顺序

1. ~~**渲染隔离**（短板 2）~~ ✅ 已完成
2. ~~**导入层抽出来**（短板 3）~~ ✅ 已完成
3. ~~**统一标注模型**（短板 1）~~ ✅ 已完成，见第七节
4. **虚拟滚动** —— 等真被长文卡到再说

**4 先放着。**

零散的、不依赖任何前置的：导出文档、有声阅读第一步、拍照识别试水。想到哪做到哪都行。

---

## 四、目前已知的小问题

- **目录（左侧栏）拖拽排序偶有异常**。左侧栏自己存了一份文档列表，和数据库传下来的那份是**两份**，拖动时改本地那份、松手后存库、存完新数据又灌回来覆盖本地 —— 两份数据来回拉扯。2026-08-28 修掉了其中一半（排序保存以前是「发出去就不管」，拿不到结果，容易被旧数据覆盖）。如果还有问题，需要具体现象才好定位。

---

## 五、背景：2026-08-28 已完成的部分

搁置约半年后的一次集中整理，从根上处理掉了几类问题。了解这些有助于理解现在的代码为什么长这样。

| 做了什么 | 解决了什么 |
|---|---|
| 加入 git 版本管理 | 之前没有存档，每次改动都得赌 |
| 改为纯触摸交互（长按取词），删掉桌面端并行逻辑 | 双击手势时灵时不灵；手机上看不出哪些词可点；净删 156 行 |
| 存储层收口：内存一份权威数据 + 排队落盘 | 偶发丢笔记；编辑长文卡顿（打字时数据库写入 40 次 → 1 次） |
| 编辑框改用本地草稿 | 编辑正文时光标被弹到文末 |
| 正文编辑后自动对账笔记位置 | 划线转移到别的词、划线消失、以及「改上文导致所有笔记整体错位」 |
| 孤儿笔记标记 +「原文已删除」+ 删除入口 | 笔记与正文对不上时无从处理 |
| 系统弹窗改为 app 内弹窗 | 弹窗样式突兀；弹出后画面被切成两半（输入法相关） |
| 引入测试（24 项） | 最绕的对账逻辑现在有回归保护 |

## 六、2026-08-29 已完成的部分

| 做了什么 | 解决了什么 |
|---|---|
| 底部避让系统导航栏 | 三键导航（后台/主页/返回）会压住左侧栏底部那排图标 |
| 长按选中的单词保持高亮 | 抽屉弹出后看不出选中的是哪个词 |
| 回收站两个弹窗改用传送门 | 它们长在带 transform 的侧栏里，遮罩只盖住 250px，点外面关不掉、还会跟着侧栏被收走 |
| 接管安卓返回键，逐层关闭 | 开着弹窗按返回会直接退出 App |
| 分词规则修正 + 一次性数据迁移 | `she's` 无法单独选中 `she`；两种撇号行为不一致；`COVID-19` 被切坏 |
| 句摘原文改为按位置截取 | 拼词还原是有损的，句中的数字和标点会被吞掉 |
| 整理模式隐藏「更多」按钮 | 拖拽时碍事、易误触 |
| 文库拖拽不再回弹 | 文库没有本地副本，重排只在松手时发生，动画只能往回飞 |
| **渲染隔离**（结构优化 1） | 滚动时整棵树重渲染 —— 这才是当初卡顿的真正原因 |
| **抽出导入层**（结构优化 2） | 加新格式要改界面代码 |
| **epub 导入** | 导入层的第一次检验：界面一字未动 |
| **一键填充（AI）** | 空白笔记只能一条条手工查 |
| 复习页工具栏瘦身 + AI 角标 | 按钮换行；标记太扎眼；改过之后标记不消失 |
| **卡片可拖拽排序** | 复习顺序只能按添加先后 |
| **卡片改为就地编辑** | 编辑模式把排好版的卡片变成一张表单：三个字段各套一个灰框，卡片还猛地长高 31% |
| **取词抽屉重做** | 输入框 32px 太小、音标与词性平分宽度、白底压白底浮不起来、顶部一条骗人的「可拖拽」横杠 |
| 整理模式随侧栏收起自动退出 | 收起来它还留着，下次呼出仍在整理态 |
| 导出的备份改名「文库备份-日期」 | 手机上写死叫 backup.json，攒几个就分不清谁是谁 |
| 右侧栏只点单词才跳转 | 想看释义却被弹回正文 |
| 右侧栏在手机上去掉关闭键 | 与左侧栏一致；宽屏保留，见下方说明 |
| **多 AI 供应商**（见第七之二节） | 只能用 DeepSeek 一家；换一家要改代码 |

这批做完时测试是 117 项；第七、七之二两节又各有增加，现在共 230 项。

---

## 七、~~统一标注模型（短板 1）~~ ✅ 2026-08-29 已完成

**核心变化**：笔记原来的键就是它的坐标（`L0W2` = 第 0 行第 2 个词），
位置即身份，所以正文一编辑身份就变，只能靠事后「对账」把笔记一条条搬回去。
现在每条标注有一个**永不改变的 id**，坐标降级成它身上的一个属性。

单词、短语、句子从此是同一种东西，只差范围长短 ——
单词的首尾是同一个坐标，短语和句子的首尾是两个坐标。

| 从前 | 现在 |
|---|---|
| 单词笔记以坐标为键，住在主库 | 一张标注表，每条有稳定 id |
| 句摘另一套系统，住在浏览器 localStorage | 同一张表里，`type: 'sentence'` |
| 备份要从两个地方拼数据 | 只从一处取 |
| 孤儿要编一个假坐标 `orphan:xxx` 才不撞车 | 孤儿就是「位置为 null」 |
| 加「短语」要把逻辑写第三遍 | 已经是同一种东西，不用写 |

**你能看见的行为一条没变**：正文下划线、句摘虚线、长按取词、
「原文已删除」弹窗与角标、删除入口、原文改回来自动复活 —— 全部照旧。

### 唯一一处看得出来的变化

复习页的卡片顺序。从前没有排序，卡片按「你当初添加的先后」排；
现在按**正文顺序**（跟着文章从头排到尾）。内容一条不差，只是排列变了一次。

### 顺手修掉的一个旧 bug

从右侧栏点进去修改一条已有句摘，保存后会多出一条一模一样的
（旧实现在那里一律新增，不看有没有）。现在同范围已有就改它。

### 安全网

- 旧的 notes 与 localStorage 里的句摘**原样保留没删**，代码也留着并加了标记。
  等真机上用一阵子确认无误，再连同代码一起清理
- 「迁移过没有」的标记存在数据里，不是 localStorage —— 后者会脱节：
  清掉之后迁移会重跑，而重跑是拿旧数据重建整张表，会冲掉迁移后新加的标注
- 恢复迁移前导出的老备份会自动补跑迁移，否则恢复回来的笔记一条都不显示
- 导出的备份里仍额外写一份旧形状的 sentences，万一要退回旧版本 APK 认得

### 验证

**迁移预演**（拿真实备份跑，只读不写，不碰任何数据）：

```bash
node scripts/preview-migration.ts "文库备份-2026-08-29.json" --dump
```

用真实备份跑过：17 条（15 单词 + 2 句摘）进、17 条出，逐篇核对全对得上；
再做了一遍逐字段核对 —— 音标 / 词性 / 释义 / 原形 / AI 标记 / 句型 / 翻译
一个字都没变。

**真机数据跑界面**：把那份备份灌进浏览器、模拟手机首次启动新版本，逐项验过 ——
迁移到位、句摘 id 不变、阅读页 15 条下划线与 2 段句摘虚线位置正确、
复习页 15 张卡片按正文顺序；长按新建 / 修改（id 不变、不产生重复）/ 删除、
编辑正文触发「原文已删除」弹窗、选保留后位置置空且顶上来的词不被误画线、
把词打回去后自动复活且 id 不变。

测试增至 188 项。

### 这一步解锁了什么

短语归类、卡片可拖拽排序、自动检索词典划生词、有声阅读的「读到哪高亮到哪」——
这四项从前都卡在「笔记没有稳定身份」上，现在前置条件都齐了。

---

## 七之二、~~多 AI 供应商~~ ✅ 2026-08-29 已完成

**一句话**：填充不再只能用 DeepSeek，任何一家「OpenAI 兼容」的服务都能接上。

在复习页点「一键填充」→「AI 设置」，里面多了一排选择：

- **DeepSeek** —— 和从前一样，粘个 Key 就能用，地址和模型都是填好的
- **自定义** —— 自己填**服务地址**和**模型名**，通义千问 / 智谱 / Kimi / OpenAI，
  以及自己电脑上跑的 Ollama，都是这一种

**为什么不是一家一家写**：这些服务对外是同一套接口（`/chat/completions` +
`Bearer` 认证），差别只有地址和模型名两样。所以做法是把这两样从代码里挪出来
变成配置 —— 一份实现接所有家，以后再出新的一家也不用改代码。

### 用起来的几个细节

- **地址随便粘**：结尾多个斜杠、不写 `https://`、把文档里那条完整的
  `.../chat/completions` 整个粘进来 —— 都认，保存时自动规整成正确的样子
- **「测试连接」按钮**：拿 `stood` 这个词真跑一次，当场看到模型给的释义。
  填错地址或模型名不必等填充跑一半才发现
- **各家的 Key 分开记**：从 DeepSeek 切到别家再切回来，不用重新粘
- **错误说人话**：连不上会说「连不上 https://xxx，请检查网络和服务地址」，
  404 会指向「地址或模型名不对」，而不是甩一个状态码
- **兼容性兜底**：个别服务不认「强制 JSON 输出」这个参数，遇到就自动去掉重试一次，
  并记住这件事，后面几批不再白试

### 窄屏上的一次返工（同日）

第一版在手机上「自定义」那屏**显示不全、还能左右轻微滑动**。量出来才知道不是「字多」，
是**一整串没有空格的网址**：提示语里那条示例网址要 302px，而卡片里文字的可用宽度只有 275px，
断不下来就把卡片顶宽，整页跟着能横滑。输入框里的提示「服务地址，如 https://...」同理，
264px 塞进 257px 的框，尾巴被切还没法滑动去看。

三处一起改：

- 提示语删掉那条长网址，示例挪到输入框下面一行小字（`例：api.moonshot.cn/v1 · moonshot-v1-8k`）,
  省掉 `https://` 是有底气的 —— 保存时会自动补上
- 输入框提示词缩成「服务地址」「模型名」
- **根上的那道兜底**：卡片里的文字一律允许在长串中间断行。
  这条比前两条要紧 —— 连不上时错误信息里必然带着用户自己填的那串网址，
  不断行的话换个网址又会犯

顺手还修了错误信息：从前把服务商返回的**整坨 JSON** 原样倒在界面上，
现在只取里面那句话（不是 JSON 的仍原样截断，信息不丢）。

窄到 320px 实测：页面不横滑、卡片内不横滚、三个按钮仍在一行。

### 再一次返工：安全键盘弹出来关不掉（同日）

手机上**第一次**点填充按钮，安全键盘会跟着弹出来，而且关不掉。

根因是一帧闪烁：弹窗的配置原本给的是空初值，等 effect 跑完才从本机读出真正的 Key。
于是第一次打开先按「还没设过 AI」渲染了一帧，那屏上的**密码输入框**被代码自动聚焦
（本意是方便直接粘 Key）—— 安卓的安全键盘就是这么被叫出来的；紧接着 Key 读到了，
界面切走，输入框消失，键盘却还开着，**没有对应的输入框可关**。

两处一起治：

- 配置改成**在初始化时当场读出来**（`useState(loadConfig)`），不再有那一帧
- **取消自动聚焦**。手机上自动弹键盘本来就烦人，用户自己点更可控；
  也就再不会有「键盘弹出来而框已经没了」这种状态

顺带按用户意见调了那段提示：示例不再和提示之间空一行（它们是同一句话的两半），
示例改成两行「地址 …」「模型 …」，并且**把 `https://` 加回来** ——
不写的话看不出这是个网址（真填时不写也行，保存时会自动补上）。

### 「待填充」的标准改成「缺哪格补哪格」（同日）

用户反馈「填充按钮好像只数单词不数句子」。查下来**计数本来就是两者相加**
（造一条全空的句摘，按钮当场从 2 变 3），真正的原因是另一条规矩：

**从前只有「整条全空」才算待填充。** 单词卡有音标 / 词性 / 释义三格，
句摘卡有句型 / 翻译两格 —— 只要有一格写过字，这条就既不计数、也填不上。
于是「写了一半」的卡片没人管：句摘写了句型没写翻译，AI 帮不上忙，
数字里也看不见它。

现在改成：

- **有一格空着就算待填充**（`isWordNoteIncomplete` / `isSentenceIncomplete`）
- 填充时**只往空着的格子里写**（`mergeWordFill` / `mergeSentenceFill`）——
  「绝不覆盖用户写过的内容」这条底线收在这两个函数里，只此一处，
  别处不再各判一遍
- 每批回写前会重新读一遍最新数据：这中间用户自己补了几格的，也不会被盖掉

实测（把某个词的音标、某条句摘的翻译单独清空）：按钮从「填充 2」变成「填充 4」，
弹窗写「待填充：3 个单词·1 个句子」—— 正是用户说的那种数法。测试增至 237 项。

界面上的说法跟着改了：按钮提示「还有 N 条笔记没填全」，
弹窗里说明「只补空着的那几格，你写过的一个字都不动」。

### 填充按钮改为常驻（同日）

从前只有「还有没填全的笔记」才显示，全填完就消失。改成一直在：

- 有得可填：实心琥珀 + 报数（「填充 4」），和从前一样
- 没得可填：收成一枚安静的图标（只剩「填充」二字），与旁边「词/句」同一等级

除了位置固定、不会今天在明天不在，还顺手解决了一个死角：
**「AI 设置」只有这条路进得去** —— 从前全填完之后按钮消失，就再也改不了 Key 了。
没得可填时点开，弹窗写「待填充：都填全了」，「开始填充」是灰的，但「AI 设置」能进。

实测（375px）：两种状态按钮都是 30px 高，工具条 57px 不变，不换行。

### 老用户不受影响

从前那个只存一个 Key 的老格式会被自动认成「DeepSeek 的 Key」，
打开就是能用的状态，不用重新填。保存时也仍往老位置写一份 ——
万一要退回旧版本 APK，那边还认得。

代码上 `enrich/deepseek.ts` 改名成了 `openaiCompatible.ts`，
新增 `enrich/config.ts` 管供应商配置。测试增至 230 项。

---

## 八、待办清单（意向，未拍板）

2026-08-29 记录的一批想法。已做完的见第六节（含「多 AI 供应商」），剩下这些**都还需要先定细节**。

### 要先决定「做成什么样」

| 想法 | 卡在哪 |
|---|---|
| **同一文档内的重复单词也标为重点** | 文库复习是把同名词**合并成一张卡**记频次；单篇复习是一条笔记一张卡。单篇若也合并，**刚做的拖拽排序在那里就用不了**（合并出来的卡没有对应的一条笔记可写回顺序）。两条路：合并（放弃单篇排序），或每张卡都留着、重复的加个「×3」角标（排序还能用） |
| **主题色切换** | 现在的「主题」是三种纸色（标准/青灰/暖白），**全是浅色**。要的是换强调色（现在通体琥珀），还是**深色模式**？后者是大工程，每个颜色都要重配。开关放哪儿已经不是问题了 —— 设置页「阅读外观」那组里再加一行就行 |
| ~~**设置功能更直观**~~ ✅ | 已做，见第十九节。当时散在三处：外观（左侧栏左下角浮层）、AI Key（藏在填充对话框里）、备份恢复（左侧栏），现在收进了一个设置页 |
| **AI 复核已写的笔记** | 现在的「一键填充」只填空白、绝不覆盖。复核是反过来：自己写的让 AI 挑错。关键是**结果怎么交付** —— 直接改掉（危险），还是并排列出「你写的 / AI 的建议」逐条选（推荐，但界面工作量实在） |

### 方向清楚，动手前确认一下即可

| 想法 | 说明 |
|---|---|
| **侧栏左右滑动开关** | 风险在误触：正文要长按取词，横向也可能有滚动。建议**只认从屏幕边缘起手**的滑动 |

### 一条已知的不对称，别当成 bug

手机上右侧栏没有关闭键（与左侧栏一致，靠点遮罩或安卓返回键关闭），
但**宽屏上保留着** —— 那边遮罩是隐藏的，浮动的「笔记」按钮在打开状态下也不显示，
删掉就再没有关掉它的办法了。

---

## 九、下一步从哪接

三个结构短板**全部做完了**，只剩虚拟滚动 —— 等真被长文卡到再说。

**地基解锁的四项**（从前都卡在「笔记没有稳定身份」上）：

- ~~**卡片可拖拽排序**~~ ✅ 2026-08-29 已完成，见下
- ~~**短语归类**~~ ✅ 2026-08-29 已完成，见第十二、十三节
- **自动检索考试词典划生词** —— 代码不难，**难在先找到一份可用的词表**
- **有声阅读的「读到哪高亮到哪」** —— 声音进度对应到 start/end 就行。
  第一步「能读」已完成（第十节），这是第二步

### ~~卡片可拖拽排序~~ ✅ 2026-08-29

复习页右上角那支笔（编辑模式）里，卡片左上角会出现拖拽手柄，按住它拖。
不新增按钮，词和句各排各的。

**只在「单篇文档」的复习里开放。** 文库复习的条目是按拼写合并出来的
（同一个词在三篇文档里各标过一次，那里只显示一条、频次记 3），
拖了没有一条标注可以写回去，所以那边不给手柄。

不依赖任何前置、想做就能做的：**字幕导入（.srt）**、**导出 HTML/Markdown**、
**拍照识别试水**、**英音/美音开关**（地址里 `type=1/2`，代码早留好了）。
有声阅读第一步已经做完了，见第十节。

已知待观察：
- ~~**标注模型刚换，先在手机上正常用几天再清理旧代码**~~ ✅ 已清理，见第二十节。
  ⚠️ 但 **`data.notes` 这个字段删不掉** —— 恢复一份换代之前导出的老备份还得靠它。
  第二十节删掉的是旧的写入函数和旧的 `reconcilePage`
- epub 只用自造的标准包测过，**真实电子书千奇百怪**，遇到怪的再针对性适配
- AI 填的音标准不准还没有实际数据。若错得多，就补一份词典 +「变形还原」
  （`stood -> stand` 那一层）；错得少就省了。一键划词那边是同一个悬而未决的问题

**跑测试**：

```bash
npm test
```

**打包到手机**：见 [发布到手机.md](发布到手机.md)

---

## 十、~~有声阅读第一步：点词/点句读出来~~ ✅ 2026-08-29 已完成

**做成什么样**：复习页里，单词卡上的那个英文词、句摘卡上的那句英文，
本身就是按钮 —— 点一下读出来，后面跟一枚小喇叭点明它可以点。

- 正在念的那个字变亮，念完自动复原
- **点正在念的那个 = 停下来**；念着的时候点另一个 = 直接换成后一个，不排队
- 单词放慢到 0.85 倍速（一个词一闪而过听不清），整句用正常语速
- 卡片别处照旧是「点一下翻开答案」，两件事不打架
  （外层那个点击本来就跳过按钮）

**没有做阅读页的整篇朗读** —— 用户明确说机械音通读没什么好听的，
要的是查发音。这也让第一步的范围小了很多。

### 结构

又是同一个路子（`src/speech/`）：`types.ts` 定约定、`webSpeech.ts` 是实现、
`index.ts` 是入口。现在用的是 WebView 自带的 Web Speech API ——
它背后接的就是手机系统的「文字转语音」引擎，不引任何库、不联网。

**要换成安卓原生插件，只改 `index.ts` 一个文件。**

几处防身的细节：

- **挑嗓子**：优先本地合成（离线可用、开口快），其次 en-US；
  一个英文嗓子都没有就交给系统按 `lang` 自己看着办，绝不拿中文嗓子念英文
- **兜底时长**：安卓 WebView 上 `onend` 偶尔不来，真发生了高亮会一直挂着像卡死，
  所以按字数估一个上限，到点就当念完
- **读不了的手机**：`speechSynthesis` 都没有的话，喇叭根本不画 ——
  不摆一个按了没反应的按钮
- **读不出来时**：工具条下面出一条黄色提示，直说「去系统设置里装一个英文语音包」

### 实测

浏览器里拦下真正交给系统的内容逐项核对：

| 验的东西 | 结果 |
|---|---|
| 单词发出去的内容 | `stumble`，`en-US`，语速 0.85 |
| 句子发出去的内容 | 整句原文，`en-US`，语速 1 |
| 念时高亮 / 念完复原 | 100ms 高亮，500ms 已复原 |
| 点正在念的那个 | 停下 |
| 念着时点另一个 | 前一个熄灭，后一个亮起 |
| 点词会不会连带翻开答案 | 不会 |
| 375px 排版 | 卡片、按钮尺寸正常，页面与卡片都不横滚 |
| 编辑模式 | 拖拽把手与朗读按钮共存，互不影响 |

**真发不发得出声，只有在手机上才知道** —— 浏览器这边是把发声拦下来验参数的。

测试增至 247 项。

### 后来改成了原生（同日，第一版在手机上不出声）

**现象**：手机上点词点句毫无反应，**连喇叭图标都没有**。

**根因**：安卓的 WebView **没有实现网页版朗读接口**（`speechSynthesis`）——
手机 Chrome 有，WebView 没有，这是它的老毛病。代码检测到没有就把喇叭藏了，
所以用户连图标都看不见。**电脑浏览器里测不出这个问题**，
因为电脑 Chrome 是有的。

**改法**：加一个原生实现，绕开 WebView。

- 装了 `@capacitor-community/text-to-speech@8.0.2`（正好配 Capacitor 8），
  直接调系统的 TextToSpeech
- `src/speech/nativeSpeech.ts` 是新实现；`index.ts` 里分路：
  **手机上走原生，电脑浏览器里仍走网页版**（开发时要在浏览器里调界面）
- 约定层（`types.ts`）和界面**一个字没改** —— 当初留的口子这次真用上了
- 错误话术收进 `errors.ts`（纯函数、好单测），两条路共用
- 缺英文语音时，提示条上多一个「**去安装**」按钮，直接跳系统的语音包安装界面
  （`openInstall()`，安卓独有）

**这次学到的**：凡是「浏览器里能跑」的能力，装进 App 之后未必还在 ——
WebView 不等于 Chrome。以后凡涉及系统能力（朗读、相机、文件、分享），
**先在真机上验一个最小版本，再往上盖界面**。

测试增至 250 项。

### 喇叭图标：调了两版，最后删了

原本在文字后面跟一枚小喇叭，点明「这里可以点」。用户觉得偏高，
量出来确实：图标落在**大写高度**的正中，而卡片上的词多是小写，
相对小写中线高了 2px。先改成按小写中线（`align-middle`），
又按用户提议改成「**看结尾那个字母的大小写**」分别对齐 ——
两版都量到了 0.4px 以内。

**但用户看着还是别扭，最后决定删掉。**
文字本身就是按钮、点了会变色，反馈已经够了。
`utils/speakIconAlign.ts` 连同它的测试一并删除。

留个教训：**这种「加个小图标点明可点性」的补丁，在触摸端未必值**。
真正让人看出可点的是「点下去有反应」，不是多一枚图标。

### 踩到的一个坑（记下来免得再犯）### 踩到的一个坑（记下来免得再犯）

`useSpeak()` 一开始被放在了「没选文档就提前 return」的**后面**，
React 的钩子数量因此忽多忽少，整页白屏。**钩子必须在所有提前 return 之前调用。**
这错在浏览器里一眼就看见了，单测看不出来 —— 又一次说明光跑单测不算验过。

---

## 十一、~~新标注按正文顺序插入~~ ✅ 2026-08-29 已完成

**问题**：文中 `apple and ear`，先标了 apple 和 ear，回头再标 and ——
它会跑到复习页卡片列表的**末尾**，而不是待在 apple 和 ear 中间。

**根因**：新建标注给的 order 是 `nextAnnotationOrder`，也就是
「同文档同类型里的最大值 + 1」= 永远排最后。这是当初做拖拽排序时定的，
只考虑了「加到末尾」这一种情况。

**改法**：`utils/annotationOrder.ts`（纯函数、13 项单测）——
按坐标（`L第几行W第几个词`）找到**正文里紧挨着它前面的那一条**，排在它后面；
它要是正文里最靠前的，就排到最前面。

**为什么不干脆整篇按正文重排**：那会把用户拖出来的顺序冲掉。
只认新来的这一条该插在哪，别人的相对次序一概不动 —— 这一点专门写了测试。

取值可能是小数（两条之间取中点）。排序只看大小，够用；
用户下次拖动时 `reorderAnnotations` 会重新编成整数。

孤儿标注（原文已删除、没有坐标）不参与比较，也不会被挤掉。

### 顺序已经被拖乱时，新的落在哪

规则只有一条：**紧跟「正文里它前面那条」，插在那条后面** —— 那条现在排在哪，
它就去哪。正文里它前面没有别人，就插到「正文里它后面那条」的前面。

三种情形都有单测钉住（正文顺序都是 a b c d）：

| 现在的排列 | 新词在正文里的位置 | 结果 |
|---|---|---|
| `d b a c` | 夹在 b 与 c 之间 | `d b 新 a c`（跟着 b） |
| `d b c` | 比谁都靠前 | `d 新 b c`（插在正文后继 b 前面） |
| `d b c` | 比谁都靠后 | `d 新 b c`（跟着 d，而 d 被拖在第一位） |

所以**乱过之后，新卡片不一定出现在列表末尾，也不一定"看着像正文顺序"** ——
它只保证挨着自己在正文里的邻居。这是有意的：列表已经被手动排过，
再谈「正文位置」就没有一致的参照了，「挨着邻居放」是唯一说得通的做法。

要想让某篇文档重新变回正文顺序，现在只能手动拖。
将来若需要，可以加一个「按正文顺序重排」的按钮 —— 那是**用户主动要求**的重排，
和这里「不擅自冲掉手动顺序」并不矛盾。

### 实测（浏览器里走真实流程）

在示例文档里长按 `or`（坐标 L38W2，正文中夹在 Flying `L38W0` 与 below `L38W4` 之间）
保存，然后看库里的顺序：

```
Flying(L38W0 · 6)
or    (L38W2 · 6.5)   <- 新标的，落在正中间
below (L38W4 · 7)
```

复习页卡片顺序同样如此。**其余 15 条的 order 一个没动** ——
这份测试数据本身就不是正文顺序（stood 在正文最前，却排在第 3 位），
正好验证了「不冲掉已有排列」。验完把测试笔记删掉了。

---

## 十二、~~短语模块~~ ✅ 2026-08-29 已完成

**一句话**：长按连选两个词之后，抽屉顶部多了「短语 | 句子」两个标签 ——
从前一段范围只能存成句摘，现在可以存成短语。

### 三个当初卡着的决定（用户拍板）

| 决定的事 | 结果 |
|---|---|
| 怎么告诉 App「这是短语不是句子」 | **抽屉里加一个切换**，不新增手势。默认按长度猜：**四个词以内当短语** |
| 短语卡放复习页哪里 | **并到「词」页签**。短语本质也是词汇，和单词一起背最自然，工具条也不必变三态循环 |
| 短语卡填什么 | **释义 + 用法说明**。释义存 `definition`、用法存 `grammar`（和句子共用同一批字段，备份与对账不必为短语再开一路） |

### 动手时才发现的一件事：排序得并组

`order` 原本是按「同文档 + **同类型**」编号的。短语要和单词显示在同一列卡片里，
可两者各有一套编号（词 0..14、短语 0..2），混在一起按数值排就会乱插。

所以引入了**排序分组**：`vocab`（单词 + 短语）和 `sentence` 各一条队。
`nextAnnotationOrder` / `orderForNewAnnotation` / `reorderAnnotations` 都改成按组。
实测新存的短语落在 6.5，正好排在它首词 Flying（6）之后、below（7）之前。

### 都改到了哪儿

一条短语要同时在五个地方露面，漏一处就是「存进去了但看不见」：

- **正文**：一条**连续实线**（穿过词与词之间的空格，看得出是一个整体）。
  句摘仍是虚线，单词仍是词底下的下划线 —— 三者一眼可分。
  同一段既是句摘又含短语时，实线在内、虚线在外，两条都画
- **抽屉**：「短语 | 句子」切换；短语两格（释义 / 用法），句子两格（句型 / 翻译）。
  重新选中一段已存过的，会自动回到它当初的类型并把内容填回来，还给删除按钮
- **复习页**：「词」页签里，带一枚「短语」标签，不显示音标和词性，
  释义下面多一行用法。拖拽排序照旧可用（和单词同一条队）
- **右侧生词板**：也在「生词」那一栏，带「短语」标签
- **一键填充**：短语有自己的提示词（要的是搭配用法，不是音标词性），
  25 条一批。仍然只补空着的格子

**对账（正文编辑后搬位置）是免费继承的** —— 它本来就按「单词 vs 范围」分支处理，
短语走的是和句摘同一条「范围收缩」的路。

### 实测（浏览器里走真实流程）

长按 `Flying` 再长按 `or` → 抽屉默认选中「短语」（三个词）→ 填两格 → 保存：

| 验的东西 | 结果 |
|---|---|
| 存进去的形状 | `Flying high or`，L38W0–L38W2，释义/用法各就各位 |
| 排序 | order = 6.5，落在 Flying(6) 与 below(7) 之间 |
| 正文 | 一条 2px 连续实线盖住三个词；两条句摘虚线不受影响 |
| 复习页 | 卡片在 Flying 与 below 之间，带「短语」标签、无音标行 |
| 右侧生词板 | 有这一条，带「短语」标签 |
| 重新选中同一段 | 自动回到「短语」、内容填回、出现删除按钮 |
| 六个词的范围 | 默认落在「句子」，字段是句型/翻译 |
| 删除 | 标注没了、实线没了、两条句摘虚线还在 |

测试增至 285 项（新增短语填充 11 项、读模型分路 8 项）。

### 下一步：一键划词（决定已拍板，未动手）

阅读页放一个按钮，AI 通读全文、挑出重点词与短语并直接填好。三个决定：

1. **依据**：AI 判断，**按难度等级**，用户自己选档（四级 / 六级 / 考研 / 雅思）。
   不走考试词表 —— 难在弄到带释义的数据，而且词表基本覆盖不了短语
2. **重复词**：同一个词在一篇里出现多次，**只划第一次**
3. **短语**：模块已经就绪，可以一起划

**真正的难点是「定位」不是「挑词」**：AI 返回的是文字，得对应回坐标。
风险是 AI 给原形（take off）而正文是变形（took off）、或者干脆编一个文里没有的词。
解法：要求 AI 返回**原文里的确切写法 + 所在行号**，在那一行里精确查找，
**找不到就跳过并如实报数**（「挑了 42 个，划上 38 个，4 个没对上」）。

还要配一个**整批撤销** —— 一次划出几十条，不能撤销的话用户不敢按。

---

## 十三、~~短语模块的两处返工~~ ✅ 2026-08-29

用户在手机上试完短语模块，说「问题挺多」。问清楚是**两件事**，一件是观感、
一件是真 bug。

### 一、三条线撞在一起

**用户的原话**：「划短语的那条实线和划句子那条虚线重叠了，而且划词已经也是实线了。」

两个毛病同时存在：

- **短语和单词长得一样** —— 都是 2px 琥珀实线，只有长短之分
- **短语的实线把句摘的虚线整条盖住**

**根因（量出来的，不是猜的）**：短语用 `border-b-2`、句摘用 `border-b`，
两层 span 套着，各自 `padding-bottom: 1px`。行内元素的内边距**不撑开高度**，
所以两层的内容盒底部在同一个位置，两条边框必然画在同一条水平线上。

**改法**：三条线**换成同一套画法**（`text-decoration`），都从基线往下量，
才谈得上「错开」。位置一律用 `em` —— 正文字号是用户可调的。

线型也各不相同，不必靠长短去分辨：

| | 线型 | 粗细 | 离基线 |
|---|---|---|---|
| 单词 | 直实线 | 2px | +2px |
| 短语 | **波浪线** | 1px | +0.36em |
| 句摘 | 虚线 | 1px | +0.6em |

**纵向预算是量出来的**（18px 字号、行距 1.8）：基线往下 4px 是内容盒底部
（从前两条线都落在这儿），9.7px 是行盒底部，再往下还有 5.7px 行间空气
才碰到下一行的字顶 —— 也就是基线以下约 15px 都是安全的。

**为什么没走「底色」这条路**：淡琥珀底色已经被三个状态占着了
（按住中 `amber-300/70`、当前选中的词 `amber-200/80`、正在框选句摘范围
`amber-100/70`）。短语再用底色，会和「正在框选」撞脸。

三个类名收在 `LyricEditor.tsx` 顶部（`WORD_LINE_CLASS` / `PHRASE_LINE_CLASS` /
`SENTENCE_LINE_CLASS`），三条线摆在一处才好对着调。

### 二、右侧生词板里短语总排在最后

**用户的原话**：「新的短语会被直接放进最后，而不是像划新词那样找到文中的位置
参与排序，但是复习模式下短语卡片的位置似乎又没有问题。」

**根因**：右侧栏那份清单是**分两步拼**的 —— 先把单词按顺序排好塞进去，
再用第二个循环把所有短语一股脑追加在末尾，从头到尾没让两者比过顺序。
数据本身没错（短语的 `order` 存的就是 6.5 这种正确值），纯粹是拼清单的方式不对。

而复习页走的是**另一条路**：直接从标注表里把单词和短语一起捞出来、一起按 order 排。
所以「复习页对、右侧栏不对」—— 同一份数据，两条路。

**改法**：新增读模型 `buildVocabList()`（`utils/annotationViews.ts`），
右侧栏改走它，和复习页同一个算法。App.tsx 里那段 44 行的拼装代码缩成 4 行。

顺手统一的两件小事：短语不再往清单里塞音标/词性（卡片上本来就不画那两格）；
键撞车时退回用 id 的规则和 `buildNotesIndex` 保持一致。

### 实测（浏览器里走真实流程）

造了一段三线叠加的极端情况：句摘 `Is to wish my life away` 里套一条短语
`wish my`，短语里再套一条单词笔记 `wish` —— 三条线同时画在 `wish` 这一个词上。

| 验的东西 | 结果 |
|---|---|
| 单词线 | solid / 2px / 离基线 2px |
| 短语线 | **wavy** / 1px / 离基线 6.48px |
| 句摘线 | dashed / 1px / 离基线 10.8px |
| 三条线的间距 | 彼此隔开 1.5px 以上；最深的一条离下一行字顶还有 3.6px |
| 嵌套关系 | 句摘 ⊃ 短语 ⊃ 单词，三条同时画得出来 |
| 旧的 border 画法 | 页面上一处不剩 |
| 右侧栏顺序 | `wish` → **wish my** → `dream`；`Flying` → **Flying high or** → `below` |
| 复习页顺序 | 与右侧栏**逐条一致** |

验完把测试标注删干净了（20 → 17 条，无残留）。

新增 `utils/buildVocabList.test.ts`（8 项）钉住排序那个 bug，
测试增至 293 项。

### 留个教训

**同一份数据被两处界面用，就要走同一条路。** 这次的 bug 不是算错了顺序，
是右侧栏压根没算 —— 它自己拼了一份。读模型（`annotationViews.ts`）当初就是
为这个立的规矩，右侧栏是唯一一处还在自己拼的，现在收回来了。


---

## 十四、API Key 那格粘贴不了（2026-08-29，第一轮）

**用户的原话**：「填充功能那里不是要先输入 deepseek 的 api 吗？这个输入 api 的框
居然没法使用粘贴功能。」

### 诊断：代码里没有任何东西挡着粘贴

翻了一遍可能拦路的地方，全都干净：

- 全局样式里没有「禁止选中文字」（`user-select: none`）那类规则
- 弹窗和它的外层没有任何拦截长按的代码 ——
  正文里那个「长按取词」确实会吃掉长按，但只挂在单词 span 上，跟弹窗不沾边
- 安卓那边是 Capacitor 的默认壳子，`MainActivity` 是空的，没做过任何定制

那个输入框就是一个最普通的 `<input>`。所以问题在 **WebView 本身**，
两个已知原因都对得上：

1. **安卓 WebView 长按「空的」输入框，经常根本不弹「粘贴」气泡**。
   手机 Chrome 会弹，WebView 不弹。而 API Key 这格第一次用时永远是空的
2. 那格是 `type="password"`，部分 WebView 版本对密码框不给选择/粘贴菜单

**这两个在电脑上分辨不出来** —— 电脑 Chrome 两种情况都正常。
又是那条老教训：**WebView 不等于 Chrome**（朗读那次栽的是同一件事）。

### 第一轮：先试不装插件的做法（用户拍板）

只改一处：**Key 那格默认改成明文文本框**（`type="text"`），
右边加一枚眼睛可以遮起来。同时关掉自动大写/自动纠错/自动填充
（`autoCapitalize` / `autoCorrect` / `autoComplete` / `spellCheck`）——
Key 是一串随机字符，被输入法「纠正」过就废了。

这一改同时办两件事：

- **验证假设 2**：如果换成普通文本框长按就能粘了，那原因就是密码框
- **本来就该有**：Key 是一长串，粘成一串圆点的话粘错、粘漏都看不出来，
  只能等填充跑起来报错才知道。现在粘完能当场核对

默认明文而不是默认遮住 —— 自己的手机、自己的 Key，没什么可藏的；
真要遮就点那枚眼睛。

### 浏览器里验过的

| 验的东西 | 结果 |
|---|---|
| 那格的类型 | `text`（从前是 `password`） |
| 输入法干扰 | autocomplete/autocapitalize 都是 off，spellcheck 关 |
| 眼睛按钮 | 36×36，点一下变 `password`、标签变「显示 Key」，再点回来值不丢 |
| 文字会不会被眼睛压住 | 输入框右内边距 36px，正好等于按钮宽度，不会 |
| 375px 窄屏 | 输入框 275px，页面不横滑、卡片内不横滚 |

### 如果这一轮还是粘不了

那就是原因 1（WebView 对空输入框不弹粘贴菜单），跟密码框无关。下一步：

- **先试不用插件的粘贴按钮**：Capacitor 是从 `https://localhost` 提供页面的，
  属于「安全上下文」，`navigator.clipboard.readText()` 有可能直接能用
- 不行再装 `@capacitor/clipboard`，走原生 —— 和朗读那次同一个路子

**系统能力这种事只有真机说了算**，浏览器里测不出来。


---

## 十五、~~AI 角标在句子卡上多占一行~~ ✅ 2026-08-29

**用户的原话**：「句子卡的四芒星角标不知怎的会加到下一行制造一行空白。」

**用户观察到的那个不对称就是线索**：单词卡没这毛病，只有句子卡有。

### 根因：inline-block 是一个整块

角标从前跟在朗读按钮的**后面**：

```
<p>
  <button>整句英文</button>
  <角标 />
</p>
```

`<button>` 是 `inline-block`。inline-block 是一个**整块** ——
里面的文字换行之后，这个块的宽度就变成**整行宽**（不是最后一行的宽度）。
后面再没有位置放角标，角标只好掉到下一行，独自占一行。

- **句子长了才会换行**，所以只有句子卡看得出来
- 单词就一个词，块永远很窄，角标挤得下 —— 这就是那个不对称的由来

**量出来的**（375px 窄屏，一条三行的句子）：按钮 66px 高、外层容器 97px 高
—— 凭空多出 **31px** 的空白。

**试过但行不通的改法**：把按钮改成 `display: inline`。
浏览器**强制**把按钮变成 inline-block，连 `!important` 都压不动
（算出来仍是 `inline-block`）。这条路直接堵死。

### 改法：角标搬进按钮里面

```
<button>整句英文<角标 /></button>
```

角标成了按钮内部行内流的一部分，跟着最后一个词走，自然待在最后一行。
单词卡也一并搬进去，两处保持一致。

顺手治了第二个毛病：角标那个 span 从前会**撑到一整行的行高**
（10px 的图标占出 24px 的盒子），上标又是抬高的，于是每条带角标的卡片
行高都被顶高 8px。加 `leading-[0]` 之后盒子就是 10×10。

代价：点那枚 10px 的角标现在会触发朗读（从前是翻开答案）。
它紧挨着本来就点了会朗读的文字，这个改变可以忽略。

### 实测（375px 窄屏）

| 验的东西 | 改前 | 改后 |
|---|---|---|
| 三行长句：容器比按钮高出 | **31px**（多一整行） | **1px** |
| 单行短句：容器比按钮高出 | 8px | 2px |
| 角标盒子 | 10×24 | 10×10 |
| 角标离最后一个词 | （掉到下一行） | 2px，同一行 |
| 单词卡 | 正常 | 正常（多出 0px） |
| 右侧栏生词条目 | 正常 | 正常（那里本来就在按钮里面） |
| 右侧栏句摘条目 | 正常 | 正常（纯文本，不经过按钮） |

右侧栏的句摘**从来没坏过** —— 它的句子是直接写在 `<p>` 里的纯文本，
不经过按钮。这一点正好反证了根因就是那个 inline-block 按钮。

验完把测试数据清干净了（17 条标注，无残留）。

### 留个教训

**inline-block 里的文字一换行，这个块就占满整行宽** ——
凡是「文字后面跟一个小标记」的排版，标记必须放在那个 inline-block 的**里面**，
放外面迟早会掉下去。而且这个坑**只有文字够长、真换行时才现形**，
短文字上怎么试都是好的。


---

## 十六、~~改了卡片，AI 角标却不消失~~ ✅ 2026-08-29

**用户的原话**：「设计的初衷是当我修改了卡片的内容后四芒星角标就消失，现在修改
单词卡片好像是必须清空一个框才会触发消失……而对于短语卡，问题不止于此，
你修改了退出编辑模式结果删掉的东西又回来了，它好像并没有被记忆。句子好像没问题。」

查下来是**两个各自独立的 bug**，凑巧症状看着像一个。

### bug 一：改动只在「失焦」时才保存

角标不消失只是表象 —— **真正没发生的是保存**。

那两格输入框**只有 `onBlur` 一个保存触发点**。手机上改完直接点铅笔退出编辑模式时，
输入框往往还没来得及失焦就被换成阅读态了，那次改动就这么悄悄没了：
数据没存，角标自然也不会消失。要是中间先点了别处（输入框失了焦），
它就存得上 —— 这就是「有时灵有时不灵」的由来。

**浏览器里复现过**：把释义从「飞鸟」改成「鸟」，不触发失焦、直接点铅笔退出，
落盘的仍是「飞鸟」，`auto` 还在。

**改法**：进编辑模式时登记一个清理函数，**离开编辑模式（或卡片被卸掉）时兜底保存一次**。
不管失焦事件来不来得及，改动都保得住。单词卡、短语卡、句子卡三处都加了 ——
句子卡有同样的隐患，只是还没撞上。

### bug 二：短语的改动**根本存不进去**

`updateAnnotationsByWord()` 里写死了一句 `if (a.type !== 'word') return a` ——
**短语被静静跳过**：不报错、也不写库。所以短语卡改完退出就打回原形，
连带角标也永远清不掉。这跟 bug 一无关，给足正常的失焦也一样存不进去。

**根子在名字**：这个函数叫 `updateAnnotationsByWord`，「word」既指
「按拼写找」又被读成「只管单词」。后来加短语时，数据层免费继承了一大半，
唯独这一处的类型判断没人想起来改。

**改法**：函数改名 `updateVocabByText`，判断改成「**除了句摘都算**」。
单词和短语在复习页本来就是同一列卡片，改法也该是同一条路。
短语的原文必然带空格，和单个词撞不了车。

### 实测（浏览器里走真实流程）

| 场景 | 改前 | 改后 |
|---|---|---|
| 单词卡改一个字 → 直接点铅笔退出 | 改动丢失、角标还在 | 存进去了、角标消失 |
| 短语卡改释义 → 正常失焦 | **完全没存**、角标还在 | 存进去了、角标消失 |
| 短语卡改用法 → 正常失焦 | 完全没存 | 存进去了，释义不受影响 |
| 短语卡改完 → 直接点铅笔退出 | 完全没存 | 存进去了、角标消失 |
| 句子卡改完 → 直接点铅笔退出 | 改动丢失 | 存进去了、角标消失 |

新增 2 项单测钉住「短语也要改到」和「短语与单词按原文各认各的」，
测试增至 295 项。

### 留个教训

**函数名里的类型词会变成陷阱。** `updateAnnotationsByWord` 里的「Word」
本意是「按拼写」，读起来却像「只管单词」—— 加短语时全项目都改到了，
唯独这一处的 `type !== 'word'` 没人想起来。以后凡是「按某个键找一批标注」的函数，
名字里别带类型词，判断也宁可写成「排除谁」而不是「只要谁」。

**还有：只有一个保存触发点是危险的。** 输入框的 `onBlur` 在手机上不保证来得及 ——
组件先被卸掉，事件就永远不来了。凡是「边改边存」的界面，
除了失焦还要有一条卸载时的兜底。


---

## 十七、~~一键划词~~ ✅ 2026-08-29 已验收

阅读页生词板右上角一枚魔杖按钮：AI 通读当前这一篇，挑出值得记的单词和短语，
**直接划在正文上并填好释义**，划完出一条战报，可以整批撤销。

### 用户拍板的四件事

| 决定的事 | 结果 |
|---|---|
| 按钮放哪 | **右侧生词板右上角**。它做的是「对着这一篇正文挑词」，而生词板正是这一篇笔记的所在 —— 划完新词就出现在下面那张清单里 |
| 依据 | **AI 判断，按难度档**，用户自己选（四级 / 六级 / 考研 / 雅思）。不走考试词表 —— 难在弄到带释义的数据，而且词表基本覆盖不了短语 |
| 一次划多少 | **每次自己选**：精挑（15–20）/ 适中（30–40）/ 尽量多。和难度档一起放在弹窗里，记住上次的选择 |
| 撤销 | **跑完出一条提示条**「已划上 38 条，4 条没对上原文 · 撤销」，离开这篇文档就消失 |

### 真正的难点：定位，不是挑词

AI 返回的是**文字**，而标注要挂在**坐标**上（第几行第几个词）。三种翻车方式：

- 给了原形而正文是变形（take off / took off）
- 行号记错
- 干脆编一个文里没有的词

对策：**只认它自己给的那一行，在那行里精确查找，找不到就跳过并如实报数**。
不去全文搜、不做模糊匹配 —— 猜错了会把线画到无关的词上，那比少划几个难受得多。

`mark/locate.ts` 是纯函数，17 项单测钉住了上面每一种翻车方式。
另外两条规矩也在这里：**同一个词一篇里只划一次**（用户手标过的也算数），
**是词还是短语按它实际占了几个词算，不听 AI 的**（AI 说是短语却只给一个词的情况是有的，
照它说的存会得到一条没有音标、却挂着「短语」标签的怪东西）。

### 分层

又是同一个路子（`src/mark/`）：

- `types.ts` 定约定，`locate.ts` 定位（纯函数），`parse.ts` 收模型返回的 JSON，
  `openaiCompatible.ts` 是提示词与调用，`runner.ts` 管分批与中断，`index.ts` 是入口
- **请求管道和「一键填充」共用**：把原来藏在填充器里的 `call` 抽成
  `createChatCaller`，地址、认证、JSON 模式兜底、错误话术只此一份
- **Key 也共用**：对用户来说本来就是同一个服务，没道理配两遍。
  划词弹窗里没配好时给一个「去设置 AI」，直接借填充那张设置屏

`parse.ts` 的职责只有一个：**认不出的条目丢掉，绝不抛错** ——
一条格式不对不该让整批白跑。9 项单测。

### 送出去的东西

只送**含英文词的行**（中文对照行一行不送，省一半的量），
但行号一律用正文里的真实行号 —— 定位就靠它，不能重新编号。
每批 40 行。弹窗里如实写明「这篇文档的英文正文会被发送给 xxx」。

### 实测（浏览器里走真实流程）

真发请求会花用户的钱，所以是**在页面里拦下网络请求喂一份假的 AI 回复** ——
除了模型本身，解析、定位、建标注、排序、画线、撤销全都是真的跑了一遍。
假回复里故意混了三种对不上的：已标过的、行号错的、给原形的、编造的。

| 验的东西 | 结果 |
|---|---|
| 送出去的内容 | 25 行，中文对照行一行没送，行号是原文行号（0、2、4…） |
| 报数 | 「划上了 3 条，另有 3 条 AI 挑了但没对上原文，已跳过」 |
| 落库的形状 | `sorted out` 存成**短语**（有用法、无音标），三条都带 `auto` 记号 |
| 排序 | order = 2.5 / 2.625 / 2.75，按正文顺序插在 stood(2) 之后，不堆在最后 |
| 正文 | fairly / wanna 画上单词直线，sorted out 画上短语波浪线 |
| 生词板 | 三条都出现在清单里 |
| 撤销 | 三条全删、正文线消失、战报条自己收起，**原有 15 条一条没动** |
| 未配置 AI 时 | 弹窗改成「得先设好 API Key」+「去设置 AI」，能跳到设置屏 |
| 375px 窄屏 | 卡片 309px，页面不横滑、卡片内不横滚，难度四个 / 数量三个各自一行 |

新增单测 40 项（定位 17、解析 9、分批 6、记档 5，另有 3 项在别处），测试 295 → 333。

### 撞到的一个坑（浏览器里当场抓到，单测抓不到）

第一版跑完之后**数据进了库，正文上却一条线都没有**。
原因是划词那层只调了 storage 写库，没把新数据推回 React 的 `appData`，
界面拿的还是旧的那份。改成由上层给一个 `writeAnnotations` 回调
（里面 `setAppData(await addAnnotations(list))`），和「一键填充」那边一个路子。

**留个教训**：这个项目里「写库」和「界面更新」是两件事，
storage 的函数只负责前者，返回的新数据必须有人接住往 `setAppData` 里送。
单测只看得到库，看不出界面没动 —— 又一次说明光跑单测不算验过。

### 用户试完提的两条，当日已改

**一、提示词走样了**：填充要求「不是原形就在释义末尾用括号补上原形」
（flying -> 飞行；飞翔（fly 现在分词）），划词却没这一条 ——
同一本书里两种写法混着出现。

根子是**同一套要求写了两份**。所以不是补一句话了事，而是把字段要求抽成
`enrich/fieldSpecs.ts` 一份，两边的提示词都从它拼出来：
音标、词性、单词释义（含原形括号）、原形、短语释义、短语用法。
划词也跟着把 `lemma` 一路带到标注里，和填充存进同一个字段。
另配 6 项单测钉住「两边用的是同一份要求」，再走样会当场红。

**二、点魔杖再点「AI 设置」，弹窗内容变成了填充的**。
第一版图省事，划词那边没有设置面板，直接把用户甩到填充弹窗去 ——
标题、说明、按钮全变了，逻辑明显不对。

改法：把设置面板抽成独立组件 `AiSettingsPanel`，两个弹窗各自渲染它。
现在划词弹窗里点「AI 设置」就在原地展开，保存后回划词、取消也回划词；
填充那边同理。顺带把面板里那句「填充由 AI 完成」改成
「填充和划词都由 AI 完成」。

这一改还顺手把 `AutoFillDialog` 里 140 行设置界面和一半的 state 搬了出去。

浏览器里验过：划词弹窗里保存 Key 后回到划词屏、点「AI 设置」原地展开、
取消回划词屏；填充那边三步同样正确，之前修的明文 Key + 眼睛按钮都还在。
测试增至 339 项。

### 还没做的

- **只对单篇**。整个文库一次划下来太贵，也不好撤销
- 战报条**离开这篇文档就消失**，关掉 App 再回来就撤不了了（用户选的就是这一档）
- 音标准不准仍未知，和「一键填充」同一个问题

### 留个教训

**同一套要求写两份，一定会走样。** 提示词也是代码 ——
填充和划词往同一批格子里写字，要求就该收在一处。
这和第十六节那条「同一份数据被两处界面用，就要走同一条路」是同一个道理。


---

## 十八、~~生词卡改成「点一下展开」~~ ✅ 2026-08-29

**用户的原话**：「当短语过长的时候，右侧栏的小卡片放不下，于是短语就是超出卡片。」
「把右侧栏生词卡片做成句摘卡片那样，可以点一下展开。短语太长一行显示不下就省略号，
点击展开可以查看全部。」「句摘的点击展开有点问题，即便是没有需要展开的情况下
点一下卡片，卡片还是展开了一点空白。」

> 先做过一版「换行、最多两行」，用户看过之后改了主意 —— 那一版已 revert
> （提交留着当存档），现在这一版是照「像句摘卡那样点一下展开」重做的。

### 溢出的根因不是「卡片太小」

显示词的那个按钮上写着 **`shrink-0`（永不收缩）**—— 宁可撑破容器也不截断。
当初那个位置只可能放一个单词，这样写是对的（防止旁边的音标把词挤扁）；
后来同一个位置开始放短语，这行字就从保护变成了破坏。

**反证**：复习页那张短语卡没有这个毛病，区别就在这一个类名。

**量出来的**（375px，38 字符短语）：按钮 278px，卡片可用宽只有 223px
—— 超出卡片 56px，比整个侧栏还宽 38px。

### 两张卡共用的一条新规矩：装得下就不给点

用户报的第二个问题（句摘点一下凭空多出一截空白）根子是**没判断有没有东西可展开**：
展开态的下内边距比平时多 4px，而这个切换跟内容是不是真被截断无关。
于是三行以内的短句子点一下也进展开态 —— 点了个寂寞，还撑高一点。

现在两张卡都先测「内容是不是真的被截断」
（`utils/isClamped.ts` 纯函数、5 项单测；`hooks/useIsClamped.ts` 盯着元素，
侧栏宽度变了跟着重测）：

- **装得下就根本不给点**（没有手型光标、没有 hover、点了没反应）
- 句摘卡的内边距不再随展开变化，从源头上不会有那 4px

钩子里有一处要紧的：**展开期间不测**。展开后截断类名已经去掉，
这时再测必然得出「没被截断」，「能不能展开」立刻翻假，卡片会自己缩回去、来回抖。

### 生词卡

- 短语**一行放不下就省略号**，点卡片展开看全，再点收回。同一时间只开一张（和句摘一致）
- **点词本身仍是「跳到文中」**，不会连带展开（那次点击被拦住了）
- 短语和「短语」标签**留在同一行**。中途试过让短语独占一行，把可用宽度从 173px
  让到 223px（多装三分之一），但标签被挤到下一行去 —— 用户说「特别难受」，
  已改回。反正一行放不下就是省略号，早截一点无所谓，点开就看全了；
  改回来之后卡片还从 95px 矮到 67px
- **单个词照旧不收缩**、仍和音标同行 —— 词短，挤得下，也更紧凑；
  它永远是一行，所以也永远不可点，行为一点没变

顺带把这张卡从循环里的内联 JSX 抽成了 `VocabCard` 组件（要用钩子，必须是组件）。

### 实测（375px）

| 验的东西 | 结果 |
|---|---|
| 38 字符短语 | 173px 一行、省略号，留在 243px 卡内，侧栏不横滚（改前 278px、超出侧栏 38px） |
| 「短语」标签 | 和短语同一行，在它右边 8px；展开成两行时贴着文字垂直居中，仍不另起一行 |
| 点它展开 | 变两行、全文可见、仍在卡内；再点收回 |
| 9 字符短短语 | 装得下 → 不可点，行为不变 |
| 普通单词 | 不可点，宽度位置全不变 |
| 句摘·没东西可展开的那张 | 点前点后都是 60px，**不再凭空长高** |
| 句摘·翻译被截断的那张 | 正常展开 60px → 93px |

测试增至 344 项。

### 留个教训

**`shrink-0` 这类「永不收缩」的写法，是给「内容长度可控」的元素用的。**
往一个已有位置塞新类型的内容时，先看看那儿的布局假设还成不成立。

**还有：「点一下展开」必须先知道有没有东西可展开。** 不判断的话，
装得下的内容点一下也会进展开态，多出一截空白 —— 用户一眼就看出不对。

---

## 十九、~~设置收进一个页面~~ ✅ 2026-08-30 已验收

分支 `settings-page`。第八节里那条「设置功能更直观」。

### 原来为什么不直观（量出来的，不是凭印象）

左侧栏底部原本是**五个光秃秃的图标**：导入书籍、阅读外观、导出备份、恢复备份、回收站。
三个毛病：

1. **一个字都没有**，全靠 `title` 属性 —— 那是鼠标悬停才冒出来的提示，
   **手机上根本不存在**。五个格子等于五个哑谜，只能挨个点开试。
2. **三类性质不同的东西排成一模一样的五格**：导入是天天用的动作、
   回收站是东西的去处、另外三个是低频设置。排得一样宽，反而更难分辨。
3. 250px 的侧栏切五份，每份 50px，图标只能挤在正中，连放两个字的地方都没有。

AI 设置的毛病不是「散」，是**没有家**：它只作为「一键填充」「一键划词」
两个弹窗里的一个分支存在，想换个 Key 得先打开一键填充、假装要跑一次填充，
再点里面的「AI 设置」。组件本身早就是两边共用的，问题纯粹在入口。

### 改成什么样

底栏五格降到三格，**带上文字**：`导入` `回收站` `设置`。
量过：375px 窄屏上每格 82.3px、高 56px，最长的「回收站」三个字占 33px，宽裕。

设置页（[SettingsDialog.tsx](src/components/SettingsDialog.tsx)）分四组：

| 组 | 内容 | 从哪来 |
|---|---|---|
| 阅读外观 | 字号 / 字体 / 主题 | 原样搬走了左下角那个浮层 |
| AI | 一行摘要（发给谁 · 用哪个模型），点进去是设置面板 | 直接嵌 `AiSettingsPanel`，没重写 |
| 数据 | 导出备份 / 恢复备份 | 从底栏搬进来，隐藏的文件框也跟着搬 |
| 关于 | 用户协议与版权声明、版权行 | 新加 |

「用户协议」从前只在**头一次启动**弹一次，之后再也看不到。现在正文收在
[agreement.ts](src/agreement.ts) 里，首启那张卡和设置页「关于」共用同一份，
改了不会只改一处。

### 四个拍板过的取舍

1. **导入和回收站不收进设置。** 导入是高频动作；回收站有个数字角标，
   有东西的时候得一眼看得见。收进去反而变难用。
2. **两个弹窗里的「AI 设置」按钮保留。** 头一回用填充或划词的人就地能配才顺，
   赶去设置页再走回来更烦。设置页只是多给它一个固定的家，不是唯一入口 ——
   三处共用同一个 `AiSettingsPanel`，不是写了三遍。
3. **「一键划词」的难度和数量没有搬进来。** 那是每次跑之前的选择
   （这篇挑多点、那篇挑少点），不是一劳永逸的偏好，搬走反而更难用。
4. **不显示版本号。** `package.json` 里一直是 `0.0.1`、从来没动过，
   摆出来只会骗人。真要让用户看得出手里是哪一版，得先有个会跟着走的版本号。

### 子屏怎么退

设置页里有两个子屏（AI 设置、用户协议）。返回键**只登记一层**，
自己判断退到哪：在子屏里退回列表，在列表里才关掉。
右上角那枚 X 同理 —— 在子屏上它是「返回」，在列表上才是「关闭」。
`BackPriority.settings` 定成 80，和回收站同层（两者不会同时开着）。

### 实测（浏览器里走真实流程）

375px 窄屏：

- 底栏三格 82.3 × 56，标签宽 22 / 33 / 22px，`scrollWidth - clientWidth = 0`，不溢出
- 设置页 343.3 × 690.5（812 的 85% 是 690.2，正好卡在上限、内部滚动），
  页面没有横向滚动条
- 点 AI 那行 → 标题变「AI 设置」，卡片缩到 312.9px，Key 输入框 317.3px；
  点「取消」→ 标题变回「设置」
- 点「用户协议与版权声明」→ 五条全在；点 X → 退回「设置」而不是关掉
- 字号 + 一下：正文从 18px 变 20px，localStorage 同步写成 20
- 三种主题：正文背景 `#ffffff` → `#f8f9f8` → `#fffefc`，确实各不相同
- 列表页点 X、点遮罩空白处：都能关掉

1280px 宽屏：卡片 448px 宽、居中（x=416），高 680.4（85vh 上限），底栏三格不变。

### 量出来才发现的一处

AI 那行本来打算上行写供应商名、下行写 `describeTarget()`。
在浏览器里一看，内置的 DeepSeek 两行**一模一样**（`describeTarget` 对非自定义
供应商就是返回 `preset.name`）。改成：上行 `describeTarget()`（自定义时显示实际域名，
因为「自定义」这三个字等于没说），下行显示**模型名**。

### 已知的一点小退步

在手机上调字号时，设置页盖住了正文，看不见字变大变小 ——
只能看设置页里那个数字。从前那个浮层能露出侧栏外面约 125px 的正文。
实际用下来如果碍事，再想办法（比如调字号时让卡片临时半透明）。

### 没动的地方

`AiSettingsPanel`、`AutoFillDialog`、`AutoMarkDialog` 一行没改。
`useBackHandler` 只多了一个常量。数据流完全没动 ——
`readerSettings`、`onExportBackup` 这些 LeftSidebar 手上本来就有。

---

## 二十、~~清掉标注模型换代留下的旧代码~~ ✅ 2026-08-30 已验收

分支 `cleanup-legacy-notes`。交接说明里那两件「到期收尾」的第一件。

### 为什么当初留着

2026-08-29 换了「笔记怎么存」的底层做法（第七节）：旧的一套**拿坐标当身份**
（`L0W2` = 第 0 行第 2 个词，键就是位置），新的一套每条标注有自己的 id。

换的时候**故意没删旧的** —— 两套并存当保险，万一新模型出问题，
旧数据和读写它的代码都还在原地。新模型在真机上跑了一阵没出事，保险可以拆了。

### 删了什么（净删 528 行）

| 在哪 | 什么 | 为什么能删 |
|---|---|---|
| `storage.ts` | `saveNoteForPage`、`deleteNoteForPage`、`updateWordEverywhere` | 全库零调用 |
| `reconcile.ts` | `reconcilePage` 及其私有帮手 `unmark`、`locateSequence`、`ReconcileResult` | 只有它自己的测试在调 |
| `reconcile.ts` | `ORPHAN_PREFIX`、`isOrphanKey`、`makeOrphanKey` | 旧模型专用（孤儿得编个假坐标），随 `reconcilePage` 一起死 |
| `reconcile.test.ts` | 测 `reconcilePage` 的 18 个用例 | 测的是已删掉的东西 |

测试从 344 项降到 326 项。少掉的 18 项覆盖的场景，
`reconcileAnnotations.test.ts` 里那 24 项一条不落地覆盖着 —— 没留下空档。

### ⚠️ 交接说明里那条写得不准

原话是「连同 `data.notes` 一起清掉」。**`data.notes` 删不掉**，它还在给两件事干活：

1. **分词迁移**（`she's`、`COVID-19` 那次规则修正，见第二节）修的是**旧形状**的
   数据，修完得靠 `replacePageNotes` 写回去 —— 所以那个函数留着，
   虽然它和被删掉的三个长得一模一样
2. **恢复老备份**。恢复一份**换代之前导出**的 .json 时，里面只有旧的 notes，
   得留着这条路才能再转成标注表

准确的说法：**删掉的是「写」旧数据的函数和旧的对账逻辑；
`data.notes` 这个字段本身要留着，当「读老备份」的入口。**

`migrateAnnotations.ts` 里那份 `LEGACY_ORPHAN_PREFIX` 也是同理 ——
生成假坐标的代码删了，但**认出**存量假坐标的常量得留着。

### 实测（浏览器里走真实流程）

单测只能证明零件没坏，对账这条路必须在浏览器里真编辑一遍。四步都过了：

1. **正文前面插一行** —— 四条标注整体下移一行、词一个没认错：
   `stood` L0W2→L1W2、`stumble` L8W2→L9W2、`learned` L10W1→L11W1、
   `bursting` L46W3→L47W3；词数 155→160（新行 5 个词，对得上）
2. **删掉划了线的 `stood`** —— 「原文已删除」弹窗照常弹出，列着 stood
3. **点「保留」** —— 下划线从顶上来的 `up` 身上撤走（这正是旧模型
   会出事的地方：坐标没消失、只是换了别的词站上去，于是给错的词画线），
   复习页里 stood 带上「原文已删除」标记
4. **把 `stood` 打回正文** —— 自动重新挂上 L1W2，标记自动清掉，不再弹窗

控制台零报错。

### 留个教训

「文件里加了标记」不等于「标记是对的」。这次三处标记里有一处
（`data.notes` 可以一起删）是错的 —— 当时写标记的人没想到**恢复老备份**
这条路还要用它。动手前把每个标记**逐个去代码里核实一遍**，
别照着清单删。

---

## 二十一、~~句摘被改短就永久失真~~ ✅ 2026-08-30 已验收

分支 `sentence-keep-original`。用户报的：
「编辑全文改了一个已经加了笔记的句子，笔记里的句子直接失去对应的部分且没有提示，
把那个单词恢复回去，句子也不会恢复。」

### 根因：一行代码把「你当初划的是哪句」覆盖掉了

旧代码在范围收缩时是这么写的：

```ts
const text = getRangeText(newContent, newList, nextStart, nextEnd)
kept.push({ ...a, start: nextStart, end: nextEnd, text })   // ← 原句就此消失
```

`text` 是这条笔记里**唯一**记着「原本划的是哪句话」的地方。范围一缩它就被改写，
原始记录从数据里**永久消失**。库里实测：

| 操作 | 存的 text |
|---|---|
| 原始 | `Is to wish my life away` |
| 从正文删掉 `away` | `Is to wish my life` ← 原句没了 |
| 把 `away` 加回去 | `Is to wish my life` ← 救不回来了 |

### 两个症状分别怎么来的

**不弹窗** —— 判定「这条没了」的门槛是**剩不到两个词**（一个词不成句）。
删一个词还剩五个，不算没了，所以不问。这是当初刻意的设计
（怕「删掉第一个词就整条报废，可其余部分明明还好好的」），
但它漏想了「用户得知道这条笔记被改小了」。

**恢复不了** —— 救援那一遍只处理**位置为空**的孤儿。缩过的句摘位置还有效，
压根进不了救援；就算硬让它进，`text` 已是缩短版，拿去找也只找得回缩短版。

**单词为什么没这毛病**：单词的 `text` 从来不被改写。范围类（句摘、短语）
是唯一会自我改写的一类。

**一个会误导人的现象**：删**中间**的词能自己好，删**头尾**的就好不了。
删中间时范围两头没动，词加回来正好落在范围**里**，被重新框进来 —— 纯属侥幸；
删头尾时范围往里收了，词加回来落在范围**外**，永远框不回来。

### 改法：加一格 `sourceText`，原句永不覆盖

`Annotation` 新增一个可选字段 `sourceText`：

- `text` 继续**跟着正文走**（卡片上显示的永远是正文现在的样子，
  所有显示和 AI 的地方一行都不用改）
- `sourceText` 记**当初划的那一句**，`a.sourceText ?? a.text` 保证**只写第一次** ——
  连缩两次记的仍是最初那句，不是上一次缩完的样子
- 和正文一致时**这个字段不存在**，它同时就是「正文已改」记号的判断依据

配三件事：

1. **自愈**：对账时，带 `sourceText` 的范围先按原句在正文里找一遍，
   找到就整条还原、记号清掉。**必须放在收缩逻辑前面** ——
   走完收缩范围两头就按缩短后的位置钉死了，加回边缘的词落在范围外，
   永远框不回来（这正是从前删句首/句尾之后再打回去也恢复不了的原因）
2. **孤儿救援优先按原句找**：一条句摘可能先被改短、后来整条没了，
   这时 `text` 是缩短版、`sourceText` 才是原句。按原句救回就清记号，
   按缩短版救回就继续留着记号
3. **卡片上加「正文已改」记号**（[EditedMark.tsx](src/components/EditedMark.tsx)），
   展开能看到「原句：…」。样式跟着「原文已删除」那枚走 ——
   两者是同一类信息，差别只在程度

### 用户拍板：不弹窗

有了「原句不丢 + 能自愈 + 卡片上有记号」这三样，弹窗的价值只剩打断。
所以缩短时**不弹**，只有整条都没了才照旧弹「原文已删除」。

### 文库复习那边故意不显示

文库复习是按拼写把同一个词/短语**合并成一条**的，同一条底下可能压着三篇文档
里的三处标注，改过的也许只有其中一处。挂个记号再显示「原句：xxx」，
用户没法知道说的是哪一处，只会更糊涂。想看是哪处改了，进单篇复习。

### 实测（浏览器里走真实流程，375px）

先把库清空重来，从干净的示例数据开始：

| 步骤 | text | sourceText |
|---|---|---|
| 初始 | `Is to wish my life away` | 无 |
| ① 删句尾 `away` | `Is to wish my life` | `Is to wish my life away` ✅ |
| ② 把 `away` 加回去 | `Is to wish my life away` ✅ | 无（记号清掉）✅ |
| ③ 删句首 `Is` | `to wish my life away` | `Is to wish my life away` ✅ |
| ④ 把 `Is` 加回去 | `Is to wish my life away` ✅ | 无 ✅ |

③④ 正是从前的死路。

界面（375px 窄屏）：

- 右侧生词板句摘卡：收起 89.8px 高，显示「Is to wish my life / 正文已改 / 释义」；
  展开 145.3px，多出「原句：Is to wish my life away」。卡片 243px，零横向溢出
- 复习页句摘卡同样有记号和原句；遮着答案时不显示（否则等于剧透）
- 长按记号有完整提示语：「正文改过了，这条笔记跟着变了。你当初划的是：…」

单测加了 8 条（334 项全过），把上面四步、连缩两次、缩完再整条删掉再回来、
以及「没动过不该平白多出记号」「整体位移不算改过」都钉住了。

### 抓到一个当场的坑

第一版把「原句：…」写成了 `<p>`，而复习页的句摘卡本身就把英文包在一个 `<p>` 里 ——
`<p>` 套 `<p>` 是非法嵌套，浏览器会擅自把外层那个提前闭掉，版式会莫名散架。
控制台的 `validateDOMNesting` 警告当场抓到，改成 `span` + `block`。
**单测看不出这种问题，只有在浏览器里才现形** —— 这是本项目第四个这类坑。

### 一件救不回来的事

**这次修复之前就已经被改坏的句摘，救不回来** ——
原句在当时就没有被存下来，数据里已经没有可还原的依据。
从这版起新发生的都不会再坏。

### 留个教训

「有意的设计」和「说得过去的行为」不是一回事。
范围收缩本身是对的（删一个词不该整条报废），错的是**收缩时把唯一的原始记录
覆盖掉了**，于是这个决定变得不可撤销。凡是要覆盖用户原始输入的地方，
先问一句：**覆盖完还能不能回去？**

---

## 二十二、~~魔杖加上文字~~ ✅ 2026-08-30 已验收

分支 `wand-label`。聊「要不要做新手引导」时顺手查出来的一个实打实的问题。

### 先查现状：这个 App 的引导比想象中多

动手加引导之前先数了一遍已经有的，免得把做过的又做一遍：

- **阅读页顶栏那行字是会变的**（[useWordInteraction.ts:116](src/hooks/useWordInteraction.ts:116)）——
  什么都没选时说「阅读模式 · 长按单词添加笔记」，选中一个词之后变成
  「轻点其它单词可连成句子」。两个最难发现的手势正好都被它盖住了，
  而且是**在你正需要的那一刻说那一句话**，这是最好的一种引导
- 右侧生词板、复习页、左侧文库、划词没结果，四处空状态都写了引导话

**结论：核心回路（长按取词 → 连成句子 → 生词板 → 复习）已经引导得不错，
缺的是进阶功能那一层。**

### 真正的缺口，以及一条判断

没人说话的地方：一键划词（魔杖）、文库复习和单篇复习的区别、
「整理」按钮、复习页那支笔里才有的拖拽手柄。

但其中**一半根本不是缺引导，是界面没说清楚**。于是定了一条：

> **能用标签解决的，就别用引导解决。** 引导是补丁，标签是修好。

魔杖属于「标签就能解决」那一类，所以这次只改它。

### 改了什么

生词板标题行右边那枚 `Wand2` 从纯图标改成**图标 + 「AI 划词」**。

写「AI 划词」而不是「划词」：正文里长按取词本来就是在划词，
光写「划词」分不出这颗按钮特别在哪 ——「AI」两个字才是它的卖点。

（和「填充」按钮的写法略有出入，那边只有两个字。要不要一并改成
「AI 填充」求对称，留给用户定，没有擅自动。）

### 量出来的（先量再改，不是改完再看）

| | 改之前 | 改之后 |
|---|---|---|
| 按钮宽 | 28px | 79px |
| 标题行剩余空间（260px 窄屏） | 130px | 79px |
| 按钮高 / 是否换行 | 28px | 30px / 没换行 |
| 侧栏横向溢出 | 0 | 0 |

宽屏（侧栏 350px、还多一枚关闭键）：右侧按钮组 109px，仍剩 139px。
按钮左右边界 283→363，正好贴着侧栏 12px 的内边距，完全在框内。

### 用户否掉的两条（都否得对）

一并提过的另外两条，用户都拒了，理由都成立：

- **复习页的「英 / 中」不改** —— 用户觉得那两个字已经够直观
- **导入按钮不写成「导入 txt / epub」** —— 那一格量过只有 **82.3px**
  （第十九节量的），写四个字加两个格式名必撑破。「导入」两个字正好

第二条是提建议的人自己前两天量过的数字，提的时候却忘了 ——
**量过的数字要用起来，不是量完就算。**

### 没做的：首次启动轮播

理由不是「以后加功能会过期」，是**收益本来就低** ——
人在手上还没有内容的时候记不住手势，而阅读页那行会变的提示
已经在正确的时刻教正确的事了。

### 引导这件事以后怎么办

不做成一个会腐烂的一次性项目，做成加功能时的固定动作：

> **新功能要么自己会说话（按钮上有字），要么在「使用说明」里补一段。**

这样就不存在「等功能稳定了再做引导」——那永远等不到。
设置页里的「使用说明」还没做，文案可以从功能介绍那份现成的搬。

---

## 二十三、~~设置页里加「使用说明」~~ ✅ 2026-08-30 已验收

分支 `user-guide`。第二十二节定的两层里的第二层。

### 只写界面自己说不清楚的

**核心手势一条都没写。** 阅读页顶栏那行会变的字已经在你正需要的那一刻讲了
（没选中说「长按单词添加笔记」，选中之后变成「轻点其它单词可连成句子」），
比翻说明书管用得多。把它再抄一遍进说明书，只会让说明书变长、没人看。

收进来的是另外三类：

- **藏在模式后面的** —— 整理模式、复习页那支笔里才有的拖拽手柄
- **两条路长得像但结果不同的** —— 点文档 vs 点文库的复习
- **没法靠按钮讲明白的** —— 「我改了正文，笔记会怎样」

一共十二条。

### 三个刻意的决定

**一、一张截图都不放，只用文字。**
说明书跟着功能走，不跟着长相走。以后挪按钮、做深色模式，都不用回头改这里。

**二、默认全部收起，先给一张目录。**
十二条全摊开就又是一篇要滚半天的长文了。你不知道哪条点哪条 ——
和生词卡、句摘卡「点一下展开」是同一套做法。

**三、同一时间只展开一条。**
理由同上：允许多条同时开着，滚动条马上就失控。

### 放在哪

设置页新增一组「上手」，**排在「阅读外观」前面** ——
不知道怎么用的人第一眼就该看见它，而不是先看到字号调节。

进去是设置页的第三个子屏（前两个是 AI 设置和用户协议），
返回键和右上角的 X 都退回设置列表，再按一次才关掉整个设置页。

### 实测（375px 窄屏）

- 设置列表首屏就能看到「上手 / 使用说明」
- 说明页十二条目录全在，卡片 343 × 663，零横向溢出
- 展开最长的那条（「改了正文，笔记会怎么样」）：正文 305px 宽、207px 高，不溢出
- 展开第二条时第一条自动收起，确实只开一条
- 说明页点 X → 回到「设置」；再点 X → 整个关掉
- 控制台零报错

### 引导这件事的收尾

第二十二节定的两层做完了：

1. ~~把哑谜按钮改成会说话的~~ ✅ 魔杖（第二十二节）
2. ~~设置页加「使用说明」~~ ✅ 本节
3. 首次启动轮播 —— **不做**，理由见第二十二节

以后加功能时按那条规矩办：**要么按钮上有字，要么来这儿补一条。**

---

## 二十四、~~「填充」改成「AI 填充」~~ ✅ 2026-08-30 已验收

分支 `ai-fill-label`。第二十二节留给用户拍板的那条对称问题，用户定了：加上。

第二十二节给生词板那颗魔杖加了「AI 划词」，当时特意没顺手改这颗 ——
它和「填充」不在一个界面上，属于另一处的取舍，该由用户决定。
现在两颗凑成一对：**AI 划词**（阅读页 · 生词板）和 **AI 填充**（复习页）。
两个 AI 功能一眼看得出是同一类，而不是一个像功能、一个像动作。

顺手把 title 从「用 AI 补全」改成「让 AI 只补空着的格子」——
「用 AI」这三个字按钮上已经写着了，提示语的位置留给更有用的信息。

### 量出来的（375px 窄屏，含带数字的各种情况）

这一排是 `flex-wrap` 的，加字有挤到第二行的风险，所以逐档量过。
数字不是算的 —— 是把按钮文字临时换成各档内容之后当场量的：

| 按钮内容 | 按钮宽 | 左右两组之间还剩 | 工具栏高 |
|---|---|---|---|
| （改之前）填充 | 62px | 123px | 57px |
| AI 填充 | 79px | 105px | 57px |
| AI 填充 9 | 92px | 93px | 57px |
| AI 填充 99 | 101px | 84px | 57px |
| AI 填充 999 | 110px | 75px | 57px |

**工具栏高恒为 57px**（`py-3` 24 + 按钮 30 + 边框），也就是自始至终单行，
横向溢出全程 0。三位数是不可能出现的极端，留着当安全边界看。

顺带一个巧合：「AI 填充」和「AI 划词」量出来都是 **79px**，
两颗按钮不在同一屏，但宽度正好一样。

### 留个教训

这次没犯上次那个错。上一轮提「导入按钮改成『导入 txt / epub』」时，
那一格的宽度（82.3px）**是自己两天前量的**，提建议时却没想起来，
被用户当场否掉。这回是先量再提、再量再改。

**量过的数字要用起来，不是量完就算。**

---

## 二十五、~~单词改用词典的真人录音~~ ✅ 2026-09-01 已验收

这是「有声阅读」音质那条线的第一步，只做最小版：单词卡点一下，
放**词典里人录好的**发音，取不到才退回原来的机器音。界面一个像素没动。

**用户真机验收：音质满意，开口慢的那半秒可以接受。**

### 为什么不是「换个好嗓子」就完事

先走的是最省的路 —— 换系统语音引擎，不用改一行代码。走不通：

这台手机的朗读引擎只有厂商自带的 `AiService TTS` 一个，
**英语底下没有第二个嗓子可选**，应用商店里也搜不到 Google 语音服务。
这类引擎中文做得好，英文往往只塞一个凑合的嗓子，而且换不掉。

**所以问题不在代码里，在这台手机上** —— 再怎么改，系统合成也只有这一个嗓子。
要好听只能不走合成：单词直接放现成的真人录音。

### 覆盖面（拿真实请求一个个试出来的，不是猜的）

| 试的 | 有没有 |
|---|---|
| 普通词 `stumble` | ✅ |
| 变形词 `stumbling` / `houses` / `ran` | ✅ |
| 生僻词 `obstreperous` / `perspicacious` | ✅ |
| 短语 `give up` / `look forward to` / `in the dark` | ✅ |
| `COVID-19` / `1990s` | ✅ |
| 整句 `He stumbled over a stone.` | ❌ |
| 生造词 `zzqwmbl` | ❌ |

**硬边界：词典只收词条，整句一律没有。** 所以句摘卡换不了，那边继续用系统朗读。
复习页会变成「点词是真人、点句是机器音」这种不齐整的状态 —— 用户知情。

**短语其实也有录音**，这次没接（最小版只动单词卡），下一步顺手就能开。

### 改了哪儿

界面和 `useSpeak` 一个字没动，全在朗读层里加了一层：

- `speech/dictAudio.ts` —— 拼地址、用一个 audio 元素放。
  **为什么不用 fetch**：这个地址不带跨域许可，fetch 会被浏览器拦下来，
  而 audio 标签放外站的声音不受这条限制。（将来要把录音存下来离线用，
  就得走 Capacitor 的原生网络绕开跨域，那是另一件事。）
- `speech/humanFirst.ts` —— 「先真人，不行再机器」的那层壳。
  播放器是当参数传进来的，所以这层逻辑能单测，不需要浏览器
- `speech/index.ts` —— 原来的系统朗读（原生 / 网页版）外面套上这层壳

### 用户拍板的两件事

- **美音**（`type=2`）。英音的地址也留着（`type=1`），以后想加开关很容易
- **取不到就悄悄退回机器音**，不弹提示、不做记号。反正机器音是从前的水平，
  退回去不算变差；而且实测下来真词几乎都有录音，这条路平时用不上

### 三个绕不开的坑

1. **按停之后不能补念。** 取录音要等，等的过程中用户可能已经按停了。
   要是这时候「没有录音」的消息才回来，就会把用户刚按停的词用机器音再念一遍。
   靠「叫停算正常结束」也能对，但那是指望另一个文件不改脾气 ——
   所以 `cancel()` 里也把号往前走一格，谁改都漏不出去。
   **这条是单测先炸出来的**，写测试的时候顺手试了一把脏路径
2. **等录音的空当里点了别的词**，前一个不许插进来念。同样靠对号
3. **换词时系统那边也要停。** 从前每次 `speak` 自己会先停一下，
   但现在这一次可能走的是放录音、根本不经过 `speak` ——
   不主动停，上一句就会跟录音一起响

### 实测（浏览器里走真实流程）

| 验的东西 | 结果 |
|---|---|
| 点 `stood` | 卡片亮起，取了 1 次录音，**系统朗读一次都没被调用** |
| 再点 `stood` | 熄灭、停下，不重新取 |
| 快点 `stumble` 再点 `learned` | 只有 `learned` 亮着，前一个没插进来 |
| 生造词 `zzqwmbl` | 219ms 后退回机器音 |
| 整句 | 0ms 直接机器音，压根不查词典 |
| 控制台 | 干净 |

真词开口约 **0.6 秒**（要联网取），取不到的约 **0.2 秒**后落回机器音。
比从前一点就响慢一拍，这是这条路的代价。

测试 334 → **348 项全过**；`tsc -b --force`、`npm run build` 都通过。

### 还没做的

- **缓存** ← **下一步就做这个**。现在每点一次都联网取一次，没网就只有机器音。
  录音一个约 10KB，读过一次存下来，一千个词也才 10MB。
  存下来之后连那半秒的等待也一起省了
- **短语卡**（词典里有，只是这次没接）
- **英音/美音开关**
- 系统朗读整个不可用的手机，现在还是连喇叭都不画。
  其实光有录音也能读单词了，这条以后可以放宽

### 一条要记着的风险

这是有道词典网页自己在用的地址，公开、不要 Key，但**没有官方承诺**，
哪天变了得有退路 —— 退路已经有了（取不到就落回系统朗读），
真变了顶多是退回从前的机器音，不会按了没声音。

我们只在用户点的时候取一次，不是拿它当词典刷。

### 留个教训

**先查外部服务到底给不给，再谈怎么做。** 这次动手前拿真实请求把
单词 / 变形 / 生僻 / 短语 / 整句挨个试了一遍，才知道「整句没有」这条硬边界 ——
要是先写完代码再发现，句摘卡那半边就白做了。

---

## 二十六、~~发音存下来 + 短语也用真人~~ ✅ 2026-09-01 已验收

**分支 `audio-cache-and-phrases`，还没并。** 第二十五节留的两条尾巴一起做了 ——
分开做等于把同一处改两遍。

### 缓存：单开一个库，绝不能塞进主数据

这个 App 是把全部笔记当**一整块**存的，改一条就整块重写。
录音混进去，以后每存一条笔记都要驮着几十兆音频走一遍，
导出的备份文件也会被撑爆。所以 `audioCache.ts` 另开一个库
（`localforage.createInstance`），跟正文数据完全分开 ——
录音是「丢了还能再下」的东西，跟笔记不是一回事。

顺带记一本「存了哪些、各多大」的小账（`__index`）。
不然设置页想显示「占多少」就得把所有录音读出来量一遍，每开一次卡一下。

### 取字节这件事，两边走的不是一条路

播放不受跨域限制（audio 标签放外站的声音一直是允许的），但**取字节是**。
浏览器里 fetch 那个地址直接报 `Failed to fetch`（当场试出来的）。所以：

- **手机上**走 Capacitor 的原生网络（`CapacitorHttp`），请求由安卓发出，没有跨域这回事
- **电脑浏览器里**走 vite 的开发转发（`vite.config.ts` 里那条 `/dictvoice`）。
  **纯粹是为了能在浏览器里把缓存这段验掉** —— 这个项目栽过的跟头全是「只在一边验过」。
  打包出去的 App 不经过它

取字节要是彻底不通，还留着「直接流着播」这条老路兜底：听得见，只是这次存不下来。

### 短语：不用猜，让界面自己说

从前是在朗读层里数空格判断「这是词还是句」。短语接进来以后这条就不成立了 ——
`give up` 有空格，但词典里有录音。

改成调用处传一个 `lookup`：**词卡和短语卡传 true，句摘卡不传**。
界面本来就知道点的是哪种卡片（这两种卡在代码里本来就是两个不同的调用处），
没必要在下面猜。

### 预取（用户拍板：打开就预取）

打开复习页就把这一页的词悄悄取回来存好，省掉每个词**第一次**点时等开口那半秒。
三条规矩：已经存过的跳过、一个一个来（不甩几十个请求出去抢网）、失败不吭声。

**加了 200 个的上限**：单篇一页也就二三十个词，可「文库复习」是把所有文档并在一起的，
动辄上千 —— 一开就闷头下几十兆不像话。剩下的点到了现取。

### 设置页加了一组「发音」（用户拍板：要带占用）

一行「已存 N 个发音，占 X」加一个「清空」，底下一句话说明清空只删录音、不动笔记。
没存东西时按钮自动变灰。

### 一个绕不开的坑（和第二十五节同一类）

取录音要等（查库、联网），**这中间用户完全可能已经点了别的词或者按了停**。
等回来不对一下号就直接播，放出来的是上一个词，还会跟新的撞在一起。
所以播放器里也发号（`generation`），`cancel()` 也把号往前走一格。
外面那层壳（`humanFirst`）已经有一套号了，但那层管的是「要不要退回机器音」，
拦不住播放器自己串音 —— **两层各管各的，都得有**。

### 实测（浏览器里走真实流程）

| 验的东西 | 结果 |
|---|---|
| 打开复习页 | 4 个词全部预取存好，共 37KB |
| 点已存过的词 | **一个新请求都没有**，250ms 内卡片就亮了 |
| 开口要多久 | 联网取 **223ms** → 存下来之后 **7～9ms**，基本等于秒响 |
| 再打开同一页 | 存过的跳过，只取新增的那个 |
| 短语卡 `give up` | 预取取到了，点了走录音，**系统朗读一次都没被调用** |
| 连点两个词 | 只响后一个，前一个没串出来 |
| 设置页（375px） | 行宽 291px 装在 343px 的面板里，面板和页面都不横滚 |
| 清空 | 库清零，按钮自动变灰，那行变成「还没存下任何发音」 |
| 控制台 | 没有新错误 |

测试 351 → **359 项全过**；`tsc -b --force`、`npm run build` 都通过。

短语卡样本数据里没有，是往库里塞了一条（照现有标注复制，保证结构合法）验完再删掉的。

### 手机上才知道的那一段

**取字节走的是原生网络这条路，浏览器里验不到** —— 开发转发和 `CapacitorHttp`
是两码事。装了 APK 才知道通不通。真不通也不会哑：会落回「直接流着播」，
表现就是回到第二十五节的样子（能听，但存不下来、每次都要等）。

### 还没做的

- **英音/美音开关**（地址里 `type=1/2`，代码早就留好了）
- 缓存**没有上限也不会自动清**。用户选的是「自己管」—— 设置页看得到占多少、能清空。
  真涨到难看的地步再说
- 系统朗读整个不可用的手机，现在还是连喇叭都不画。
  其实光有录音也能读单词和短语了，这条以后可以放宽

---

## 二十七、~~弯撇号丢录音 + 冠词 a 读成字母名~~ ✅ 2026-09-01 已验收

**和第二十六节同一条分支 `audio-cache-and-phrases`，一起验。**
起因是用户报的一个具体现象：`with a thud` 里的 `a` 被读成了字母 A。
查这件事的时候顺手撞到一个更值钱的 bug。

### 一、弯撇号让录音白白丢了（真 bug，用户没报，是查出来的）

| 写法 | 词典 |
|---|---|
| `don't` `it's` `can't` `I'd` `wouldn't have` | ✅ 全有录音 |
| `don’t` `it’s` `can’t` `I’d` `wouldn’t have` | ❌ **全部查不到** |

差别只有一个字符：直撇号 `'` 和弯撇号 `’`。**导入的电子书里基本全是弯的。**

后果：一整类**本来有真人录音**的缩写词和短语，一直在白白落回机器音。

**这个项目别处早就在统一两种撇号了** —— 分词（`tokenize`）、对账（`reconcile`）、
划词定位（`locate.ts` 里那个 `normalize`）都特意处理过，唯独新写的查词典这段漏了。
改法就是查之前先换：`normalizeWord()`，地址和缓存键都过它。

**缓存键也必须过**，否则 `don't` 和 `don’t` 会各存一份同样的录音。

### 二、冠词 a 被读成字母名

**根因不在我们这儿**：这台手机的引擎在「把文字变成读音」这一步偷懒，
夹在词中间的单字母 `a` 被它认成了字母名。好引擎知道那是冠词、该读成一带而过的轻音。

引擎换不掉（第二十五节查过：这台手机只有厂商自带的一个，英语底下连第二个嗓子都没有），
所以只能在送出去之前把字改掉绕过它 —— **卡片上显示的文字，和发给引擎的文字，
本来就不必是同一份**。

`pronounceFix.ts` 是一张补丁表，现在只有一条：`a` → `uh`
（`uh` 的音 /ʌ/ 跟冠词该有的轻音几乎一样，而且是个正常单词，引擎不会往字母上想）。

两条边界：

- **只在多个词的时候改**。单独标了一个 `a` 当生词时，读字母音才是对的
- **只在退回机器音这条路上改**。走真人录音时压根不经过

**做成一张表**是因为这类毛病只能一个个撞出来 —— 引擎哪里读错了，得有人听见才知道。
以后再发现，加一行就行。

### 顺带查清楚的覆盖情况

`with a thud` 是运气不好，不是普遍现象：

| 试的 | 有没有 |
|---|---|
| `with a smile` / `with a bang` | ✅ |
| `a lot of` / `as a result` / `in a hurry` / `have a look` | ✅ |
| `in the dark` / `on the other hand` / `all the time` | ✅ |
| `Dr.` / `e.g.` / `3rd` / `1990s` / `COVID-19` | ✅ |
| **`with a thud`** | ❌ 就它没有 |

所以机器音只在**偶尔漏一个**的时候才轮得上。

### 一条目前没有便宜解法的（记着，别当成没有）

**同形异音词，真人录音也救不了。** `read` `lead` `live` `wind` `tear`
`close` `record` `present` `bass` —— 词典一个拼写只有一条录音，
那它就只可能给一个读音，这是结构上必然的。

你标的是 `I read it yesterday` 里的 `read`（/red/），词典多半给的是 /riːd/。
**比冠词那个更骗人**：听起来自然、发音标准，就是读错了词。

要治得知道这个词在句子里是什么词性 —— 那是 AI 的活，不是词典的活。

### 实测（浏览器里走真实流程）

| 验的东西 | 结果 |
|---|---|
| `don’t`（弯撇号） | 查到录音了，**不再落回机器音** |
| `don't`（直撇号） | 同上 |
| 两种写法 | 拼出同一个地址，**只存一份**（库里 count 仍是 1） |
| `with a thud` | 退回机器音，引擎收到的是 `with uh thud` |
| 走真人录音时 | 一个字都没改 |
| 复习页 | 照旧预取、点词零请求、不惊动系统朗读 |
| 控制台 | 只多一条 500 —— 那正是「这个词没录音」的信号，按设计该有 |

测试 359 → **372 项全过**；`tsc -b --force`、`npm run build` 都通过。

### 配了一份试音单

`试音单.txt`，十句，把上面提到的几类都塞进去了（冠词、同形异音词、数字、缩写、
连字符、弯撇号）。导进 App 划成句摘和生词，挨个听一遍，**哪些真坏了再补** ——
补丁表加一行就是了。不这么做的话就是凭印象补一堆用不上的。

### 留个教训

**别处已经统一过的东西，新写的代码也得统一。** 撇号这件事这个项目处理过三遍
（分词、对账、划词），第四处却漏了 —— 因为那三处都是「比对文字」，
这一处是「拼一个网址」，看着不像同一类问题。
**判断标准应该是「这段文字从哪来」，而不是「拿它去干什么」** ——
只要来自正文，两种撇号就都可能出现。

---

## 二十八、~~AI 把 fetters 填成名词~~ ✅ 2026-09-01 已验收

**分支 `prompt-pos-from-context`。** 用户报的：`what fetters my fate` 里的
`fetters` 是动词「束缚」，却被填成了名词「羁绊」，词性也标了 `n.`。

### 先排除一个猜测：上下文其实是传了的

代码把这个词**所在的那一行原文**一起发给了 AI（`useAutoFill.ts` 里按
`anchorId` 找到行号再取整行）。释义那条要求也早就写着「必须结合上下文选择该处
真正的含义」，音标那条写着「按该词在上下文中的实际读音标注」。

**所以不是「AI 看不到句子」。** 毛病在提示词自己身上。

### 根因一：词性那条，一个字没提上下文

从前整条就是：

```
词性缩写，用中文习惯写法，例如 n. / v. / adj. / adv. / prep. / conj.
```

没告诉模型词性该从句子里判断，它就按**词典词条的主词性**填。
`fetter` 作名词比作动词常见，于是 `n.`。

释义那格有上下文要求、词性那格没有，**两格于是各说各的** —— 这本身就是个隐患，
就算这次没撞上 fetters，迟早也会出现「词性说 n.、释义写的却是动词义」。

### 根因二（更要命）：变形清单里漏了第三人称单数

释义那条要求「不是原形就补上原形和变形类型」，列的是：

> 过去式、过去分词、现在分词、**复数**、比较级

**没有第三人称单数。** 而 `fetters` 正好是动词加 `-s`。
清单里唯一能对上 `-s` 的就是「复数」，而且例子还给了
`children -> 孩子们（child 复数）`。

**模型不是随便猜的，是照着我们写的规则推的 —— 规则漏了一种情况，
而且漏的那一种正好被另一条规则顶替了。**

### 根因三：例子全是同词性换义项

`stood`：站立 / 仍然有效 —— 两个都是动词。
**最难的那种情况（词性本身变了）一个例子都没有。**

### 改了什么（全在 `fieldSpecs.ts`，两边共用）

- **词性**：加「必须是这个词在所给这句话里实际充当的词性，不是词典里最常见的那个」，
  配三个同拼写不同词性的例子（fetters / lead / present）
- **变形清单**：补上第三人称单数、最高级；加一条专门的提醒
  「**词尾的 -s 不一定是复数**，动词第三人称单数也是 -s，是哪一种由句子说了算」
- **两格必须一致**：「释义必须和 pos 那一格说的词性一致，先按句子定下词性，
  再在那个词性底下选义项」
- **原形**：按句子里的词性还原
- **填充的系统提示**：加「先读 context，判断这个词在那句话里充当什么成分，再动笔」

改的是 `fieldSpecs.ts`，**「AI 填充」和「AI 划词」共用这一份**
（当初特意收在一起就是防止两边走样），所以一次改两边都好了。

### 加了测试钉住

`fieldSpecs.test.ts`：词性那条必须提到「所给这句话」、变形清单必须含
「第三人称单数」、释义必须要求和词性一致、两边提示词都得用上 POS_SPEC。

**从前 `prompt.test.ts` 只钉了释义和原形两边一致，词性漏在外面** —— 这次补上了。
这类错不报错、不崩，只安静地填错一格，只能靠测试守。

测试 372 → **381 项全过**；`tsc -b --force` 通过。

### ⚠️ 这一条我验不了

**手上没有 API Key，改完的提示词跑不了真实请求。**
而且提示词是概率性的 —— 改好了是「大多数时候对」，不是「保证对」，
不像代码非黑即白。只能靠用户在手机上拿 `fetters` 那句复验。

**重测要先清空**：填充只填空白、绝不覆盖，所以得先进编辑模式把那张卡的
释义和词性删掉（或者删掉整条重新标），再点 AI 填充。

### 和第二十七节那条是同一类

第二十七节末尾记过「同形异音词，真人录音也救不了 —— 词典一个拼写只有一条录音」。
这次是同一个根：**一个拼写对应多种可能，光看词认不出来，必须看句子。**
录音那边没有便宜解法（词典就一条），填充这边有 —— 因为 AI 本来就看得到句子，
只是我们没要求它看。

---

## 二十九、~~生词板点词：不收面板、读出来~~ ✅ 2026-09-01 已验收

**分支 `sidebar-speak-keep-open`。** 用户要的：点生词板里的词，**保持跳转、
不收起面板、同时读出发音**，跟复习页点卡片一个效果。

### 改了什么

- `handleScrollToWord` 去掉 `setActivePanel(null)` —— 就这一行
- 生词卡的词按钮：跳转之外再调一次朗读，`lookup: true`（词和短语都查词典）
- 正在读的那个词亮起来（`text-amber-500`），和复习页同一套反馈
- 顺带把预取也挂上：面板里的词提前备好，省掉第一次点等开口那半秒。
  **和复习页共用同一个仓库**，那边存过的这里直接就有

### 手机上会被挡住 —— 量出来的，不是猜的

面板在**宽屏是并排（`relative`）、手机上是盖在正文上（`fixed`）**。
375px 屏上量：面板占掉右边 260px（x 从 115 到 375），**正文只剩最左边 115px**，
大约一行的前三分之一。

所以手机上跳过去的那一行多半看不见。**滚动位置是实打实变了的**
（量过：点词前 scrollTop 0，点后 2356），关掉面板就在那儿。

按用户原话做的：他要的是「一个个点过去听发音」，那时候面板才是主角，
正文看不看得见是次要的。真觉得别扭，手机上改回收起就是一行的事。

### 没动的地方

- **句摘的「编辑」照旧收起面板** —— 那个动作是要在正文里改东西，
  面板挡着就没法改，跟点词不是一回事。验过了，没被误伤
- 句摘卡没有加朗读（用户要的是单词/短语）
- 面板里没加朗读失败的黄条（复习页有）。读不出声的手机 `canSpeak` 就是 false，
  那就只跳转不发声；真出错就是静默略过

### 顺手抓到一个真 bug：把网页当录音存了

验的时候发现 `/dictvoice` 返回的是 **App 自己的首页 HTML**
（1761 字节、`text/html`、状态码 **200**）—— 开发转发那会儿掉了，
vite 把它当成前端路由走了兜底。

**而我的代码照单全收**：`fetchAudioBytes` 只看 `res.ok`，200 就存。
于是四个词各存了一份 1761 字节的网页，缓存里从此躺着四个永久的哑巴。

**这跟开发转发通不通没关系，真机上一样撞得上** —— 网络劫持、公共 WiFi 的
登录页、运营商插页，全都是 200 的 HTML。

改法：只认 mp3 自己的开头（`ID3` 标签，或 MPEG 帧同步 `0xFF` 后三位全 1），
认不出来就当没有这个录音，宁可退回机器音也不往缓存里放。

**留个教训：状态码 200 不等于拿到了想要的东西。** 存进缓存的东西尤其要验 ——
存错了不会当场报错，只会以后每次都错，而且缓存不过期，自己不会好。

### 实测（浏览器里走真实流程）

| 验的东西 | 结果 |
|---|---|
| 点生词板里的词 | 面板**留着没收**，滚动位置 0 → 2356（跳了） |
| 发音 | 走真人录音，**系统朗读一次都没被调用** |
| 正在读的词 | 亮起（`text-amber-500`） |
| 改点另一个 | 只有后一个亮着 |
| 再点正在读的 | 停下，熄灭 |
| 阅读页打开就预取 | 4 个词共 37140 字节，与复习页那边一致 |
| 句摘的「编辑」 | **照旧收起面板**，没被误伤 |
| 375px 窄屏 | 词按钮右边缘 176～198px，都在 260px 的面板里；侧栏和页面都不横滚 |
| 转发坏掉时 | 拿回来的网页被挡下，不进缓存 |

测试 381 → **385 项全过**；`tsc -b --force` 通过。

---

## 三十、~~句摘卡的箭头改成「只跳转」~~ ✅ 2026-09-01 已验收

**分支 `sidebar-sentence-jump-only`。** 接着第二十九节：生词卡点词已经改成
「跳转但不收面板」，句摘卡右边那条箭头还是老样子 —— 跳转、收起面板、
再弹出底部抽屉。用户要它也只跳。

### 改了什么

`handleEditSentence` 原本干三件事，现在只留第一件：

| | 从前 | 现在 |
|---|---|---|
| 跳到正文位置 | ✅ | ✅ |
| 收起右侧栏 | ✅ | ❌ |
| 弹出底部抽屉改语法/翻译 | ✅ | ❌ |

顺手把名字改诚实了：`handleEditSentence` → `handleScrollToSentence`，
`onEditSentence` → `onScrollToSentence`，卡片上的 `onEdit` → `onJump`。
它已经不编辑任何东西了，名字还叫 edit 迟早骗到人。

箭头本来是个光秃秃的 `ChevronRight`，既没有 title 也没有无障碍标签 ——
既然改了含义，顺手补上「跳到这句话在文中的位置」。

### 连带清掉一条没人走的路（净删约 30 行）

抽屉不弹了，`pendingSentenceEdit` 就再没有人往里写值。留着就是死代码，
所以一并删掉：App 里的那个 state、滚动完成后「延迟 500ms 清空」的那段、
传给 LyricEditor 的 prop，以及 LyricEditor 里那条响应它的 effect。

### 编辑句摘并没有丢，两条路都还在

动手前特意查过 —— 不然就是拿掉了唯一的入口：

1. **复习页的句摘卡**，编辑模式（右上角那支笔）里语法和翻译两格都能改
2. **在正文里重新划这句话**：`openRange` 会按起止位置找出已存的那条句摘，
   **把语法和翻译填回表单**再弹抽屉。所以在正文里就地改这条路是完好的

删掉的只是「从侧栏点进来」这一条捷径。

### 实测（浏览器里走真实流程）

| 验的东西 | 结果 |
|---|---|
| 点箭头 | 要求滚到 `L28W0`（这句话的起点），位置 0 → 2005 |
| 面板 | **留着没收** |
| 底部抽屉 | **没弹**（点前 0 个，点后 0 个） |
| 箭头的提示与无障碍标签 | 「跳到这句话在文中的位置」 |
| 划新句子那条路 | `openRange` 未动，仍然弹抽屉、仍然回填已存内容 |

测试 **385 项全过**；`tsc -b --force`、`npm run build` 都通过。

### 一个只在测试环境里出现的岔子，记一笔免得下次白查

第一次量的时候滚动位置纹丝不动（0 → 0），差点以为是改坏了。
真相是：**浏览器面板被藏起来时，平滑滚动和 CSS 过渡都不推进** ——
帧循环停了。第二十九节量面板位置时也撞过同一件事（量到的是动画起点）。

判断办法：把 `scrollIntoView` 临时换成即时滚动，或者直接记录「有没有被要求滚」。
**别一看数字没变就去翻代码。**

---

## 三十一、~~长按取词时读出来~~ ✅ 2026-09-01 已验收

**分支 `longpress-speak-word`。** 阅读模式下长按一个词划生词时，顺带把它读出来。

### 界线：只有「第一次长按进入单词模式」那一下读

`openWordDrawer` 里有四种情况，只有第 1 种读：

| 情况 | 读不读 |
|---|---|
| 1. 没有选区，第一次长按 → 单词模式 | ✅ **读** |
| 2a. 再长按同一个词 → 取消选中 | ❌ |
| 2b. 长按另一个词 → 升级成短语/句子 | ❌ |
| 3. 已是范围，长按改终点 | ❌ |
| （轻点连词成句 / 改范围 / 缩回单个词） | ❌ |

用户明确要的就是这条线：**接着点第二个词是在连成短语或句子，那时候读一个词
没有意义，也吵。**

「轻点缩回单个词」那种也不读 —— 规矩定成「长按一个词才读」最好记，
而缩回是轻点，不是长按。

### 改动很小

`LyricEditor` 里挂上 `useSpeak()`，在第 1 种情况那一支加一句
`speak(anchorId, word, { lookup: true })`。走的是和别处同一条路：
先查词典真人录音，没有才退回机器音；读不出声的手机就是不响，
**长按取词本身完全不受影响**。

### 有半秒的等待，这个躲不掉

生词板那边会预取，所以点已标过的词是秒响。但**正要长按的这个词还没被标过**，
不在预取范围里，所以第一次长按要等约半秒去取录音。

不预取整篇是故意的 —— 一篇几百上千个词，为了「可能会长按其中一个」
把整篇都下下来不划算。标过之后它就进了生词板的预取名单，以后都是秒响。

### 实测（浏览器里走真实流程，用真的长按事件序列）

| 步骤 | 取录音 | 抽屉 |
|---|---|---|
| 长按第一个词 | ✅ 取了 `think` | 弹出 |
| 接着轻点另一个词（连成范围） | **没取** | 还在 |
| 再轻点一个（改范围） | **没取** | 还在 |
| 轻点缩回一个词 | **没取** | 还在 |

系统朗读全程一次都没被调用（说明走的是真人录音）。
另跑过一轮：长按 A 读了 A，接着**长按** B 升级成范围时也没取。

测试 **385 项全过**；`tsc -b --force`、`npm run build` 都通过。

### 测的时候踩到的坑，记一笔

第二轮测出来「一次都没读」，差点以为改坏了 —— 其实是**上一轮留下的选区还在**，
抽屉一直开着，`openWordDrawer` 根本走不到第 1 种情况。
刷新页面重跑就对了。

**测有状态的交互，每一轮都得从干净状态起。** 这和第三十节那个
「面板藏起来时平滑滚动不推进」是同一类：先怀疑测试环境，再怀疑代码。

---

## 三十二、~~复习页编辑模式：左滑删除卡片~~ ✅ 2026-09-01 已验收

**分支 `swipe-to-delete`。**

### 动手前查清楚的三件事

1. **不会和拖拽打架。** 拖拽只从手柄起手（`PointerSensor` 距离 5px，
   监听挂在手柄那颗按钮上），卡片本体上的左滑是自由的
2. **⚠️ 笔记删了是真没了。** 标注**不进回收站**（回收站只管文档和文库），
   `deleteAnnotation` 直接从库里抹掉。误滑一下，那条的音标、词性、释义
   —— 可能是 AI 填的，也可能是手写的 —— 一起没
3. **文库复习的卡是合并出来的。** 同一个词在三篇里各标过一次，那儿只显示一条、
   频次记 3。删它等于一次删三条标注

### 用户拍板的三件事

- **两步才删**：滑开只是把「删除」露出来，**还要再点一下**才真删。
  就是因为上面第 2 条 —— 没有回收站兜底，一步删太险
- **文库复习不给**，只在单篇文档的复习里。和拖拽排序同一个条件、同一个理由
- **编辑模式内外都给**（第二版改的，原因见下）

### ⚠️ 第一版真机上「划了一点反应都没有」

发出去的第一个包，用户在手机上**几乎划不动** —— 得手指按在输入框和文字
以外的那点空白处才行。

**根因**：那一版为了不抢输入框和拖拽手柄，把
`button, input, textarea, select, a` 全挡掉了。而编辑模式下卡片正中间
**整条带子都是输入框**：音标一格、释义一格，右上角词性一格，左上角朗读按钮。

量出来的：卡片上取 15 个探测点，**9 个落在被挡的元素上**。手指自然按下去，
几乎必然被挡。

**浏览器里为什么没发现**：当时把指针事件**直接打在卡片外层那个 div 上**，
把所有子元素都绕开了。测出来一切正常。

> **教训：测手势，事件要打在手指真正会碰到的那个元素上。**
> 打在容器上等于把自己写的过滤逻辑整个跳过去了 —— 这种测试只会让你安心，
> 不会让你发现问题。

**改法两条**：

1. 挡的范围收窄到只剩两种：**拖拽手柄**（`data-no-swipe`，它有自己的手势），
   和**你正在编辑的那一格**（`document.activeElement === field`，
   那时候横拖是在挪光标）。按钮不再挡 —— 反正走够 8px 才接管，
   点一下照样是点一下
2. **编辑模式外也能划**。用户提的，理由很硬：**非编辑模式下压根没有输入框**，
   那边天生就顺手，「会不会吸走焦点、会不会顶出键盘」这个只能靠真机试的
   不确定项直接不存在了。编辑模式留着也没坏处

### 观感上的两处返工（用户逐条挑出来的）

**一、红块和卡片贴不上。** 第一版红块只占右边 88px，卡片滑开时接缝处露出一弯底色 ——
卡片是 `rounded-xl`，右边缘是弧的，红块左边是直的，两者贴不上。
改成 `absolute inset-0` 铺满整张卡底下，按钮本身仍是 88px 靠右站。
卡片让开多少，露出来的就是多少红色。

**二、静止时边角还是透出一丝红。** 铺满之后又出现新问题：
两个同样大小、同样圆角的方块叠在一起，**边角上总会因为抗锯齿透出一线红边**。
与其去凑那一个像素，**不如没在滑的时候根本不画那一层**（`open || dragOffset !== null`）。

**三、卡片左边被「啃掉」。** 用户原话：希望卡片「随着滑动被推出屏幕边缘」，
而不是在自己的边界上被吞掉。原因是外壳的 `overflow-hidden` 是在**卡片自己的边界**上裁的。

改成 **`overflow-visible sm:overflow-hidden`**：

- **手机（单列）不裁** —— 卡片滑出去，由外面的容器在屏幕边缘裁断，那才是「被推出去」
- **宽屏（sm 起）仍旧裁** —— 一行 2～4 张卡，不裁的话滑动的这张会盖到左边邻居身上。
  量过：900px 下两列间距只有 16px，而卡片要滑 88px

红块因此自己带上 `rounded-xl`，不再依赖外层去裁。

### 红色那层要铺满整张卡底下

第一版红块只占右边 88px，卡片滑开时接缝处露出一弯底色 ——
卡片是 `rounded-xl`，右边缘是弧的，红块左边是直的，两者贴不上。

改成 `absolute inset-0` 铺满，按钮本身仍是 88px 靠右站。这样没有接缝可言：
卡片让开多少，露出来的就是多少红色，四角由外层的
`rounded-xl + overflow-hidden` 一起裁。

### 一个真 bug，是造出来的「快划」抓到的### 一个真 bug，是造出来的「快划」抓到的

第一版把手指拉到哪记在 **state** 里，松手时读它判断「滑够了没」。
慢慢拖是好的，**一次利落的快划却会弹回去**。

原因：浏览器会把好几个 `pointermove` 和 `pointerup` **合并在同一帧**送来
（快划就是这样）。这时候 React 还没来得及重画，`state` 里还是上一帧的值、
甚至还是初始的 `null` —— 照它判就是「没滑够」，于是弹回原位。
手感上就是**「慢慢拖好使，快划没反应」**，最难查的那种。

改法：位移记在 **ref** 里，那一份才是准的；state 只负责画出来。松手时读 ref。

顺带把 `setPointerCapture` 包进 try/catch —— 有的浏览器对「不是当前活动指针」的
id 会直接抛错，抓不住的话整个手势会崩掉。

### 别抢竖着滚页面

卡片是竖着排的一长列，手指多半是想往下滚。所以：

- 容器写 `touch-action: pan-y`，竖着滚交给浏览器，横向的才到我们手里
- 代码里再判一次：**横向位移既要够大（> 8px）、又要比纵向大**，两条都满足才接管；
  纵向先跑出去就彻底撒手，这一下再也不抢

### 别的几条

- 手柄、朗读按钮、编辑模式下的输入框都排除在外（`closest('button, input, textarea, select, a')`）
- 同时只开一张，开第二张时第一张自己收回去
- 刚滑完的那一下 click 吃掉，免得连带把卡片展开；已经滑开时点卡片本体 = 收回去
- 退出编辑模式 / 换一篇 / 切到句摘，滑开的那张都收回去
- 不可滑的时候**不套那层壳**，而不是套着再用 props 关掉 —— 一页上百张卡，
  白搭两层 div 和一串指针监听不划算

### 实测（浏览器里走真实流程，用真的指针事件）

| 验的东西 | 结果 |
|---|---|
| 快划一下（move 和 up 同一帧） | 开到 -88px ✅（修之前这里会弹回去） |
| **手指按在「释义」那格正中间横划** | 开到 -88px ✅（第一版这里纹丝不动），
  且划完焦点仍在 body，那格**没被聚焦** |
| 往右滑 | 收回 0 |
| 竖着滑 | **纹丝不动**，不抢滚动 |
| 滑开第二张 | 第一张自己收回去 |
| 在朗读按钮上点一下 | 不划，**照样读**（播放 1 次） |
| 从朗读按钮上起手左划 | 划开 -88，**不读**（播放 0 次） |
| 点「删除」 | 标注 5 → 4 条，卡片从界面消失 |
| 非编辑模式 | 4 张卡都能划，拖拽手柄 0 个 |
| 文库复习 | 滑动外壳 0、拖拽手柄 0，两样都不给 |
| 红块与卡片的接缝 | 铺满之后贴合，外圆角跟着卡片走 |
| 静止时的边角 | 红层根本不画（`hidden`），不会再透出红边 |
| 375px 滑开 | 卡片滑出屏幕边缘、在屏幕边上裁断，页面不横滚（scrollWidth 375 = 窗口宽） |
| 900px 两列滑开 | 外壳 `overflow: hidden`，滑动那张不压左边邻居 |
| 375px | 页面不横滚 |

测试 385 → **394 项全过**；`tsc -b --force`、`npm run build` 都通过。

### 又一次栽在同一件事上（第三次了）

量位移时先读的 `getComputedStyle(...).transform`，读到 -70，以为没开到位 ——
其实 inline style 已经是 `translateX(-88px)`，**读到的是过渡动画的中间帧**，
而这个环境里动画根本不推进（面板一藏，帧循环就停）。

第三十节量滚动、第二十九节量面板位置，栽的都是这个。
**在这个环境里验动画类的东西，读 inline style 或 React 状态，别读 computed。**

**截图也一样会骗人。** 后来看 900px 的截图，以为两张卡同时滑开了（同时只该开一张）——
量出来只有一张是 -88，另一张是 0。截图拍到的是**另一张正在弹回、被冻在半路**的那一帧。
看着不对先去量，别照着截图改代码。

---

## 三十三、~~划句子带不进开头的引号~~ ✅ 2026-09-02 已验收

用户报的：`“I like you,” he said.` 这样的句子，**开头那个引号划不进去**，
因为它在句首、贴在第一个词外面。

### 根因：范围两端一定落在词上，外面那两个符号在切口之外

笔记的坐标是「第几行第几个词」，所以划出来的范围两头一定卡在词上。取原文是
**从第一个词的头一个字母切到最后一个词的末一个字母** —— 词与词之间的标点
夹在里面所以留下了，但贴在最外面的那两个天然带不进来。

先量出来的（不是凭印象）：

| | |
|---|---|
| 原文 | `“I like you,” he said.` |
| 从前存下来的 | `I like you,” he said` |
| 开头的 `“` | ❌ 丢 |
| 中间的 `,”` | ✅ 在（夹在词中间） |
| 结尾的 `.` | ❌ 也丢 |

**所以不只是引号，句末的句号一直也是丢的**，只是不显眼，用户没报。

代码里其实已经为这件事打过两个补丁：撇号（`rock 'n' roll`、`students'`）和
句尾的数字（`hit in 2020`）。引号和句号只是第三、第四种没被补上的情况。

### 两个地方，得一起改才对得上

1. **句摘卡上的文字**（`getRangeText`）
2. **正文里那条虚线** —— 同样只从第一个词画到最后一个词，引号在线外不被盖住。
   肉眼看就是「引号没被划进去」

### 改法：分方向扩，不是「扩到空格为止」

用户当场问的就是这个：**「要是句子前面是个句号，也会被划进去吗」**。

所以规则按方向分成两张表（`src/utils/punctuation.ts`）：

- 往左**只吃开头类**：`" ' “ ‘ « ‹ 「 『 ( [ { ¿ ¡`
- 往右**只吃收尾类**：`" ' ” ’ » › 」 』 ) ] } . , ! ? ; : … 。，！？；：、`

句号不在开头类里，于是 `He smiled. “I like you...` 从 `I` 起划**只会吃到 `“`**，
前一句的句号碰都不碰。若用「一路扩到空格为止」，遇到 `He smiled."I like` 这种
没空格的写法就挡不住了 —— 分方向是硬保证，不是概率。

**撇号故意不碰**：分词和对账那边早有一套专门的撇号规则，这里只补、不动它。

### 正文画线：得先把「其它」段拆开

`He smiled. “I like...` 里的 `. “` 在分词结果里是**一整段**。整段画线会连前一句的
句号一起画，不画又盖不到引号。所以加了 `splitEdgePunctuation`：把「其它」段两头的
标点拆成独立的段，并标出它归左边还是右边的词。拆的规则和取原文用的是同一对表，
所以**「看到的线」和「存下来的字」永远对得上**。

缩写后缀（`'s`、`n't`）不拆 —— 那是词的一部分。

### 绕开的一个坑：短语不能带标点

抽屉里同一段选区**既能存成句摘也能存成短语，两边共用一份文字**。句摘要带上引号
句号才完整，短语不能带 —— 短语要拿去词典查真人录音，而 `he said.` 这样带着句号
是查不到的，整条会白白落回机器音（和第二十七节弯撇号那个是同一类毛病）。

所以短语这条路上一律剥掉两头的标点，四个入口都堵上了：

- 抽屉里保存（`LyricEditor.saveBubble`）
- 一键划词（`mark/locate.ts` —— 它只出单词和短语，从不出句摘）
- 正文编辑后对账重算（`reconcile.rangeTextFor`，按 `type` 分两套规矩）
- 查词典之前再剥一次（`dictAudio.normalizeWord`）—— 这一道是**给旧数据兜底**的，
  从前存进去的短语还带着标点

抽屉顶上显示的那一行也跟着切：切到「短语」时先剥，**看到的就是待会儿存进去的**。

### 实测（浏览器里走真实流程，375px）

`computer` 工具这一轮点不动（每次 30 秒超时），改用真的 PointerEvent 打在
词的 span 上驱动 —— 和第三十二节一个做法。

| 走的流程 | 结果 |
|---|---|
| 长按 `I` → 长按 `said`，看抽屉标题 | `“I like you,” he said.` ✅ 引号句号都在 |
| 同一选区切到「短语」 | `I like you,” he said` ✅ 两头剥掉、中间的 `,”` 留着 |
| 切回「句子」 | 又变回带引号句号的那份 ✅ |
| 存成句摘后，正文里逐段量线型 | `He` `smiled` `.` 三段**无线**；`“` 起、到句末 `.` 止全是句摘虚线 ✅ |
| 第二行 `She answered "I know" and left.` 存成短语 | 波浪线只盖 `I know`，两个引号在线外 ✅ 和存进去的字一致 |
| 直接翻 IndexedDB 看存了什么 | 句摘 `“I like you,” he said.`、短语 `I know` ✅ |
| 控制台 | 无报错 |

单测 411 → **413 项全过**（新加 19 条，`punctuation.test.ts`）；
`tsc -b --force`、`npm run build` 都通过。

### 改了两条旧断言，是故意的

`tokenize.test.ts` 里两条钉的是「句末标点不带」：`Hello, world!` 从前存成
`Hello, world`。这次正是要改掉这个行为，所以断言跟着改，并在注释里写明。

### 老数据不回补

已经存好的句摘不会自动补上引号，除非重新划一次、或者编辑正文触发对账。
**故意不做一次性回补** —— 缺个引号不影响用，回补要动全库数据，风险不划算。
（查词典那道剥离是例外，那个不动数据，只影响查询，所以顺手做了。）

---

## 三十四、~~点「编辑全文」跳回文首~~ ✅ 2026-09-02 已验收

用户报的：读到中间点「编辑全文」，页面**跳回文章开头**，得重新翻下来找位置。

### 根因：阅读和编辑是两个各自独立的滚动容器

代码里 `editMode` 为真时 `return` 的是**另一棵界面**，跟阅读那棵不是同一个 DOM。
新建的容器 `scrollTop` 天然是 0，位置根本没有渠道传过去。

量出来还发现一件光读代码会搞错的事：

| | scrollHeight | clientHeight | 真的在滚吗 |
|---|---|---|---|
| 编辑模式外层那个 `div` | 692 | 692 | **不滚** |
| 里面的 `textarea` 自己 | 8111 | 692 | ✅ 在滚 |

`textarea` 的 class 里写着 `min-h-full`，看着像是外层在滚，**实际是 textarea 自己
带着滚动条**。滚外层那个 div 一点用都没有 —— 这种事只能量，读代码读不出来。

### 回程本来就是好的

顺手验了一下：点「完成」回阅读页，会照 `savedProgress` 回到原来的位置（实测
进去前第 20 行、回来还是第 20 行）。**只有进去这一下是坏的**，所以只修一个方向。

### 改法：两边都认得的坐标只有「第几行」

位置不能按像素抄 —— 两边排版差得远（同一篇文章阅读页 11602px、textarea 只有
8111px），按比例换算量出来会偏六七行。

所以走「第几行」：

1. 点按钮那一刻，**趁阅读容器还没卸载**先算出屏幕顶上是第几行，存进一个 ref。
   正文本来就是一行一个 `<p>`，给它加个 `data-line-index` 就能认
2. 进编辑模式后，用 `useLayoutEffect`（在画出来之前，不让用户看见「先闪一下文首
   再跳过去」）把 textarea 滚到那一行

### 量那一行有多高：克隆一个同款 textarea

不能拿「行高 × 行数」算 —— 正文里长短行都有，**一行常常折成两三行**。

做法是克隆一个同款的 textarea（同样的 class、同样的宽度），只塞进前 n 行，
读它的 `scrollHeight`。折行位置和字体度量全交给浏览器自己算。量完就删。

两处容易栽的：

- **宽度必须钉死** `ta.offsetWidth`。class 里是 `w-full`，克隆体脱离文档流之后
  撑不出原来的宽度，折行位置就全变了
- **等草稿灌进来再动手**（`ta.value === content` 才滚）。草稿是在另一个 `useEffect`
  里灌的，比 `useLayoutEffect` 晚一拍，抢在它前面量的是上一份文字

### 实测（浏览器里走真实流程，375px）

拿一篇 80 行的中英对照长文（阅读页 11602px 高）逐档量。判断准不准用的是**另一种
量法**（一面镜子逐行量 y），不是拿自己的算法验自己：

| 阅读页停在 | 顶上第几行 | 编辑页 scrollTop | 镜子说该在 | 差 |
|---|---|---|---|---|
| 文首 | 0 | 0 | 28 | 故意的（文首要看见上面那圈留白）|
| 1200px | 8 | 830 | 834 | −4px |
| 3000px | 20 | 2040 | 2044 | −4px |
| 5800px | 40 | 4113 | 4117 | −4px |
| 拉到最底 | 75 | 7419（**到底了**）| 7688 | 见下 |

差 4px 是两种量法之间的系统误差，不到行高（28.8px）的七分之一，肉眼看不出来。

**最底那一档滚不过去是正常的，不是 bug**：阅读页比 textarea 高一大截（多一块
128px 底部留白，排版也松），最后一屏在 textarea 里已经没有下面的内容了，
只能停在它自己的最底。那一行仍然在屏幕上，只是不在最顶。

另外验的：

| | |
|---|---|
| 一个来回（读→编辑→完成） | 进去第 20 行，回来还是第 20 行 ✅ |
| 短文档（不满一屏） | scrollTop 0，没乱滚 ✅ |
| 控制台 | 无报错 |

测试 413 项全过；`tsc -b --force`、`npm run build` 都通过。

### 这两个函数没有单测，是有原因的

`scrollTopForLine` 和 `topVisibleLine` 都要**真的排版**才有意义
（`scrollHeight`、`getBoundingClientRect`）。测试环境不做排版，量出来全是 0，
写了也只是自欺欺人。所以这一条靠浏览器里的实测，上面那张表就是它的验收记录。

---

## 三十五、~~「新建文库」从顶栏挪进列表~~ ✅ 2026-09-02 已验收

用户说的：左侧栏顶上那两个按钮（新建文库、整理）「放在那里不是很协调，也不符合直觉」。

### 先量，别凭印象说「不协调」

| | 尺寸 | 长相 |
|---|---|---|
| 整理 | 42 × 26 | 有边框、有文字 |
| 新建文库 | 32 × 32 | 无边框、纯图标 |
| （对比）底部三格 | 82 × 56 | 图标 + 文字 |

不协调是三件具体的事叠在一起：

1. **两个按钮不是一类东西却并排**。一个有框有字、一个无框只有图标，高度还差 6px。
   眼睛去找共同点，找不到
2. **语义也不是一类**。「整理」是**模式开关**（点开会亮着、有状态），「新建文库」是
   **一次性动作**。两种性质不同的东西并排、又长得不一样，没法从长相推断行为
3. **「新建文库」在手机上是个哑谜**。说明全写在 `title` 里，而 title 是鼠标悬停才有的，
   **手机上根本不存在**

第 3 条要记一笔：**这个坑这个项目已经踩过两次**（第十九节底部五个光秃秃的图标、
第二十二节魔杖），理由写的都是同一句「title 手机上根本不存在」。顶上这个是漏网的
第三个。以后新加纯图标按钮，先想一遍这句话。

另外触摸目标：26px / 32px，都低于安卓 48dp 的最小推荐值；底部那排是 56px。

### 用户拍板：各归各位

给了三个方案，用户选了这个：

- 「新建文库」从顶栏拿走，变成**文库列表末尾的一整行**「＋ 新建文库」——
  **创建的入口贴着被创建的东西**，是最好猜的位置
- 顶栏只剩「我的文库」+「整理」，一行一件事

否掉的两个：「就地做成一对」（顶栏只有 226px，塞标题加两个文字按钮会挤，
触摸目标还是小）、「都下沉到底部那排」（四格每格只剩 62px，「回收站」三个字会挤；
而且「新建」和「导入」都是「加东西」，并排容易认错）。

### 动手时量出来的一处：两列图标对不齐

第一版直接照文库行的 `px-3` 排，量出来**整体比文库行左移 24px** ——
文库行前面有个折叠三角（12px 图标 + 内外边距共 24px），新建这行没有，
于是它的图标落在了三角的位置上、文字落在了文库图标的位置上。

改成 `pl-9` 之后：图标 x=44、文字 x=68，和文库行分毫不差。

**这是第五次靠量数字发现真正的原因**（前四次：喇叭图标偏高 2px、长网址 302px
撑破 275px 的卡片、两条线都画在基线下 4px 处、短语按钮 278px 塞进 223px 的卡片）。
截图上这个 24px 只是「看着有点怪」，量出来才知道差多少、差在哪。

### 实测（浏览器里走真实流程）

| | |
|---|---|
| 顶栏 | 只剩「整理」一个按钮 ✅ |
| 列表顺序 | 文库列表 → ＋ 新建文库 → 版权（贴底）✅ |
| 尺寸 | 233 × 48，正好够手指点 ✅ |
| 图标/文字对齐 | 375px 和 900px 下都和文库行对齐 ✅ |
| 点一下 | 文库真的从 1 个变成 2 个 ✅ |
| 整理模式 | 这一行自动收起（那会儿在拖拽排序，新建是另一码事）✅ |
| 退出整理 | 又出现 ✅ |
| 页面横滚 | 两种宽度下都没有 ✅ |
| 控制台 | 无报错 |

测试 413 项全过；`tsc -b --force`、`npm run build` 都通过。

### 用户看完提的三条，当日已改

1. **「整理」照右侧栏那颗「AI 划词」的样子做**（用户点名的参照）。
   从前是 42 × 26 描边的小文字按钮；现在是图标 + 文字、不描边、hover 才出底色、
   text-sm，量出来 62 × 30，和「AI 划词」的 79 × 30 是同一套。
   图标用 `GripVertical` —— 就是点开之后每行右边冒出来的那个拖拽手柄，
   按钮和它要你干的事用同一个图案。

   和「AI 划词」有一处必须不同：**整理是模式开关，会一直亮着**，所以留着
   `bg-amber-600 text-white` 的选中态；AI 划词是一次性动作，没有「开着」这回事。

2. **「新建文库」左边补一个加号**。用户的原话是「看起来有些飘」——
   根子在于列表里每一行都是**三段**（折叠三角 / 图标 / 文字），只有它是两段，
   最左那一竖列断了。补上加号（占三角那一格）之后三条竖线全接上。

3. **文件夹图标和文库图标对齐**。上一版量的是「三角**按钮**的 x」（20），
   这一版比的是「三角**图标**的 x」（24）—— 加号要和图标对齐，不是和按钮对齐。

最终三列：**加号 x=24 / 文件夹 x=44 / 文字 x=68**，375px 和 900px 下都和文库行
一个像素不差。行高 48px（文库行 49px，差 1px —— 文库行里还挂着一个 p-1.5 的
「更多」按钮把它撑高了，这一行没有，照抄 `py-2.5` 只有 40px、看着矮一截，
所以改用 `py-3.5`）。

### 又栽在「读 computed / 看截图」上（第四次）

验整理按钮的选中态时，`getComputedStyle` 读出来底色是**透明**，看着像没生效；
截图里也拍到过「已经关掉了、却还是琥珀色」的一帧。

两次都是假的。读 `className` 才是真的：开着是 `bg-amber-600 text-white`，
关掉是 `text-ink-muted hover:...`。把协议弹窗那两层遮罩临时藏掉之后，
computed 立刻变成 `rgb(217, 119, 6)` —— **遮罩盖着的时候那一片根本不重绘**。

第二十九、三十、三十二节栽的都是这个。规矩再重申一遍：
**这个环境里验样式和动画，读 `className` / inline style / React 状态，
别读 computed，也别信截图。**

### 使用说明里那条要跟着改

`UserGuide.tsx` 有一条写着「左侧栏右上角的**整理**」—— 这句还准（整理没挪）。
但如果以后把新建文库的位置再挪，记得回头看这个文件。

### 又一条：手机上 hover 会赖着不走（用户报的「第三种状态」）⚠️ 这一节的做法后来推翻了，见下一节

用户点完整理再点一下，按钮**没复原**，卡在「白底 + 琥珀字」上 ——
他问「明明只需要两种形态来回切换就够了吧」。

**那不是第三种状态，是 hover 卡住了。** 量出来对得上：

| 看到的 | 实际是 | 颜色 |
|---|---|---|
| 灰字无底 | 关着 | 字 rgb(92,92,92) |
| 琥珀底白字 | 开着 | 底 rgb(217,119,6) |
| **白底琥珀字** | **关着 + hover 没退** | 底 rgb(245,245,244)、字 rgb(180,83,9) |

第三行正是 `hover:bg-stone-100 hover:text-amber-700` 的样子。电脑上鼠标移开就没了，
**手机上没有「移开」这回事** —— 手指点完，浏览器认为指针还停在按钮上。

**这不是整理独有的**：全 App 84 处 hover 在手机上点完都会黏住（AI 划词、导入、设置、
每一行文库都是），只是别的按钮点完就弹窗或跳页，不容易看出来。

用户拍板：一次性治好全 App。`tailwind.config.js` 开 `hoverOnlyWhenSupported`，
Tailwind 会把所有 hover 规则包进 `@media (hover: hover) and (pointer: fine)`。

验的：

| | |
|---|---|
| 打包产物 | 20 条 hover 规则里 19 条被包了进去（剩下那条是滚动条滑块的手写 CSS，手机上本来就没有滚动条）✅ |
| 模拟手机的视口 | `(hover: hover) and (pointer: fine)` = **false**，`maxTouchPoints` = 5 → hover 根本不生效 ✅ |
| 电脑视口 | 同一条 = **true**，`maxTouchPoints` = 0 → hover 照常 ✅ |
| 整理按钮 | className 在两态之间来回，再点一下**回得去** ✅ |

**一个坑**：改了 `tailwind.config.js` 之后开发服务器不会自动生效，
得重启（第一次验的时候读到的还是没包 media 的旧样式，差点以为没用）。
打包（`npm run build`）倒是当场就对。

### ⚠️ 上面那个做法我改坏了，用户当场发现（同日）

只把 hover 关进 `@media (hover: hover)` 之后，手机上确实不再黏住了，
**但全 App 的点击反馈也一起没了** —— 底部三格、目录的「更多」、两颗 AI 按钮，
按下去都毫无动静。用户的原话：「这一下子好像改的太死板了」。

**这是我的错**：我把毛病的来源（hover 在触屏上的行为）当成了毛病本身，
一关了之，没想过触屏也需要「按下去有反应」这件事。

### 正解：换掉 `hover:` 的触发条件，而不是关掉它

不是「关掉 hover」，是**给它换一套触发条件**：

```
有真鼠标 → 认「悬停」        (@media (hover: hover) and (pointer: fine))
没有     → 认「正按着」      (:active)
```

`:active` 手指一松就退，不会像 hover 那样赖着不走 —— 第一个毛病的病根也就没了。

做法是在 `tailwind.config.js` 里**重定义 `hover:` 这个变体本身**，让它一次生成两条规则。
好处是**一次覆盖全部**：84 处写着 `hover:` 的地方自动都有了触屏反馈，
各自的颜色也照旧 —— 琥珀底的按下去加深一格（amber-600 → 700）、
白底的按下去出琥珀字。**不用一个个去补 `active:`，以后新写的也自动带上。**

一开始我确实是打算一个个补的，写到第二颗就发现该改变体定义。

### 验的（打包产物 + 开发服务器，两处都查）

| | |
|---|---|
| 只对鼠标的 hover 规则 | 19 条，全在 `@media (hover: hover) and (pointer: fine)` 里 ✅ |
| 手指也认的 active 规则 | 22 条，全在 media 外面（手机上照样生效）✅ |
| media 外还剩的 hover | 1 条 —— 滚动条滑块的手写 CSS，手机上本来就没有滚动条 ✅ |
| 逐个抽查 | `bg-stone-100`（底部三格）、`bg-stone-200`（更多）、`bg-amber-700`（AI 填充亮着时）、`text-amber-700`（AI 划词）—— 鼠标和手指两条都在 ✅ |
| 模拟手机的视口 | 「有真鼠标吗」= false，触摸点 5 → hover 不生效、active 生效 ✅ |
| 电脑视口 | 「有真鼠标吗」= true → 悬停照旧 ✅ |

### ⚠️ 这一条我验不了最后一步

**`:active` 在真机上按下去到底看不看得见，我没法验。** 这个环境里没法制造真正的
「按住」状态（合成事件不触发 `:active`），CSSOM 只能证明规则生成对了、该生效的
场合会生效。安卓 WebView 对按钮是认 `:active` 的，但手感（会不会太短、
滑动时会不会误亮）只能靠手机上试。

和「提示词准不准」「这台手机的朗读引擎读得对不对」是同一类 —— 见文档开头那一条。

### 顺带补的一处不一致

「AI 填充」平时那一档从前只有 `hover:bg-stone-100`、**字色不变**，
而「AI 划词」是 `hover:bg-stone-100 hover:text-amber-700`。两颗 AI 按钮凑成一对，
按下去却不是一个样子。给填充补上了 `hover:text-amber-700`。

### 又数错一次，记一笔

查打包产物时先按「第一次出现 `@media (hover: hover)` 的位置」切成前后两半就数，
得出「6 条 active 全被包进去了」——**吓一跳，以为白改**。其实那样切没算大括号闭合，
media 块之后的东西全被算进「里面」了。数括号找到真正的闭合位置之后，答案是 0 条在里面。

另外开发服务器的 CSS 是**美化过的**（`:active {` 带空格），
按 `:active{` 去数会数出 0 条。两处都是自己的检查脚本写错，不是代码坏了 ——
**验证脚本本身也会骗人，对不上时先怀疑脚本。**

### 「AI 划词」再单独一条：弹窗开着期间一直亮着（用户提的）

`:active` 是「正按着」，手指一松就退 —— 这颗按钮点完会弹窗，于是看到的是
**「唰地亮一下又暗回去」**，弹窗已经在眼前了、按钮却装作没事人。用户说不协调。

改成状态驱动：`autoMark.open` 传给右侧栏，弹窗开着期间按钮一直保持
`bg-stone-100 text-amber-700`（就是按下去的那个样子），关掉才还原。
让人看得出「这颗按钮和眼前这张弹窗是一回事」。

关弹窗的路子不止一条（叉号、遮罩、安卓返回键），但**`autoMark.open` 是唯一的
那份状态**，哪条路关的都会还原，不用一条条去接。

实测（浏览器里走真实流程，开→关来回两遍）：

| | |
|---|---|
| 平时 | `text-ink-muted` |
| 弹窗开着 | `bg-stone-100 text-amber-700` ✅ |
| 关掉之后 | 回到 `text-ink-muted` ✅ |
| 再开再关 | 同上，不残留 ✅ |

（读的是 `className` 不是 computed —— 这个环境里 computed 会骗人，见前面几节。）

**「AI 填充」当日也跟着改了**（用户补的一句：「改的是在没有检测到空白卡的那个状态，
那个状态的按钮本来就和 AI 划词那个按钮一样嘛」——**说得对**，我当时把两档笼统当成一颗
按钮看待，其实该分开：

| 那一档 | 平时 | 弹窗开着 |
|---|---|---|
| 没有空白卡片（长得和「AI 划词」一模一样）| 灰字 | **保持按下的样子** ✅ 跟着改 |
| 有空白卡片（琥珀底白字 + 数字）| 琥珀底 | **原样不动** —— 那个琥珀色另有含义（还差几条没填全），再叠一层就读不清是哪个意思 |

实测（浏览器里走真实流程，两档分别开关来回）：

| | |
|---|---|
| 没有空白卡片，平时 | `text-ink-muted` |
| 没有空白卡片，弹窗开着 | `bg-stone-100 text-amber-700` ✅ |
| 关掉 / 再开再关 | 干净还原，不残留 ✅ |
| 有空白卡片，平时 vs 弹窗开着 | 两边都是 `bg-amber-600 text-white`，**一模一样** ✅ 没被影响 |

「有空白卡片」那一档是**现造出来的**才验得了：在阅读页长按一个词、什么都不填直接保存，
按钮当场变成「AI 填充 1」。不造的话那一档根本不出现，只能靠读代码猜。

### 又一次「控制台报错是假的」

验完看控制台，一片 `ERR_CONNECTION_REFUSED`。其实是前面为了让改动生效
**重启过两次开发服务器**，页面在那期间够不着服务器留下的。

判断办法：往控制台插一条自己的标记，再看错误在标记前面还是后面 ——
全在前面，就是旧的。**别看见红字就以为是自己刚改坏的。**

---

## 三十六、~~三列顶栏对不齐~~ ✅ 2026-09-02 已验收

用户说的：「主页面、左侧栏、右侧栏三个部分的顶部各有不同，看起来不是很协调」。

### 量出来：五条横线落在五个不同高度

| 你看到的那条线 | 在哪一列 | y |
|---|---|---|
| 主区顶栏底边 | 中 | 37 |
| 左侧栏顶栏底边 | 左 | 55 |
| 主区第二条底边（正文从这儿开始）| 中 | 74 |
| 右侧栏顶栏底边（列表从这儿开始）| 右 | 99 |
| 左侧栏「阅读/复习」底边（目录从这儿开始）| 左 | 116 |

眼睛横着扫过去，五条线全对不上。另外两处小的：

- **分隔线是两种灰**：侧栏 `paper-border` 暖灰 `rgb(229,227,223)`、
  主区 `gray-200` 冷灰 `rgb(229,231,235)`。单看没事，并排就发脏
- **左右内边距** 12 / 12 / **16** / 12

### 结构上本来就有救：三列都在做同两件事

|  | 第一带：这是什么 | 第二带：模式 / 动作 |
|---|---|---|
| 左 | 我的文库 + 整理 | 阅读 ｜ 复习 |
| 中 | 文档标题 + 两个抽屉键 | 长按提示 + 编辑全文（复习模式下是那排工具）|
| 右 | 笔记(n) + AI 划词 | 生词 ｜ 句摘 |

对应关系是现成的，缺的只是把两带的高度、内边距、分隔线统一。
所以收进一份共用定义 `src/components/chrome.ts`：**第一带 44px、第二带 40px**，
统一 `px-3`、统一 `paper-border`。

### 两个刻意的选择

1. **用 `min-h-` 不用 `h-`**。内容装得下时高度就是这个数、三列对得齐；
   万一哪天塞多了要折行（复习页那条是 `flex-wrap` 的），它能自己长高而不是把内容切掉。
2. **底色没动**。主区那几条是纯白、侧栏是 `bg-white/80`，这是各自和下方内容的关系，
   跟对齐是两件事，一起改会把改动摊得太开。

### 顺手收进来的两条

除了用户看到的三列，还有两处也是「第二带」，不一起改的话一换模式又错开：

- **复习页那排工具**（英/中 + AI 填充 + 词/句）
- **编辑模式那条**（编辑全文 / 完成）

### 实测（浏览器里走真实流程）

| | 第一带底边 | 第二带底边 |
|---|---|---|
| 1400px 阅读模式 | 左 44 / 中 44 / 右 44 ✅ | 左 84 / 中 84 / 右 84 ✅ |
| 1400px 复习模式 | 44 ✅ | 84 ✅ |
| 1400px 编辑模式 | — | 84 ✅ |
| 375px 窄屏 | 左 74 / 中 74 / 右 74 ✅ | 左 114 / 中 114 / 右 114 ✅ |

（375px 整体比宽屏低 30px 是顶部安全区，三列一起低，不影响对齐。）

其它：

| | |
|---|---|
| 分隔线 | 三列都是 `rgb(229,227,223)` ✅ |
| 左右内边距 | 三列都是 12px ✅ |
| 有没有被压扁 | 四条带 `scrollHeight` 都没超过 `clientHeight` ✅ |
| 提示文字 | 「长按单词添加笔记」和选中后的「轻点其它单词可连成句子」，375px 下都没被截断 ✅ |
| 页面横滚 | 没有 ✅ |

为了塞进 40px，左侧栏那个「阅读/复习」药丸从 `py-2` 收到 `py-1`（28px），
右侧栏的「生词/句摘」是 30px，都还在 40px 里留着余量。

测试 413 项全过；`tsc -b --force`、`npm run build` 都通过。

### 控制台报错又是假的（第三次）

验完看到一片 `BAND_TOP is not defined`。是我**先改了用处、还没加 import**
那一瞬间热更新抓到的。插一条标记再重载，标记之后零报错。
判断办法和前两次一样：**先看红字在标记前面还是后面。**

### 对齐之后变挤，改法是拆掉「盒中盒」（同日，用户拍板）

对齐是对齐了，但用户说挤 —— 尤其「阅读/复习」和「生词/句摘」。量出来：

| | 现在 | 我改之前 |
|---|---|---|
| 左 · 按钮高 | 28 | 36 |
| 左 · 药丸上下各剩 | 4px | 8px |
| 右 · 药丸上下各剩 | **2px** | 12px |

右边那 2px 是全场最挤的一处。

**先查了能不能从第一带匀空间：匀不了。** 第一带里最高的是主区那个汉堡键，
36px 塞在 44px 里，本来就只剩 4px。所以不存在「不加高也能变松」，除非换个做法。

给了三个方案（做成网页让用户看真实像素，不是文字描述）：
A 加高到 52px、B 加高到 48px、C 不加高、拆掉盒中盒。**用户选 C。**

### C 是什么

挤的另一个根源不是高度，是**两层框套在一起** —— 40px 的带里又套了一个带外框和
底色的药丸，中间只剩 2～4px。去掉那层框，两个标签直接把整条带撑满，
选中的用琥珀色文字 + 一条 2px 的下划线。

**结果反而更好**：不加高、正文一点不少，而**点得着的高度从 28/30px 变成 39px**
（整条带减去边框）—— 比加高到 52px 的方案还大。

### 实测（浏览器里走真实流程）

| | |
|---|---|
| 两条线 | 1400px 仍是 44 / 84，375px 仍是 74 / 114，三列都齐 ✅ |
| 点得着的高度 | 39px（原来 28 和 30）✅ |
| 外框 | 没有了 ✅ |
| 切换 | 生词↔句摘、阅读↔复习 来回点，选中态跟着走 ✅ |
| 375px | 文字没被压、页面不横滚 ✅ |
| 控制台 | 插标记后重载，零报错 ✅ |

测试 413 项全过；`tsc -b --force`、`npm run build` 都通过。

### 一条经验

用户说「挤」的时候，第一反应是「那就加高」。**但加高要花正文空间，而挤的根源
未必是高度。** 这次量下来，真正吃掉空间的是那层多余的框 —— 拆掉它，
既不花空间，点击区域还变大了。**先问「挤的是什么」，再问「要不要加高」。**

### 复习页那一行也拆掉盒中盒（同日）

用户接着报：复习模式下「英 / 中 / AI 填充 / 词」那一行**竖着挤**。

量出来：这一行横着其实很空（375px 上左右两组之间空着 **100px**、不折行），
挤的是竖向 —— 40px 的带里，英/中 32px、AI 填充 30px，上下各只剩 **4～5px**。
而且**这是全 App 最后一处「盒中盒」**：两个侧栏刚拆干净，这里还剩三个实心块并排。

改法和侧栏那次同一件事：**「英 / 中」没开遮挡时不给底色**，只留图标和字；
开着才填琥珀。于是平时这一行只剩「AI 填充」一个实心块 ——
而它实心是有含义的（还差几条没填全）。

好处：不加高（不破坏三列对齐、不吃正文空间）、按钮一个像素都不用缩，
而且遮挡一开那块琥珀色反而更跳得出来（周围没有别的灰块干扰了）。

实测：常驻实心的块从 3 个降到 **1 个**；开关来回点，琥珀态跟着走；
这一行仍是 40px、底边仍在 114（375px），不折行。

### 截图第五次骗人，这次是遮罩挡住不重绘

改完截图一看，英/中 **还是灰底的**，以为没生效。读 `className` 是
`text-ink-muted hover:bg-stone-100`（没有常驻底色），读 computed 是透明 ——
把首启协议卡那两层遮罩临时藏掉再截，灰底就没了。

**遮罩盖着的那一片根本不重绘**，截图和 computed 一起骗人。
这和第「hover 只对鼠标」那节里整理按钮读到透明是同一个机制。

### 还有一条自己写错的检查

数「这一行还剩几个实心块」时正则写成 `/bg-(amber|stone)-\d/`，
把 `hover:bg-stone-100` 也算进去了，数出来 4 个（等于没改）。
改成只认不带 `hover:` 前缀的类名之后是 1 个。**验证脚本本身也会骗人。**

---

## 三十七、~~复习页那一行竖着挤~~ ✅ 2026-09-02 已验收

用户说：复习模式下顶上那一行挤 —— 中英遮挡、AI 填充检测到空白卡的形态。

### 我第一轮想歪了，记一笔

我先去量横向，发现 375px 上左右两组之间**空着 100px**、也不折行，
于是回话说「不是横向挤」。用户回：**「我就是说的竖向挤啊」**。

**人家一开始说的就是竖向。** 量对了方向没用，答错了问题。
下次先确认「挤在哪个方向」，别拿一堆数字去纠正一个我自己假设出来的前提。

### 第二轮又只改了一半

第二轮我把「英/中」**没开启时**的灰底去掉，以为这样就只剩 AI 填充一块实心。
用户再回：**「AI 填充那个琥珀色背景就是很显挤啊，中英遮挡的背景在高亮的时候也很挤」**。

对的。根子不是「平时有没有底色」，是**只要 40px 里出现实心块就挤** ——
32px 的块塞进 40px，上下各剩 4px，不分开着关着。

### 正解：这一行一个实心块都不留

状态不再靠「填一块底色」表达：

| | 从前 | 现在 |
|---|---|---|
| 英 / 中 关着 | 灰底 + 灰字 | 灰字，无底 |
| 英 / 中 开着 | **琥珀底** + 深字 | **琥珀字** + 图标从睁眼换成闭眼（两个信号）|
| AI 填充 有空白 | **整块琥珀底** + 白字 | 琥珀字 + 后面直接跟个数字 |
| AI 填充 无空白 | 灰字 | 灰字（不变）|
| 词 / 句 | 灰字 | 灰字（不变）|

**信号没丢，只是换了载体。**

中间试过把数字装进一枚 18px 的小圆点，用户看完说**不用**：直接把数字挂在
「AI 填充」后面就行。他是对的 —— 圆点是又给这一行加回了一个实心块，
而这一节要做的正是把实心块清干净；少一层东西，也少一处要对齐的地方。
（顺带查过：圆点装两位数会从正圆变成胶囊，1→18px、12→19.3、99→21.9、123→26.2，
包得住但形状会变，又是一处得操心的事。去掉之后这些统统不存在。）

按钮从 32px 收到 28px，上下各剩 6px（原来 4px）。

### 实测（浏览器里走真实流程，375px）

| | |
|---|---|
| 常驻底色 | 英 / 中 / AI 填充 三颗**一个都没有**了 ✅ |
| 唯一的实心块 | 那枚 18px 圆点 ✅ |
| 带高 | 还是 40px，不折行、三列对齐没动 ✅ |
| 遮挡开着 | 字色 `rgb(180,83,9)`、图标 `eye-off` ✅ |
| 遮挡关着 | 字色 `rgb(92,92,92)`、图标 `eye` ✅ |
| 测试 | 413 项全过；`tsc`、`build` 通过 |

### 又是截图骗人（第五次）

改完截图，看到英还是琥珀底、AI 填充还是整块琥珀 —— 差点以为改动没生效。
其实 className 里那些 `bg-*` 早就没了。重载之后再拍就对了。

后来验「遮挡开着」时又撞一次：图标已经换成闭眼、字却还是灰的 ——
那是 `transition-colors` 还没走完的中间帧。读 computed 等它停下来，是 `rgb(180,83,9)`。

**这个环境里：className 是真的，截图和 computed 都要等、都会骗人。**

### 最后一处：圆点也去了

见上表。**这一行现在一个实心色块都没有**，四颗按钮全是 28px 的纯文字，
上下各剩 6px。状态全靠字色（灰 / 琥珀）和图标（睁眼 / 闭眼）表达。


---

## 三十八、~~主题只染阅读页，复习页不跟着变~~ ✅ 2026-09-02 已验收

用户报的：设置里三种纸色（标准/青灰/暖白）换了，复习模式纹丝不动。

**根因不是坏了，是当初没接过去。** 配色表写在 `LyricEditor` 内部的一个局部常量里，
全 App 只有阅读页那两个容器够得着；复习页底色写死 `bg-paper` #fbfcf8。
量出来：青灰主题下阅读区 #f8f9f8、复习区 #fbfcf8，三档一个样。

改法：配色表搬进 `src/components/theme.ts` 给两边共用，复习页底色改用同一份。
三个文件、20 行。

### ⚠️ 这一节我返工了两次，教训比改动本身重要

**第一版我"顺手"多改了三样**：卡片边框加深一档、阴影 `shadow-sm` 加重成 `shadow`、
顶栏和两条带也接上主题（理由是青灰下顶栏会留一条白缝）。理由都成立，
但**用户一样都没要**。他的原话：「谁让你顺手改顶栏了，它们不参与颜色更换」
「阴影太重」。

**第二版我只退了阴影和顶栏，留着边框没退**，用户说「还是感觉很怪」——
因为边框加深在眼睛里和阴影加重是同一件事，我却只回答了「阴影退了」。

**最后全部撤回，重做一遍，只改底色。** 用户总结：
「我从头到尾就一个要求，把这个改主题的设置从阅读模式延伸到复习模式里，
不要扯别的，你也别测量是什么颜色，直接带过去就行了。」

记在这里：**这个项目里"顺手"两个字是危险信号。** 观感上看着该一起改的东西，
用户不一定这么看，而且他分得很清 —— 界面的带是「界面」，正文区才是「纸」。

### 划下来的界线

主题只染**正文/卡片所在的那块滚动区**。不参与换色的：左右侧栏、
顶部那两条带（44px 标题栏、40px 模式栏）。这条写进了 `theme.ts` 的注释，
免得以后又有人接上去。

---

## 三十九、~~强调色：五种可选~~ ✅ 2026-09-02 已验收

从前通体琥珀，写死在 **119 处 `amber-N`**、跨 10 个深浅档。用户要「全都要，
放在设置里自行切换」。

**做法**：类名统一成 `accent-N`，真正的色值走 CSS 变量（`src/index.css` 的十档色阶），
切换只改 `<html data-accent>` —— 不重新编译、不刷新页面，119 处一起变。
以后加第六个颜色只要加十行色值，一行代码都不用动。

五档：**琥珀**（原样，老用户不变）/ **墨蓝** / **松绿** / **朱红** / **石墨**。

### 三个当时得想清楚的地方

- **变量存 RGB 分量**（`245 158 11`）而不是 `#f59e0b`。代码里有 19 处带透明度的写法
  （`accent-500/30` 这类），存十六进制它们会全废。验过编译产物：
  10 个带透明度的类都在，长成 `rgb(var(--accent-200) / .6)`
- **设置里那五颗色点用死色**，不能用 `accent-600` 类名 ——
  否则五颗点会一起变成当前选中的颜色，每颗必须显示它自己代表的色
- **选中环用中性的 ink 色**。环若也用强调色，选中琥珀时环压在琥珀点上就看不见了

### 选色是拿网页定的，不是拿文字描述定的

第一版我做了个只有零碎片段的对照页，用户直接说「你倒是演示给我看啊，
你网页上就写几个字我看什么」。重做成**整屏对照**（五个手机并排、
能切复习页/阅读页和三种纸色）之后当场就定了。

留着：[主题色方案.html](主题色方案.html)。**以后要用户挑观感，直接上整屏对照，
别给零件。**

### 一处用词改动

「主题」这个标签改成了「**纸色**」—— 现在有两样颜色设置，「主题」指哪个说不清。
用户没否。

老用户存的设置里没有 `accent` 这一格，落回琥珀，纸色不受影响（验过）。

### ⚠️ 改了 tailwind.config 就得拿构建产物验

开发服务器**不会**重新生成工具类（`accent-*` 一个都没有），
`.claude/launch.json` 为此加了一条 `lyric-vocab-preview`（跑 dist）。
这台机器上它常被"端口被另一个 chat 占用"挡住，那就退而求其次：
把 `dist/assets/*.css` 里的规则注进跑着的页面，验的仍是真类名 + 真变量。

---

## 四十、~~弹窗点外面关不掉 + API Key 默认明文~~ ✅ 2026-09-02 已验收

用户报的三条。

### 一、AI 填充 / AI 划词 的弹窗点外面关不掉

设置弹窗一直是能的，这两个的遮罩只是块背景板、没接点击。改成同一个做法：
遮罩接 `onClick`，卡片 `stopPropagation`。

两处例外，都是照着这两个弹窗**已有的**规矩定的，不是新发明的：

- **跑着的时候不响应**。右上角那颗关闭键此时本来就藏起来了（`{!running && ...}`），
  点外面要是能关，等于给它开了个后门，手一滑就把正在跑的任务打断
- **安卓返回键不动** —— 它跑着时按下去是「取消任务」，那是明确的一次按键，不算误触
- **在「AI 设置」子屏时退回上一屏**，不是整个关掉，和设置弹窗一致

### 二、眼睛图标反了

从前跟**动作**走（明文时画划掉的眼睛 = 「点我遮住」）。改成跟**当前状态**走：
遮住时画划掉的眼睛，明文时画睁着的眼睛。`aria-label` 仍然描述动作，没动。

### 三、Key 默认明文，保密性为 0 —— 但不能一刀切

用户说得对，但**默认明文不是随手写的**，注释里记着理由：
**安卓 WebView 对密码框常常不给「粘贴」菜单**（第十四节那个坑），
而这格永远是靠粘贴填的。一律默认遮住可能把粘贴又弄坏。

所以按「有没有东西可保」分：
- **框里已存着 Key** → 默认遮住（此时你是来看/来改的）
- **框是空的** → 默认明文（此时没什么可保，而且正是要粘贴的时候）

### 实测（浏览器里走真实流程）

填充窗点空白关掉、点卡片里面不关；子屏点空白退回「一键填充」；划词窗点空白关掉。
空框 → `type=text` + 睁眼；点一下 → `type=password` + 划掉的眼睛；
造一个已存 Key 的状态重进 → 默认 `password` + 划掉的眼睛。

**⚠️ 「跑着时不响应」这一条验不了**，要真发一次 AI 请求才进得到那个状态。

---

## 四十一、~~App 图标还是 Capacitor 的默认图标~~ ✅ 2026-09-02 已验收

用户说「图标有点不直观」。查下来问题比这句话严重：**手机桌面上显示的那个蓝色 X，
是 Capacitor 打包时自带的默认图标，从来没被换过**，和这个 App 毫无关系。

另有一套网页图标（`public/*.svg`）是琥珀色的双八分音符 —— 只在浏览器/PWA 里用得到，
安卓桌面看不见；而且「音符」这个意象也早就不准了（小说、文章都能读）。

### 定稿：衬线 A + 琥珀下划线

**选它的依据是缩到 48px 之后的样子，不是大图好不好看。**
一次渲了六版并排比较，每版底下都放一个 48px 的真实大小：

| 方案 | 48px 下 |
|---|---|
| 三行灰线，中间一行划琥珀 | 糊成一团 |
| 纸页 + 一行高亮 | 细节太多，全糊 |
| **衬线 A + 琥珀下划线** | **唯一还完整的** |
| 引号 + 下划线 | 认得出，但像通用读书笔记 |
| 音符（原网页版） | 认得出，意思不对了 |
| 音符 + 下划线 | 下划线缩没了 |

下划线是这个 App 的签名动作，和正文里生词底下那道线是同一套语言（用的也是同一个
`accent-500`）。

### 换了哪些文件

- `mipmap-{mdpi..xxxhdpi}/` 三套 × 五种密度：`ic_launcher`（方形 48~192）、
  `ic_launcher_round`（圆形）、`ic_launcher_foreground`（自适应前景 108~432）
- `values/ic_launcher_background.xml`：纯白 → 纸色 `#FFFEFC`
- `public/` 那三个 svg 一并换成同一个图案

**自适应图标的安全区只有中心 66%**（直径 72 / 108），边上会被各家启动器裁掉，
所以前景层不放大，传统方形/圆形图标才放大到 1.2 / 1.15 填满。

`drawable/ic_launcher_background.xml` 和 `drawable-v24/ic_launcher_foreground.xml`
是 Android Studio 模板留下的矢量图，自适应图标走的是 `@color` + `@mipmap`，
这两个文件没人引用，没动它们。

### 生成方式

图标是脚本按 SVG 渲染出来的（临时目录的 `appicon.mjs`，用 `@resvg/resvg-js`）。
要改图案或换颜色，改脚本里 `art()` 一处，五种密度十五个文件一起重出。

### 这一轮顺带产出的东西（都在仓库根目录，未纳入版本管理）

- `宣传图/` —— 八张 1080×1440 的介绍图，小红书/朋友圈直接能发
- `截图/` —— 用户的九张真机截图，是上面那八张图的素材

---

## 四十二、~~书库：App 里搜古登堡，选中直接下载导入~~ ✅ 2026-09-02 已验收

用户提的问题不是某个 bug，是整体感受：**「这个 app 太工具了，你得自己把东西导入进去
才能使用，就像一个扳手一样，没内容只是个杠杆。」**

### 先纠正一件我上一轮说错的事

我一开始查完说「古登堡在你这儿打不开，所以联网找书这条路走不通」，**这是错的**。

那是**用户电脑上挂着的代理**造成的（本地 7897 端口），代理节点连不上古登堡。
把代理关掉直连，一切正常：

| 测什么 | 挂着代理 | 关掉代理直连 |
|---|---|---|
| gutenberg.org 主站 | 超时 | **HTTP 200，1.1 秒** |
| 下载《傲慢与偏见》epub | 超时 | **558KB，2.8 秒，验过是合法 EPUB** |
| standardebooks.org | 200 | 200 |

**教训：在这台机器上测外网可达性，必须 `curl --noproxy '*'` 再测一遍。**
挂着代理测出来的结果既不代表用户的电脑，更不代表用户的手机。

### 顺带否掉了「自建服务器中转」

用户问「能不能建一个服务器，内部搜索下载并导入」。方向对，但实测发现：

- **`lyric-vocab.vercel.app` 不挂代理打不开，超时。** `vercel.com` 也一样

也就是说，**他自己的 Vercel 反而比古登堡更难够着**。中转服务器这条路，
用现成的 Vercel 是最差的选择。而且真做了还要续费、会挂、要维护 ——
这个 App 现在是纯本地的，能不引入后端就别引入。

### 最后的做法：目录随身带，搜索在本地，只有下载才联网

- 古登堡有一份官方完整目录（`https://www.gutenberg.org/cache/epub/feeds/pg_catalog.csv`），
  9 万条、21MB。`scripts/buildGutenbergCatalog.py` 把它砍成「编号 + 书名 + 作者」的
  英文正文书，**61606 本**，gzip 后 **1.94MB**，随 APK 打包
- 搜索完全在本地做：**加载 121ms，每次搜索 3～14ms**（6 万条挨个 indexOf，
  凑够 60 条就停）。不联网也能翻，网络差也不会卡在搜索这一步
- 选中哪本 → 按编号拼地址 → 原生网络下载 → **包成 File 交给现成的导入器**

最后这条是关键：`importFile(file)` 本来就只认 File，所以「从书库下载」和
「从手机选文件」在导入层眼里完全没有区别，章节切分、编码、异常提示一行都没重写。

### 下载地址故意不用带跳转的那个

`/ebooks/{id}.epub.noimages` 会 302 跳到 `/cache/epub/{id}/pg{id}.epub`。
**直接用后者**，少一次跳转就少依赖一层「原生网络跟不跟随重定向」的跨平台行为 ——
这类差异正是这个项目栽过跟头的地方（第十节朗读、第十四节粘贴）。
抽查 16 本（编号 1 到 70000）全部 200。

### 顺手解决的：古登堡的版权声明

每本书正文前后各夹一大段固定文字（书名页 + 许可条款 + 完整授权协议 + 捐款说明）。
不剥掉的话第一屏全是「The Project Gutenberg eBook of…」，而且这些法律文本
还会被一键划词当成正文去挑生词。

短篇尤其离谱 —— **林肯葛底斯堡演说正文才 34 行，连着许可证一共 403 行，
正文占不到一成。**

古登堡自己留了界桩，几十年没变过：

```
*** START OF THE PROJECT GUTENBERG EBOOK <书名> ***
*** END OF THE PROJECT GUTENBERG EBOOK <书名> ***
```

`src/importers/gutenberg.ts` 认这两行。**找不到界桩就原样返回** ——
用户自己写的东西绝不能瞎删。放在导入层而不是书库那条路里，
是因为认的是**文件内容**不是来源，所以用户自己从别处下的古登堡书一样受益。

剥离效果（真书实测）：

| 书 | 剥离前 | 剥离后 | 结尾落在 |
|---|---|---|---|
| 葛底斯堡演说 | 403 行 | **34 行** | shall not perish from this earth |
| 爱丽丝漫游奇境 | 3721 | 3350 | THE END |
| 福尔摩斯探案集 | 11936 | 11566 | 正文最后一句 |
| 弗兰肯斯坦 | 7036 | 6663 | 小说真正的末句 |

### ⚠️ 当场踩的坑：我自己写的转发规则把书目文件劫走了

书目怎么都读不出来，404，返回的却是 6398 字节的 HTML。

根因：我给开发服务器加了 `'/gutenberg'` 这条转发规则，**Vite 的代理按前缀匹配**，
于是 `/gutenberg-catalog.bin` 也被算成古登堡的请求转去了官网，
拿回来的是人家的 404 页面。

改成 `'/gutenberg/'`（**末尾这个斜杠不能少**）就好了。

**这个坑值得记：状态码和内容双双骗人，而且骗的方式和第二十六节
「把网页当录音存了」一模一样。** 所以 `catalog.ts` 的 `decode()` 里加了一道检查 ——
目录每行都是「数字 + 制表符」开头，对不上就直接报错。
没有这道检查的话，HTML 会被当成目录一行行切开，搜索永远搜不到东西，
界面上却一点错都不报，**这种静默失败最难查**。
`download.ts` 里同理认 epub 的 zip 文件头 `PK\x03\x04`。

### 还有一个小坑：`.gz` 这个扩展名

一开始文件叫 `gutenberg-catalog.txt.gz`。虽然最后证明 404 是转发规则的锅，
但还是改成了中性的 `.bin` —— 有些静态服务器会把 `.gz` 当成「压缩变体」特殊对待，
而手机上是 Capacitor 的本地服务，行为未必和 Vite 一致。
反正 `decode()` 靠文件头自己判断压没压，扩展名不必带信息。

### 界面

左侧栏底部从三格变**四格**：导入 / 书库 / 回收站 / 设置。
量过（375px 窄屏）：每格 61.5×56px，最宽的「回收站」文字 33px，都不溢出。

**⚠️ 这个位置是临时的，等用户在手机上看过再定。** 书库和导入是同一类事
（都是往里放内容），挤在这排四个平权的格子里未必是最终形态。

### 实测（浏览器里走真实流程）

- 书目：61606 本，加载 121ms
- 搜索：austen 36 条 / pride prejudice 3 条 / sherlock holmes 8 条 / 乱敲 0 条，3～14ms
- 点「书库」→ 搜 gettysburg → 11 条结果 → 点第一条 → 下载 → 导入 → 弹窗自动关闭
- 打开导入的那篇：正文是干净的演说全文，**无版权声明**，含名句
- 单测 **440 项**全过（新增 27 项）；`tsc -b --force`、`npm run build` 都通过
- APK 6.56MB，确认 `assets/public/gutenberg-catalog.bin` 在包里

### ⚠️ 我验不了的，等真机

1. **手机上能不能连上古登堡** —— 我是在电脑上关掉代理测的，约等于手机不挂梯子，
   但**不等于**。手机用流量（4G/5G）时运营商线路可能不一样。**这一步没过后面都白搭**
2. **构建产物里 service worker 会不会截走书目请求** —— 预览服务器没起来，只在
   开发服务器验过。理论上不会（workbox 的兜底只管 navigate 请求，
   而且 `.bin` 不在 `globPatterns` 里），而且真被截走会当场报错不会静默
3. 古登堡是不是一直稳定，还是偶尔抽风，得多用几次才知道

### 内容范围（用户已知情并同意）

**6.1 万本全是 1929 年以前的公版书** —— 奥斯汀、狄更斯、福尔摩斯、马克·吐温。
书是好书，但语言偏旧，会学到一些现在不太用的词。

不过非小说很多：演讲集、游记、书信、科普，语言比十九世纪小说现代得多。
目录里编号最靠前的几本正好是这类 —— 独立宣言、权利法案、肯尼迪就职演说、
林肯葛底斯堡演说。

### ⚠️ 第一版真机上「每一本都下不了」（同日，用户报的）

搜索、界面、目录加载全正常，**下载全军覆没**，报：

```
unexpected end of stream on com.android.okhttp.Address@f3518f8e
```

用户当时在 **4G/5G** 上（截图里状态栏能看到）。

#### 排除掉的：不是编码的问题

先查响应头（`curl -sI --noproxy '*'`）：

```
content-length: 156835
accept-ranges: bytes
content-type: application/epub+zip
```

干净得很 —— 有确定长度、不分块、不压缩。所以不是「传输编码没处理对」那一类。
`unexpected end of stream` 是 okhttp 在读响应时提前撞到 EOF，
意思是**连接建起来了、传到一半被掐断**。这是长连接在这种网络下活不下来。

顺带确认了另一件事：古登堡**一个 `Access-Control-*` 头都不发**，
所以「改用网页自己的 fetch 绕开 CapacitorHttp」这条路走不通，只能走原生网络。

#### 改法：一次只要一小段

三个源实测都支持分段取（`accept-ranges: bytes`，`-r 0-1023` 都回 206、
拿到精确的 1024 字节、头是 `PK`）。于是：

- **每段 256KB**，请求短、连接活得久
- **单段失败重试 3 次**，间隔递增（400/800ms）—— 掐断是随机的，重试往往就过去了
- 掐断了只重来那一段，不是整本重来

#### 顺带加了备用源

| 源 | 同一本书的字节数 | 分段 |
|---|---|---|
| `www.gutenberg.org` | 156835 | 206 ✓ |
| `gutenberg.pglaf.org` | 156835 | 206 ✓ |
| `aleph.gutenberg.org` | 156835 | 206 ✓ |

主站不行就依次往下试。（另有 `gutenberg.nabasny.com` 也能连，
但同一本书只有 81545 字节，**内容版本对不上，故意不用** ——
不同版本混着来，以后查问题会疯掉。）

⚠️ `aleph.gutenberg.org` 的**根路径**是超时的，但 `/cache/epub/...` 这个路径通。
测可达性时别只 ping 首页。

#### 进度条（顺带就有了）

分段下载天然知道下了多少，所以把进度显示出来了：
先是「正在下载，书越厚等得越久…」，拿到第一段后变成「正在下载… 256 KB / 348 KB」
加一条进度条。用户至少知道它在动。

#### 错误提示改成人话

第一版把原始异常直接甩在界面上，用户看到的是
`unexpected end of stream on com.android.okhttp.Address@f3518f8e` ——
这句话对他没有任何用处。现在按类型翻译，连不上时的提示会**建议换个网络试试**
（Wi-Fi 和流量的线路不一样，这是真的有用的下一步）。

#### 实测（浏览器里走真实流程）

- 分段下载：《傲慢与偏见》558381 字节，**分 3 段**，815ms，进度回调
  `262144/558381 → 524288/558381 → 558381/558381`，成品是合法 epub
- 进度界面：给下载请求人为加延时后抓到帧 ——
  「正在下载… 256 KB / 348 KB」，进度条宽 `73.6238%`
- 单测 **445 项**全过（新增 5 项，钉住 `Content-Range` 解析：
  大小写、末尾空白、总长为 `*` 时必须返回 null 而不是 0，
  当成 0 会立刻判定「已下完」交出一个空文件）

#### 测的时候栽的两个坑，记一笔

1. **采样选择器一直命中左侧栏。** 我用
   `[...document.querySelectorAll('.fixed')].find(e => /书库/.test(e.innerText))`
   找弹窗，但**左侧栏底部那个「书库」按钮也含这两个字**，而且排在 DOM 更前面。
   于是连着四次采到 0 帧，差点以为进度界面没渲染。
   正解是用弹窗独有的东西定位：`document.querySelector('input[placeholder*="书名"]').closest('.fixed')`
2. **`location.reload()` 之后「用户协议」弹窗会挡住一切**，脚本点不到东西。
   重载后要先点「同意」。

#### 真机结论：分段有效；流量能下，但慢得多

用户 2026-09-02 验过，下了两本（林肯演讲、彼得潘），进度条也正常。

⚠️ **这里我先写错过一次结论。** 用户第一次反馈是「确实是得 wifi」，
我据此在文档里写死了「流量下不了」。他随后纠正：
**「流量好像也能下，但是慢得多」**。

所以准确的说法是：

| 网络 | 第一版（整本一个请求） | 第二版（分段 256KB） |
|---|---|---|
| 4G/5G | 失败 | **能下，但慢** |
| Wi-Fi | 未试 | **正常** |

分段确实解决了「长连接活不到传完」这个问题，流量上也过得去了，只是慢。
**教训：用户说「得用 X」不等于「不用 X 就不行」，别把一句话推到极端再写进文档。**

界面上照用户要求加了一句提示：「建议连 Wi-Fi 下载，用流量会慢很多」，
放在书库弹窗还没开始搜的那一屏。下载失败时的提示也跟着改了措辞 ——
从前暗示流量不行，现在是「网络不稳，没下完。连上 Wi-Fi 再试一次」。

### 还没做的

- **中文书名搜不了**（目录里只有英文原名）
- **下过的书不记得**，同一本可以重复下载导入
- **目录不会更新**，要更新得重新跑脚本再打包

---

## 四十三、~~书库接上澳洲站：终于有二十世纪的书~~ ✅ 2026-09-02 已验收

用户读完第四十二节之后提的：**「古登堡的资料实在是有些老，很多现代书都没有。」**

### 先把能连上的源全测了一遍（关代理测的）

| 源 | 结果 |
|---|---|
| VOA 学英语 / BBC / Open Library | **全部超时** ✗ |
| Standard Ebooks | 通，但和主站是同一批老书，没有增量 |
| **gutenberg.net.au（澳洲站）** | **通** ✓ |

澳洲站的价值不在网络，**在版权年限不一样**：它按「作者去世满 50 年」算，
美国主站按「1929 年以前」算。这一条差别把整个二十世纪前半段放了进来。

**《1984》和《动物农场》都在**，还有《向加泰罗尼亚致敬》《缅甸岁月》
《上来透口气》、五十篇奥威尔散文；另有伊迪丝·华顿、辛克莱·刘易斯、
尤金·奥尼尔、詹姆斯·希尔顿。

（顺带一提：中国的著作权保护期同样是作者去世后 50 年，口径和澳洲站一致。
这不是法律意见，只是把规则记下来。）

### 用户拍板的两件事

1. **两个源合成一个书库搜**，不做来源切换 ——「用户又不知道哪个源里有什么书」
2. **不在每一条上标来源**（「哪里写的下啊」），
   而是在弹窗那段提示里一起提一嘴，和「建议 Wi-Fi」那句并列

### 只收纯文本那一半，网页版不要

澳洲站 6143 本，格式分两种：

- **1786 本给纯文本** —— 现成的 txt 导入器直接能吃，几乎没有额外工作
- **4118 本只有网页版** —— 要写一个 HTML 导入器才能用

试过能不能从网页地址推出文本地址（`2400251h.html` → `2400251.txt`），
**推不出来**，新书根本没有文本版（抽 12 条，11 条 404）。

**故意不做那 4118 本**，因为查清楚了里面是什么：按作者排前几名是
Fred Merrick White（328 本）、Edgar Wallace（222）、E Phillips Oppenheim（131）、
Robert E Howard（123，《野蛮人柯南》）、Zane Grey / Max Brand（西部小说）。
**是一批 1900～1940 年代的类型小说**，加上澳洲本地作家。
花一个 HTML 导入器去换柯南·道尔同辈的通俗小说，不划算 ——
而真正有分量的那些（奥威尔那批）恰好都在不用额外工作的纯文本这一半里。

⚠️ **澳洲站 2024 年底已停止新增**，站点会一直留着，但不会再长。

### 目录格式改了：多两格

从前是 `编号\t书名\t作者`，现在是 **`源\t定位\t书名\t作者`**：

- `g` 美国站，定位是书的编号（地址能拼出来），给 epub
- `a` 澳洲站，定位是站内路径（**它的地址没规律，只能整条存着**），给 txt

合并后 **63392 本**，包体从 1.94MB 到 1.98MB，只涨了 0.04MB。
澳洲站排在前面 —— 搜同一个作者时，二十世纪那本更可能是用户想要的。

`decode()` 里那道内容校验跟着从 `/^\d+\t/` 改成 `/^[ga]\t/`。

### ⚠️ 搜索一度把「源」和「路径」也算进去了

`folded`（拿来匹配的小写版）从前是**整行**。加了第二个源之后，
搜「txt」会把 1786 本澳洲书一次全捞出来，搜「ebooks」同理 ——
因为澳洲站的定位就是 `ebooks01/0100021.txt` 这样的路径。

改成只取第三格往后（书名 + 作者）。**是写测试时撞出来的**，
`searchIn(cat, 'the', 2)` 那条断言先挂了才发现。已加测试钉住。

### 澳洲站的版权声明是另一套格式

没有主站那种 `*** START OF ***` 界桩。它的头部固定以
**「To contact Project Gutenberg of Australia go to gutenberg.net.au」**
收尾，之后往往还重复一次 `Title:` / `Author:`；结尾是一行光秃秃的站名。

`src/importers/gutenberg.ts` 里加了 `stripAustralian`，和美国站那套并存 ——
美国站的界桩优先，找不到才试澳洲规则，两套都没有就原样返回。

### ⚠️ 这一段踩了三个坑，都是真书验出来的

1. **先切头还是先找尾，顺序要紧。** 头部第一行也是「Project Gutenberg Australia」，
   和结尾那行一模一样。先找尾的话，短篇（整本十来行）往回扫的窗口会一路够到
   头部那行，**把整章切成空的**。改成先切头，那行就不在了
2. **「找不到头就整个放弃」是错的。** 拿真的《1984》验出来的：
   txt 导入器按「Chapter N」切章时，会把第一个章节标记**之前**的内容整段丢掉 ——
   头部声明就在那里，于是章节里根本找不到头部标记，
   结果尾巴那行站名原样留在了书的最后。**头和尾必须各自独立判断**
3. **头部之后还可能有一整行分隔横线。**《Shooting an Elephant》的头部收尾是
   「To contact… / 空行 / 一长串减号 / 空行 / Title: / Author:」，
   清理循环卡在横线那行，后面的 Title / Author 就跟着留下来了。已认横线

### 下载层按源分流

- 地址：美国站 `/cache/epub/{id}/pg{id}.epub`，澳洲站直接用目录里存的站内路径
- 备用源：美国站三个轮着试，**澳洲站只有一处，没有镜像**
- 校验：epub 认 zip 文件头 `PK\x03\x04`；**txt 没有文件头可认，改成反过来查** ——
  开头像 HTML（`<!doctype` / `<html` / `<?xml`）就不要，含 0 字节也不要。
  防的还是那件事：别把运营商插页、WiFi 登录页那种 200 的 HTML 当成书收下来
- 包成 File 时扩展名跟着源走，**包错了解析会当场炸**（txt 喂给 zip 解析器）
- 开发转发加了 `/gutenberg-au/`；前缀比 `/gutenberg/` 长，vite 按最长匹配，不打架

`fetchBookEpub(id)` 改成 `fetchBook(book)`，`asEpubFile` 改成 `asBookFile` ——
两个源的编号各排各的会撞，界面里「正在下载哪一本」也从编号改成了「源:定位」。

### 实测（浏览器里走真实流程）

五本书，两个源都验：

| 书 | 源 | 章数 | 开头 | 版权残留 |
|---|---|---|---|---|
| Nineteen eighty-four | 澳 | 30 | It was a bright cold day in April… | 无 |
| Animal Farm | 澳 | 10 | Mr. Jones, of the Manor Farm… | 无 |
| Homage to Catalonia | 澳 | 18 | In the Lenin Barracks in Barcelona… | 无 |
| Shooting an Elephant | 澳 | 1 | In Moulmein, in lower Burma… | 无 |
| Pride and Prejudice | 美 | 15 | （美国站那条路没坏） | 无 |

《1984》开头正是那句 “the clocks were striking thirteen”，结尾停在 THE END。

界面：点书库 → 搜 orwell → 出奥威尔的书 → 点《1984》→ 下载（3 段）→
导入 → 弹窗自动关 → 打开正文干净。

单测 **456 项**全过（新增 5 项澳洲剥离 + 2 项搜索范围 + 4 项 txt 校验）。

### 提示那段量过

加完来源之后提示涨到 6 行、占弹窗高度 **59%**，还有一行只剩「的书」两个字挂着。
收紧成 4 行、134px、**53%**，没有孤字。（375px 窄屏量的。）

### ⚠️ 澳洲站第一版真机上全下不了：安卓不许明文 HTTP

用户报的，错误很直白：

```
Cleartext HTTP traffic to gutenberg.net.au not permitted
```

**安卓从 9（API 28）开始默认禁止明文流量。** 我把源写成了
`http://gutenberg.net.au` —— 因为查资料时那个站的地址就是 http 开头，
我照抄了，没想到打包进 App 会被系统直接拦掉。

**这一类错误只在真机上才会暴露**：浏览器里走的是 vite 开发转发，
用什么协议都看不出问题。

改法很简单 —— 澳洲站其实支持 https（验过，同一本书 169718 字节，
和 http 一模一样）。顺带发现 `aleph.gutenberg.org`（美国站的第三备用）
**只有 http，试过 https 连不上**，所以它在安卓上永远用不了，直接删掉 ——
留着只会白白多等一轮超时。

美国站现在两个源：`www.gutenberg.org`、`gutenberg.pglaf.org`，都是 https。

**加了测试钉住**：`US_HOSTS` / `AUS_HOSTS` 里每一条都必须以 `https://` 开头，
且两个列表都不能为空。单测替真机守这一关，因为浏览器里守不住。

### 提示里那句版权解释删了（用户要求）

原来三行里有一句「澳洲站按『作者去世满 50 年』算，有二十世纪的书」，
用户说「这个多余了」。删掉，现在是三行：

> 共 63,392 本版权已过期的免费英文书
> 来自古登堡计划的美国站与澳洲站
> 建议连 Wi-Fi 下载，流量会慢很多

### 还没做的

- **那 4118 本网页版**，要 HTML 导入器（结论是不划算，见上）
- **中文书名搜不了**（两个源的目录都只有英文原名）
- **下过的书不记得**，同一本可以重复下载导入
- **目录不会更新**，要更新得重新跑脚本再打包；澳洲站反正也不再新增了

---

## 四十四、~~回收站一键清空~~ ✅ 2026-09-02 已验收

用户提的：回收站里攒了 95 项，一条条点太累。

### 界面

回收站弹窗**底部单独一行**「清空回收站」（红字，整行宽），
点了跳二次确认：**「确定要清空回收站吗？共 N 项，此操作不可恢复。」**

- **回收站为空时整行不出现**，免得摆一个点了没用的东西
- 二次确认和单条「彻底删除」**共用同一个弹窗** —— 问的是同一件事（不可恢复），
  只是措辞和范围不同。省一套弹窗，安卓返回键那套优先级也不用再加一层
- 窄屏量过：按钮 325×36px

#### ⚠️ 第一版放在标题栏里，挨着关闭叉，用户当场指出来了

原话：「那个清空按钮离关闭按钮有点太近了吧」。**量出来两者只隔 4px** ——
一个是「把东西全删光」，一个是「关掉窗口」，手指按下去差之毫厘。

挪到底栏之后隔着整个列表：**垂直距离 142px**，误触基本不可能。

这条又一次印证了那个规律：**观感上的事，用户先看出来的多半是对的。**
我当时只想着「标题栏右边是放操作的地方」，没想过这两个动作的后果差多远。

### 存储层单写了一个函数，没有循环调现成的

`emptyTrash()`（`src/storage.ts`）。**没有**循环调 `deleteBookPermanently` /
`deletePagePermanently`，因为那两个函数每调一次都是一整轮
「读全量 → 改 → 落盘」。用户回收站里有 95 项，循环调等于连写上百次，
慢不说，**中途被打断还会留下删了一半的状态**。改成一次遍历、一次落盘。

顺带清掉被删文档的 `notes` 和 `annotations` —— 不清就是一堆再也够不着的垃圾。

### 一条容易删错的语义，保留了

**删掉一个文库时，它下面「没有被删」的文档不跟着消失，而是提升到根级**
（和 `deleteBookPermanently` 一致）。

正常情况下遇不到 —— `moveBookToTrash` 会把文库连同它的文档一起标记删除。
但**文档可以被单独恢复**（`restorePage`，而且恢复时若父库还在回收站会自动提到根级），
所以「父库在回收站、子文档不在」是真会出现的。这条兜底不能省。

### 测试的重点是「有没有误删」，不是「删干净了没有」

`src/storage.emptyTrash.test.ts`，6 项。这是永久删除，删错了没有任何办法找回来，
所以最重要的一条是**「没删的东西一样不能动」**：文库、文档、笔记、标注四样都断言了。

### 实测（浏览器里走真实流程）

造了 1 个回收站文库 + 2 篇回收站文档 + 1 篇「不该被删的」：

- 角标显示 3，弹窗列出 3 项，「清空」按钮出现
- 确认弹窗文案：「确定要清空回收站吗？共 3 项，此操作不可恢复。」
- **先点「取消」→ 3 项原封不动**
- 再点「清空」→ 确认 → 回收站为空、角标消失
- **刷新页面后复查**：「示范用的文库」没了，**「不该被删的」还在**
- 单测 **465 项**全过（新增 6 项）

### 测的时候踩的两个坑，记一笔

1. **改完代码后，探针脚本和 App 用的可能不是同一份模块。**
   我在控制台 `await import('/src/storage.ts')` 读数据，界面明明已经清空了，
   读出来却还是 3 项。原因是 Vite 热更新后 App 用的是带版本号的
   `/src/storage.ts?t=…`，和我裸导入的那份**是两个模块实例，各有各的缓存**。
   **验持久化要刷新页面之后再看，别信同页面里裸导入的模块。**
2. **存储层是攒一下再落盘的**（见 `storage.ts` 开头）。
   写完数据立刻 `location.reload()` 会把改动丢掉 —— 造测试数据时要等一下再刷。

---

## 四十五、~~六处小优化~~ ✅ 2026-09-02 已验收

用户攒了一张单子，一次提了六条。逐条记要紧的。

### 一、返回键在主界面直接把 App 杀掉

`App.tsx` 里写的是 `exitApp()` —— 那是真退出，进程结束，任务列表里也不剩。
当初这么写是因为「接管了返回键，系统默认行为就不会发生」，怕主界面按返回没反应。

改成 `minimizeApp()`：等同于按 Home，界面留在后台，再点图标回到原来那一屏。
**没加「按两次退出」** —— 退到后台什么都不丢，多一步反而烦。用户认这个理。
同意书那里的「不同意 → 退出」仍是真退出，没动。

### 二、复习页的编辑模式退不出去

那个开关一旦打开就没人关它。实测：开着编辑 → 切阅读 → 切回复习，**还开着**，
而用户以为早退出了。改成离开复习模式自动关掉。
**换文档、换文库时不关** —— 那种情况多半是接着往下整理。

### 三、排序手柄偏上 —— 两轮才对，教训在第二轮

第一轮：手柄按钮 24px 高、顶对齐，图标中线在 199；单词那行行高 28px，
套一个同高的盒子居中，图标中线挪到 201。数字对上了，**用户仍说偏上**。

他是对的。**眼睛对齐的是小写字母那一坨，不是整个行盒。**
量出来（375px、18px Playfair）：基线 208、小写 x 顶 198，
**小写的中线在 203**（字母 o 的中线 203.5）；而 stood 里 d、t 这些上伸部
把行盒中线拽到了 201 —— 跟眼睛看到的差 2px。再往下压 2px 就对了。

### 四、句摘虚线不均匀

**根因：浏览器画下划线是一个子元素一段地画的**，而正文里每个单词、每个空格
都各自是一个 span（要挂长按和按压高亮），于是虚线的节奏在**每个词的接缝处
重新起头** —— 有的接缝并成一根长线，有的空出一大截。
在真页面上并排比过：同样的样式，那一段里若没有子元素，虚线完全均匀。
单词还带 2px 左右内边距，相邻两段的线还会互相重叠。

改法：句摘那条线不再用 `text-decoration`，改成**画在整段上的背景条纹**
（`.sentence-line`，见 index.css）。背景是一次画完的，词与词之间没有接缝。
`padding-bottom` 只是把背景盒往下撑（行内元素的上下内边距不影响排版），
尺寸用 em 跟着字号走，颜色走 `--accent-600` 跟着强调色走。

验过：均匀；换行正常；和短语的波浪线叠在一起不打架；
字号 16→30px 时线与下一行之间还留着 14～19px。

### 五、「文件夹」统一改成「文库」

界面上只有一处说漏了嘴（复习页的空状态），注释里同义的几处一并统一。
`LeftSidebar` 里那句「加号 x=20、文件夹 x=44」说的是文件夹**图标**，没动。

### 六、左滑删除

单独成一节，见第四十六节 —— 这一条前后返工了三轮，最后整个重做了。

---

## 四十六、~~左滑删除重做：不再露红按钮，划到位问一句~~ ✅ 2026-09-02 已验收

用户最初报的是：「划开红按钮后快速往回甩，红色会消失，看着别扭。」
**我连着改了三轮都没打中根子**，第四轮他说「换个设计思路吧」。

### 三轮各修掉了一个真问题，但都不是那个毛病

1. **红色凭空消失** —— 红层只在「手指还在划」时画，一松手立刻不画，
   而卡片还在 180ms 的回弹里。改成收回期间继续画着
2. **换来了边角闪红** —— 红层和卡片一样大、一样圆角，抗锯齿会透出一圈红边。
   红层各边内收 1px，圆角整个落进卡片圆角里面（45° 方向差 1.4px）
3. **那 88px 被重演了一遍** —— 快划时浏览器把几个 pointermove 和 pointerup
   合在同一帧送来，React 一帧都没画过，屏幕上那张卡还停在 -88px；
   下一次提交把它设成 0 还带着过渡，于是松手后又从头演一遍。
   用 `getAnimations()` 抓到证据：松手后生成的过渡是「从很靠左的位置滑到 0，180ms」。
   改法是松手时先把 transform 直接写成手指最后到的位置、读一次 offsetWidth
   逼浏览器认下这个起点

**仍然闪。** 到这一步就该认：**这是结构的问题，不是参数的问题。**
红色的显隐走主线程绘制，卡片的位移走合成器，两条路只要差一帧，
就会有「卡片还没归位、红色已经整块画出来」的那一帧。调时间只是在赌哪一帧。

### 用户拍板的新做法

**不留常驻的红按钮**：手指划的时候卡片跟着走，松手一律弹回原位，
划过 **72px** 就弹「确定要删除…吗？此操作不可恢复」。仍是两步才删。

- 过了触发线**阻尼变三分之一**（最多 96px）—— 没有红色可看，
  手指忽然变沉是唯一的「已经划够了」的提示
- **等卡片回弹结束（180ms）再弹窗**，用户提的：一松手就弹，
  框是压在一张还在动的卡片上出来的。等待用的就是回弹那个常量
- 弹窗**删除在左、取消在右**（用户拍板）。回收站那两个框仍是老顺序，他说不用改
- 换文档、切词/句、退出编辑模式时把还开着的框收掉 —— 卡片已经换了一批

屏幕上自始至终只有一个会动的东西：卡片自己。三轮那类毛病没有立足之地了。

### 两个坑，记下来免得再犯

1. **React 在同一帧只提交一次渲染，你手动写进 DOM 的东西它不会替你收拾。**
   松手时手动改 transform 那三句（关过渡、写位置、把过渡和终点写回去）**一句都不能少**：
   那一帧里 React 渲染出来的内容和手势开始前是同一份，它认为什么都没变，
   于是一个字都不往 DOM 上写 —— 临时写的 `none` 和位移就留在了元素上，
   卡片歪在半路不回来。浏览器里当场撞见过
2. **整篇重写一个文件时，开发服务器可能缓存下一个空文件**（`cat >` 会先清空再写，
   watcher 正好在中间读了一次）。报出来的错却是「找不到导出」，白查了一会儿。
   摸不着头脑时先 `curl` 一下开发服务器给的到底是什么

### 实测

浏览器里走真实流程，为了不动原有笔记，临时长按造了一条专门用来删：
划 40px 不问、卡片从 -40 弹回 0；划 100px 问、卡片从 -81.3 弹回 0（阻尼算对了）；
点取消卡片还在；点删除卡片消失，**刷新页面仍然没有**；原有 5 条一条没动。
测试 466 项全过（左滑那组断言按新规则重写）。

---

## 四十七、~~平板：布局分界线从 768 挪到 1024~~ ✅ 2026-09-03 已验收

用户说「平板上问题有点太杂了，你先检查一下」。体检下来问题很集中：
**三栏布局的分界线是 768（Tailwind 默认的 md），手机永远够不到，
所以那套从来没人验过；而平板竖屏正好落在上面一点。**

### 量出来的（正文列宽 = 一行英文实际能用的宽度）

| 屏幕 | 左栏 | 笔记栏关着 | 笔记栏打开 |
|---|---|---|---|
| 768（iPad 竖屏） | 250，收不起来 | 正文 518 / 文字 444 | 正文 168 / **文字 94** |
| 800（安卓平板竖屏） | 250，收不起来 | 正文 550 / 文字 476 | 正文 200 / **文字 126** |
| 1280×800（横屏） | 250 | 正文 1030 / 文字 674 | 正文 680 / 文字 606 |

文字 94px 是一行放不下两个词。左栏 250 + 右栏 350 = 600，从 768 里挖走六成。

### 改了什么

- **新增断点 `wide`（1024）**，App/RightSidebar 里表示「电脑版布局」的 `md:` 全换成它。
  768～1023 归手机那套：侧栏浮起来盖住正文，正文永远整宽 —— 那套是验熟的。
  `md/lg/xl` 恢复 Tailwind 本意，别再拿来表示布局模式。
  JS 那边同一条线在 `src/hooks/useWideLayout.ts`，**两个数必须一致**
- **左栏在宽屏也能收起来**。汉堡键不再只在窄屏出现；宽屏点它是收起/放出，
  用的是右栏那套已验过的办法（收起 = fixed 移出屏幕不占位，展开 = relative 占一列）
- **宽屏上长按取词改回贴着词的小窗**（320px），不再是横贯屏幕、还压住文库列表的底部抽屉

### 小窗是「接回来」，不是新做的

2026-08-28 的 `d54ebe4` 删过一次 —— 当时它跟着**鼠标点击**走，手机上从不显示，
删得对。这回三点不同：**跟着长按走**（全平台仍只有这一种手势）、
**按可用宽度决定**（不按设备种类，那个 `useIsMobile` 删得对）、**会自己躲边界**：
下面塞不下翻到词上面，两边都紧就压矮自己让内部滚动，最后再夹一道保证留在屏幕内 ——
量到过一次：词在 y=1029、屏幕才 800 高，小窗被摆到了 795。

### 实测

- 800×1280：侧栏是浮层，正文恒为 800，复习卡 368×2 列，无横向溢出
- 1280×800：三栏 250/1030/350；收起左栏后正文 1280，再点回到 1030
- 小窗：词在上方 → 落在词下方 8px；词在屏幕下缘 → 翻到词上面；一律不出屏幕
- **手机不受影响**（375px 实测）：正文整宽、顶栏两颗键、侧栏是浮层、
  长按仍是横贯屏幕的底部抽屉

### 一条环境上的事

模拟器改视口尺寸时**不发 resize 事件**，`matchMedia` 的值对、事件却不来 ——
于是「CSS 已经换布局了、JS 还以为没换」。手工补发一次 `resize` 就正常。
`useWideLayout` 里 `change` 和 `resize` 两个都听，就是为这个。

---

## 四十八、⚠️ ~~沉浸式系统栏~~ 做了又撤（2026-09-03）

用户在平板上报了两条：**顶上一条深灰的状态栏和 app 格格不入**；
**底下导航栏那一行是空的**，弹窗把背景压暗时它不跟着暗，别的 app 都是交融的。

### 深灰那条的真因（我第一次说错了）

我张口就说是「主题跟随系统深色模式」，用户回「我平板没开深色模式啊」——
去依赖包里翻了才查清楚：

```
<style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
```

**名字看着像继承自上面那个 `AppTheme`（app 自己那份配色），但写了显式的 parent
就以 parent 为准**，点号命名那套不算数。于是它压根没继承 app 的颜色，
拿的是 AppCompat 的默认值，而系统画状态栏用的正是它：

`状态栏色 = colorPrimaryDark = primary_dark_material_light = material_grey_600 = #757575`

**浅色模式下也是这个灰**，跟深不深色无关。底色是灰的，系统自然配白图标。

### 做了什么（已全部撤回）

主题换成固定浅色、两条系统栏透明、`setDecorFitsSystemWindows(false)` 打开沉浸式、
在 MainActivity 里把系统栏尺寸算成 `--safe-area-inset-*` 四个 CSS 变量报给网页，
CSS 那边改成「先认原生报上来的值，再退回 `env()`」并且不再只对 767px 以下生效。

### 真机上一装就废了

**留白根本没生效，正文直接顶到状态栏底下。** 用户当场要求撤回，已 revert（`00ec1e9`）。
两条路（原生注入、`env()`）在他那台机器上都没送到值 ——
这正是动手前就写明「验不了、要真机看」的那一段。

### 教训（比这次改动本身重要）

1. **系统栏、窗口模式、安全区留白这类东西是「两边共用的地基」，不算「改平板」。**
   用户随后定了规矩：**改平板就是改平板，不要影响到手机那边。**
   平板的问题一律锁在 `wide`（1024）那条线之内，或者用 `useIsWide()` 判断；
   地基类的改动要单独提、单独验、单独一条分支
2. **我验不了的东西，不要一次改一大片。** 这次同时动了主题、窗口模式、
   原生注入、全局 CSS 四处，真机一挂根本分不清是哪一处的错

### 还留着的选项（没做，等用户开口）

**不碰窗口模式，只把两条系统栏染成和 app 一样的白、图标改深色。**
深灰那条会消失，风险几乎为零；代价是弹窗压暗时底部那一行仍不跟着暗。

---

## 四十九、~~沉浸式系统栏（这次成了）~~ ✅ 2026-09-03 已验收

第四十八节撤回的那件事，重做，通过。分支 `system-bars-edge-to-edge`。

### 先记用户那两句反问 —— 方案是被它们逼出来的

我原本给的是低风险选项「只把系统栏染成白的」。他当场反问：

> 你只给顶部染色有什么用？到时候就会出现一条向上延伸的断线，
> 就是左侧栏与阅读界面的交界线，这个你怎么处理？打开右侧栏同样会出现这条线。
> 底部呢？也染色？根本没用，到时候 app 始终踩在底部一条空白横区上，像个未完成品。

**他说得对，而且这正是第四十八节那一版就算数值全到位也会长成的样子** ——
那一版把留白加在 `#root` 上、还把那一圈刷成白色。我是翻代码才看出来的，他没装就看出来了。

### 正确的分工：底色铺满，只推内容

- **底色和竖线铺满整块屏幕** —— 左栏的白、正文的纸色、右栏的白都从 y=0 铺到最底。
  分界线于是从状态栏后面穿过去，没有断点；app 也不再踩在任何东西上
- **只把每一栏「里面的内容」往里推** —— 顶栏那行字让出状态栏，底部那排图标让出导航栏

弹窗压暗时遮罩本来就铺满整屏，两条系统栏跟着一起暗，这才是他要的「交融」。

⚠️ **顶部那一下加在中列容器上，不能加进 header 里。** 三栏的第一条带是对齐过的
（chrome.ts，横着扫只有两条通栏的线），加进 header 会连着带子一起长高，三条线当场错开。
量过：加在容器上时三条带的下沿都是 84（=40+44）。
底部则反过来，加在**滚动区里面**，纸色才铺得到最下沿。

### 取值：从「原生往网页里塞」改成「网页主动问原生要」

第四十八节栽的就是这个 —— 塞的时机和网页加载抢跑，真机上四个值全是 0。
现在新增一个 `SafeAreaPlugin`，网页起来之后调 `getInsets()` 要，桥会排队，抢不了跑；
转屏、键盘这类**之后**发生的变化仍由原生推（那时候网页早在了）。
两条路都没到值时退回 `env()`，再不行顶部还垫 24px 保底。
⚠️ `registerPlugin` 必须写在 `super.onCreate` **之前**，桥是在 super 里建的。

顺带修掉那条深灰的真因：主题写了显式 parent，点号命名的继承不算数，
于是拿了 AppCompat 的默认灰 #757575 ——**和深色模式无关**（第四十八节我说错过）。

### 底部让位：四轮才对，教训在这里

顶部一版就对了，底部改了四版。用户第一次的原话是「整体效果很好，
就是左侧栏与右侧栏底部被抬高了」—— 中间那栏是从滚动区里面让的位、正文照旧铺到底，
只有两侧栏被顶起来，一眼就看得出。

| 版本 | 依据 | 结果 |
|---|---|---|
| v1 | 系统栏高度 | 手势条的机器上白让一条 |
| v2 | `tappableElement`（本该回答「放按钮要让多少」） | **这台 ROM 报得和系统栏一样大**，落空 |
| v3 | `navigation_mode`（系统设置里那个开关本身） | **这台 ROM 读回 -1**，落空 |
| v4 | **按厚度判断** | 成了 |

按键导航栏是实心的、48dp 上下，从来没细过 32；手势条只有 16～24dp，透的、点得穿。
所以两条依据都落空时以 32dp 为界，细的一律不让。**界是量出来的**：
用户平板报的正是 16。

现在的规矩是三层：读得出「手势」就不让 → 读得出「按键」就整条让开 → 都读不到才按厚度分。

### 那张读数图一次问清了三件事

v3 之后用户说「还是老样子」，但这回带着读数（平板：安卓 31 / 密度 2 / 屏宽 1440）：

```
来源：原生·推送
原生报的：上 32 / 右 0 / 下 16 / 左 0
放按钮要让：16（系统自报 16）
导航方式：读不到（-1）
网页打包于 09/03 02:22
```

一张图同时排掉了「跑的是不是旧网页」、否掉了 v2 的依据、否掉了 v3 的依据。
**这是这一轮最值的一件事** —— 前两轮各花了一个来回在猜，加上读数之后一轮定案。

### 顺带堵掉的一个坑：装成 app 不该有离线缓存

网页文件本来就在安装包里，本地读，PWA 那层缓存毫无好处；
可它会截住请求先给缓存里那份，于是**装了新 APK 跑的还可能是上一版的网页**，
代码明明改了、手机上「好像没有变化」，一切看起来又都正常，极难查。
现在装成 app 时不注册，并且把上一版留下的注销掉、缓存删干净；浏览器里照旧。

这次「没有变化」最后查明不是它干的，但坑是真的，留着迟早踩。

### 「开发者 → 系统栏参数」这一屏留下来了

本来是为了验这一次临时加的，用户验完说留着。跟使用说明那套子屏一样：
设置里一行「系统栏参数」，点开换整屏、右上角从「关闭」变「返回」。
数值进这一屏时现读（转屏、收放键盘之后会变）。

里面还有一行「网页打包于 MM/DD HH:mm」（`vite.config.ts` 里 define 编进去的）——
对不上刚装的那一版就说明跑的是旧网页。

### 实测

- 浏览器（375 / 420 / 1400）：不挂 `.native` 时四边全 0、页面和从前一样，SW 仍注册；
  假装原生报上 40/48 后三栏都从 0 铺到底、三条带下沿都在 84、正文纸色到最下沿、
  左栏底部工具条停在 764（=812−48）、遮罩盖满整屏
- 两种导航各走一遍：tap=0 时工具条下沿落在 812（贴到底），tap=48 时落在 764
- 真机（用户平板）：顶部一版就对，底部第四版对。测试 466 项全过

### 留个教训

1. **他没装就看出来的东西，我翻了代码才看出来。** 「只染色」这个方案里
   那两条断线是结构性的，画一秒钟草图就该发现 —— 别急着给低风险选项
2. **验不了的东西，先想办法让它自己说话。** 前两轮都是「改一版、装一版、猜一版」；
   加上读数之后一轮定案。**盲改超过一轮，就该停下来先做诊断手段**
3. **依据落空要换依据，不要换参数。** tappableElement 和 navigation_mode 都是
   「正规」做法，但这台 ROM 两个都不给；能用的是「16 不可能是按键栏」这个物理事实

---

## 五十、~~宽屏点中间收起笔记栏~~ ✅ 2026-09-03 已验收

用户在平板横屏上说的：「右侧栏没法通过点击空白处关闭了，必须通过点击右侧栏
右上角的按钮关闭很麻烦」。分支 `right-panel-click-to-close`。

### 为什么宽屏没有这条路

窄屏点遮罩就能收，而遮罩写着 `wide:hidden` —— 宽屏三栏各占一列，
正文不该被压暗，所以那层本来就不该有。于是宽屏上只剩笔记栏右上角那颗关闭键，
而它离手指很远。

现在 `main` 上挂一个 click：宽屏且笔记栏开着时收起。关闭键随之删掉
（`onClose` 这个 prop 和 PanelRightClose 图标一并清），「AI 划词」自然靠到最右。
**手机上那颗键本来就不显示**（`hidden wide:inline-flex`），所以窄屏一点没变。

### 三种情况必须放过去

不然会「点哪儿都收栏」，比原来还难用：

1. **能操作的东西**（button / a / input / textarea / select / label）—— 交给它自己
2. **正文里的英文词** —— 长按取词松手时浏览器也会补一个 click
3. **取词小窗开着的时候** —— 那一下归 LyricEditor 里那个全局 click 管，先关小窗

### 顺带修的一个白屏

上一节加的 `__BUILD_STAMP__` 是直接引用的。改了 `vite.config.ts` 开发服务器不会
自动生效（这条早就记在交接说明里），没重启时这个名字压根不存在 ——
**模块顶层抛 ReferenceError 就是整个 app 白屏**。当场撞到一次。
一个只用来显示的时间戳不该有把界面弄没的能力，加了兜底（`typeof` 判断）。

### 实测（浏览器里走真实流程）

- 1400 宽：浮动「笔记」键能开、关闭键已没有、标题行最右是 AI 划词、
  点正文空白收起、点英文词不收、点顶栏按钮不收
- 375 窄屏：顶栏键能开、点遮罩能收、点正文不受影响 —— 和从前一样

### ⚠️ 又栽了一次：位置不能信

判断「栏开没开」我先用了 `getBoundingClientRect`，读出来永远是关着的 ——
**面板隐藏时过渡根本不推进，拿到的是冻住的那一帧**。这条就写在交接说明那张表里，
我还是先栽了一回才想起来。改用 `className`（`translate-x-0` / `translate-x-full`）
当场就对了；要截图则先注一条 `*{transition:none!important}`。

---

## 五十一、~~平板这一批：键盘、抢跑、断线、两侧栏可拖宽~~ ✅ 2026-09-03 已验收

用户在平板上一次报的四条，加上后续两条。分支 `tablet-batch-2`。

### 一、键盘弹起会把左侧栏也顶起来

根因是第四十九节为防输入框被挡加的那段：安卓 15 以下**把整个窗口往上挤**，
三栏一起变矮。手机是 15+，走不到那段 —— 用户说的「手机上就没这个问题」正是这么来的。

改成**原生只报输入法高度（`--kb`），不动窗口**，谁该避让谁自己让：

| 谁 | 怎么让 |
|---|---|
| 弹窗（九个遮罩） | 统一挂 `.kb-safe`：底部让出键盘，**里面那张卡片的最高高度改按「剩下的空间」算** —— 只加留白不改高度的话，85vh 的卡片会从顶上溢出去 |
| 正文列 | 只在**编辑全文**时让（见下） |
| 两侧栏 | 只在 `:focus-within` 时让 —— 也就是你正在这一栏里重命名。不打字时纹丝不动 |
| 长按抽屉 | `bottom: var(--kb)`，自己坐到键盘上面 |

**⚠️ 第一版我把正文列的避让写成了不分场合，返工了一次。** 用户的原话：
「除了这个编辑模式，没有必要任何情况下键盘出现都要顶起来吧」。对 ——
编辑全文时输入框就是正文本身，不让开看不见自己在打什么；
书库搜索、重命名、填 Key 这些时候键盘只是盖住下半屏，正文没有理由跟着缩。

底部抽屉那一条是**上一版带出来的退步**：它从前靠「整个窗口被顶上去」才浮在
键盘之上，窗口不动之后就整个躲到键盘背后了。改窗口行为时，凡是靠它蹭到好处的
地方都要一并想到。

### 二、「笔记」键抢在侧栏收完之前跳出来

写的是 `{!showRight && ...}`，状态一翻转就出现，而侧栏要滑 200ms。
改成常驻 + 透明度：出现时 `delay-200`（等侧栏滑完），消失时不等。

### 三、短语的波浪线断在 g 上

`decoration-wavy` 默认**给下伸笔画让路**（skip-ink）。平板上字被系统放大，
g 的尾巴正好压到线上就断一截；手机字小碰不到。加 `[text-decoration-skip-ink:none]`。
这条线表示「这几个词是一伙的」，断了含义就变了。

### 四、两侧栏都能拖宽了

250px 里真正留给文档名的只有 111px（一半以上被缩进、图标、「⋯」吃掉），
平板上九章书全成了「CHAPTER…」。**与其替他挑一个新的死数，不如让他自己拖。**

一份实现两处用：`src/hooks/usePanelWidth.ts`。左栏的杆在右边缘，右栏的杆在左边缘
（往左拖才是变宽，`invert`）。手指区 16px（那条线才 1px，按不准），
自己透明、按住显出一条强调色。范围左 200～560、右 260～560，
再夹一道「不超过屏幕一半」，松手存 localStorage。

**只在宽屏。** 窄屏两栏都是盖住正文的浮层，拖宽只会遮更多 —— 那边仍是 250 / 260。

⚠️ 松手时的宽度**当场按事件位置算**，不能读 state：那是渲染时捕获的旧值，
抬手和移动挨得近时 React 会并成一批，存进去的是拖之前的数（浏览器里当场抓到过）。

### 五、长按小窗被键盘整个盖住（宽屏）

`place()` 按 `window.innerHeight` 算净空，键盘不占位置，于是长按屏幕下半部分的词，
小窗正好落在键盘底下。量过：词在 y=572，小窗 602–888，键盘顶边 580。
**这条不是这一批改出来的**，从前靠整窗上顶时它同样会被切掉。

改法很小：把「可用屏高」减去键盘，`place()` 里三处用它 ——
上下取舍、压高度、最后那道「无论如何留在屏幕里」的夹子全都自动跟着走。
键盘高度靠 `safeArea` 新加的订阅拿（CSS 变量改了不通知 JS，而这个位置是 JS 算的），
见 `hooks/useKeyboardHeight.ts`。

### 留个教训：验不了的东西，给自己留一个手工入口

`window.__onNativeInsets` 现在**在浏览器里也挂上**（只是原生不会去调）。
于是在电脑上敲一句 `window.__onNativeInsets(32,0,16,0,0,320)` 就能完整走一遍
真机那条通路 —— 小窗躲键盘那组数就是这么量出来的，不是照着代码推的。

和第四十九节那屏读数是同一个道理：**我验不了的地方，先想办法让它自己说话。**

## 五十二、~~启动屏还是 Capacitor 的默认蓝 X~~ ✅ 2026-09-03 已验收

用户在平板上清后台再打开 app，闪过一屏：中间一个蓝色的 X、顶上一条写着
「我的文库」的原生标题栏、标题栏里两道拉扁的蓝色波浪。「感觉很粗糙」。
分支 `launch-screen`。

**那一屏根本不是 app，是安卓的启动窗口** —— 进程还没起来、网页还没画出来时，
系统照 `AndroidManifest.xml` 指定的主题先糊一屏顶着。三样东西全是建工程时的默认货，
从 1 月 28 日起没人动过（和桌面图标一样是漏掉的，见第四十一节）：

| 看到的 | 出处 |
|---|---|
| 蓝色 X | `drawable/splash.png`，Capacitor 自带的默认启动图（11 个密度目录里全是它）|
| 「我的文库」那条白栏 | `Theme.SplashScreen` 在安卓 12 以上继承自 `DeviceDefault.DayNight`，**那是带标题栏的**，没关掉 |
| 两道蓝波浪 | 同一张 X。`android:background` 是**给控件用的**属性，写在主题里，凡是没自己指定背景的控件都拿它当底 —— 标题栏里那些窄控件把图横着拉扁了 |

根子在 `styles.xml` 最后那三行，整个文件里只有这一段还是原样：

```xml
<style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
    <item name="android:background">@drawable/splash</item>
</style>
```

现在四件事：系统启动屏画纸色底 + 桌面图标那个衬线 A（用 `mipmap/ic_launcher_foreground`，
不另存一份）；启动屏退场到网页画出来之间那一层窗口底画同一件东西
（`drawable/launch_screen.xml`，288dp 对齐系统的图标尺寸，交接时不跳）；
关掉标题栏；两条系统栏透明。

**顺带修了一处我提出的**：`android:windowLightStatusBar` 写死成 true。
那个父主题跟系统深浅色走，一开深色模式启动那一瞬图标会变白，而底下是纸色 ——
第四十九节栽过同一个坑。`MainActivity` 里管这事的代码要等 app 起来才跑，比启动屏晚一步。

那 11 张默认蓝 X 没删，只是不再引用；要删是另一件事。

## 五十三、~~划词的线跟着短语线一起强行穿过下伸笔画~~ ✅ 2026-09-03 已验收

用户报的：「fuck you」里单独给 you 划词，y 的尾巴截断那条线，这是对的；
再把 fuck you 存成短语，那条线就强行连起来了。分支 `word-line-skip-ink`。

**`text-decoration-skip-ink` 是可继承属性**，而单词的 span 套在短语的 span 里面。
第五十一节为了让波浪线不断在 g 上给短语关掉了让路，里面的单词就一起被关掉了 ——
同一个词，单独划是断的，落在短语里就变成不断的，两幅样子。

### ⚠️ 第一版只改了一行，改出了新毛病

给单词那层显式写回 `auto` 挡住继承。看着对了，用户当天就报回来：
「plain gone 不断，sticking out 会断」—— 差别正是**那个词有没有被单独划过词**。

因为浏览器画祖先那条下划线是**画到哪个 span 就用哪个 span 的值**：
给单词那层开了让路，短语的波浪线画过那个词时也跟着破一个口子。
两条线只要都走 `text-decoration`，就共用这一个开关，甩不掉彼此。

**验的时候漏在哪**：我只试了 `you`（没被划过词的那种），没试
「被划过词的那个词正好带下伸笔画」。改完一处继承关系，要把
「祖先关、后代开」这类**组合**都摆出来看一遍，不能只看自己改的那一头。

### 定案：短语的波浪线改用背景图

用户在两个方案里选了「划词让路、短语线仍不断」。要两边都理想，短语那条就得
离开 `text-decoration` —— 改成背景图（和句摘那条一个路数，起因不同），
从此不归那个开关管。见 `index.css` 的 `.phrase-line`。

几个数是和原来那条**叠着**量出来的（21px 正文放大 9 倍看）：波长 6px、峰高 5px、
线粗 1px、`padding-bottom: 0.28em` 落回原位。**波形用 px 不用 em 是照着原样** ——
浏览器画的波浪线只跟线粗走，字号调大时波形本来就不跟着长，只有位置跟着走。

颜色只能一种一份：背景图是一段 URL，里面的颜色没法写成变量。图案放在文件开头
五个强调色块里的 `--phrase-wave`。**以后再加强调色，那里也要补一行。**

### 留个教训：字号放太大，反而看不出这类差别

一开始我把字号放到 300px 去看让路效果，两种写法都是一条连着的线，差点当成改了没用。
线粗写死成 2px，字一大那点缺口就按比例缩没了。**按真实字号量、再整体放大来看**
（`transform: scale()` 保留原尺寸算出来的断点，是可信的放大方式）。

## 五十四、~~波浪线上每隔几个波一道小裂缝~~ ✅ 2026-09-03 已验收

用户在手机上看见的，平板上没有。上一节那张背景图带来的。

背景图是**一块砖一块砖铺过去**的。第一版一块砖只装一个波、从半山腰起笔
（`M0 2.5 Q...`），砖缝正好切在波形最陡的地方。线的两头是平口切的，
切口垂直于笔画方向，在陡处就成了一道斜切 —— 两块砖的斜切对在一起，
中间留出一个楔形的空档。

**两台机器屏幕密度不同，平板那边凑巧盖住了。** 不是平板没这毛病，是它碰巧看不见；
这类问题一律按「两台都可能中招」处理。

改成**从波谷起笔**：那里笔画是水平的，平口切出来是一条竖线，两块砖严丝合缝。
顺带一块砖装四个波，砖缝少四分之三。波长、峰高、线粗、位置一个数没动 ——
新波形的峰谷和旧的完全重合，只是起笔相位挪了半个波。

验的办法值得记：把同一张图按 20 倍尺寸（`background-size: 480px 100px`）铺一条出来，
缺口一眼就看见。**图案类的毛病，放大铺开比在真实尺寸下瞪着看快得多。**

## 五十五、~~沉浸阅读~~ ✅ 2026-09-03 已验收

用户提的：「像看视频那样」，只剩正文。分支 `tablet-immersive`，做了三轮。

### 第一轮：平板横屏，两侧栏都收起来就进

app 的两条顶栏（标题栏 + 阅读工具带）、安卓的两条系统栏、右边那颗「笔记」键
一起消失。点正文空白处，顶栏那一套露 3 秒又自己收；再点一下立刻收。
从屏幕顶端下滑露系统栏那一段是安卓自带的（`BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`），
网页不掐那块表。

⚠️ **藏系统栏是手机平板共用的地基**（第四十八节栽过）。所以：

- 开关握在网页手里，进出的条件抽成纯函数 `shouldImmerse`，单独有测试
- 原生只多一个方法（`SafeAreaPlugin.setImmersive`）
- 组件卸载时兜底把系统栏放回来，不留「只能杀进程才能退出」的死角
- 从后台回来补喊一次，免得系统把栏放回来了而网页还以为自己沉浸着

**两条顶栏在沉浸态下浮在正文上面，不占位置**：占位置的话每露出来一次正文就被
往下推一次、收回去再弹上来，读到哪儿都跟着跳。浏览器里量过，来回切换时
正文的高度、顶边、滚动位置三个数一动不动。

`--sa-top` 那个 24px 保底在沉浸时要让开，否则顶上剩一道白边。保底本身没动 ——
那是手机上「正文顶到时钟底下」的护栏。

### 第二轮：状态栏跟顶栏绑成一体

用户报了两件事，其实是同一个根子——**状态栏和 app 顶栏各有一套作息**：

- 沉浸里点出来的顶栏贴着屏幕最顶上（84px），正常模式下同一条顶栏上面还顶着
  状态栏（108px）—— 同一条栏两种厚度，他说的「跨度」
- 退出沉浸时那两截**分两次**长出来：网页那一步是瞬间的，状态栏那一步要等安卓
  滑完动画才把新尺寸报上来 ——「变宽然后再变宽一点」

他先想过「正常模式也一律藏状态栏」，我不赞成：读书时一直看不到时间电量是长期代价，
而状态栏只占 108px 里的 24px，大头是那两条带。**方向是对的（让两种形态长得一样，
而不是把跳变演得柔和些），但落点该换一个。**

定案：系统栏**跟着「顶栏露没露」走**，不再跟着「沉不沉浸」走；露出来的顶栏
按 `--sa-top-real` 预先留好状态栏的位置，于是和正常模式一模一样厚。

`--sa-top-real` 是新加的一格（`safeArea.ts`）：**状态栏本来多高**。原生报 0 时不更新、
报了真值才跟上，所以转屏换机器自动跟着走。退出沉浸的第一帧就能按真值留位置，
第二段自然没有了。顺带治了「下滑看时间，时钟压在标题上」。

量过（模拟 33px 状态栏，并**故意让原生晚一拍才报尺寸**）：
正常 117 / 沉浸 0 / 沉浸里露出来 117 / 退出第一帧 117 / 状态栏回来后 117。

### 第三轮：手机和平板竖屏

**触发方式故意和宽屏不一样，两条都是用户定的**：

| | 宽屏（平板横屏） | 窄屏（手机、平板竖屏） |
|---|---|---|
| 怎么进 | 自动：两侧栏都收起来 | 手动：点一下空白处 |
| 顶栏怎么回来 | 点空白露 3 秒，自己收 | 点空白，一直待着不走 |
| 计时器 | 有 | 没有 |

窄屏不能照搬宽屏那条规矩：那边侧栏本来就总是收着的，拿它当信号会一开 app 就沉浸；
而且**窄屏的顶栏是唯一的出口**（侧栏、笔记都从那儿开），不能让它自作主张地消失。

点空白那个处理器加了一道闸：复习页和编辑全文里什么都不做 —— 那两处沉浸本来就
进不去，在那儿悄悄翻开关，等回到阅读页顶栏会莫名其妙地不见了。

### 留个教训：验证环境里改视口尺寸，媒体查询不会自己更新

「窄屏一律不进」那条我一度以为写漏了 —— 把视口从 1280 改成 800，顶栏还是沉浸态。
其实是**模拟器不发 `resize`**（文档开头那张表里早写着），手工补一句
`window.dispatchEvent(new Event('resize'))` 就对了。

同一天还栽了一次 `getComputedStyle`：className 里明明是 `opacity-0`，
读出来的 opacity 却是 1 —— 面板隐藏时过渡不推进，读到的是冻住那一帧。
**这两条表里都有，遇上了还是先怀疑了自己的代码。查表比再栽一次便宜。**

## 五十六、~~「更多」菜单里「移至回收站」折成两行~~ ✅ 2026-09-03 已验收

用户在平板上报的，手机上没问题。分支 `menu-width`。

**根子不是 140px 这个数不够大，是系统把字放大了**（WebView 的 textZoom），
而菜单写死了宽度。手机上字小，同样的 140px 就够。写个更大的数只是把门槛往后挪，
他把系统字号再调大一档又会犯。

加 `whitespace-nowrap`：140px 从此只是**下限**，盒子按里面最长那条撑开。
少了它，绝对定位的盒子会照可用宽度去挤，于是折行。

复现和验证都在浏览器里把根字号从 16 调到 22 / 26（等价于系统字体放大）：

| 根字号 | 改前 | 改后 |
|---|---|---|
| 16（手机） | 140 宽，两项都 36 高 | **一模一样，没动** |
| 22 | 140 宽，「移至回收站」77 高（两行）| 162 宽，两项都 50 高 |
| 26 | —— | 191 宽，两项都 59 高 |

同一串类名这个文件里有三处（文档、文库、回收站里的条目），三处都改了。

**这一类毛病值得单独记一笔**：用户的平板系统字号比手机大，凡是写死宽度、
写死高度的地方都可能在他那儿折行或被切掉，而我在浏览器里按默认字号是看不见的。
往后遇到「他那儿排版不对、我这儿正常」，**先把根字号调大再看**。

## 五十七、平板上机器音一概没声音 —— 查明是设备缺件，不是代码（2026-09-03）

用户报的：平板上点朗读，**只有网上搜得到真人录音的词能响**，
搜不到的词、短语，以及**所有句子**，一概没声音。手机上完全正常。

### 先把结构摊开，范围当场就锁死了

朗读是两条互不相干的路（见 src/speech/humanFirst.ts）：

| 内容 | 走什么 | 平板上 |
|---|---|---|
| 单词、短语（词典有录音） | 下载 mp3 用 audio 标签放 —— **不碰系统引擎** | 能响 |
| 词典没录音的 + 所有句子 | 交给安卓系统的「文字转语音」引擎合成 | 哑的 |

现象和这张表完全对上：凡是过系统引擎的一律没声。

### 结论：同一个包，手机好、平板不行

用户的平板是**鸿蒙**，而且**系统设置里翻不到「文字转语音」那一项**。
安卓的朗读引擎是系统的一个独立部件（得单独装、单独下语音包），
华为设备没有 Google 服务，Google 那个引擎本来就不在。

**同一个 APK 在手机上一切正常** —— 到这一步代码已经洗清了，没有再打诊断包的必要。
出路在设备那头：给平板装一个朗读引擎，或者改成不依赖设备的云端朗读。

### 顺手验掉一条以前只是「推测」的事：词典接口读不了整句

以前文档里写「词典只收词条、整句没有录音」，是推断。这次直接问了接口
（`dict.youdao.com/dictvoice?type=2&audio=…`，**走代理和直连各试一次，结果一致**）：

| 请求的内容 | 结果 |
|---|---|
| `hello` | 200，10605 字节音频 |
| `take it for granted` | 200，37145 字节音频 |
| `I never stood up very tall` | **500，`returned null audio`** |
| `zzqqxxwv`（生造词） | **500，同上** |

所以那条路补不上，句子只能靠合成。「句摘用云端朗读」那条老悬案（第三十节前后）
现在多了一个理由：**它同时也是平板这类设备唯一的出路。**

### ⚠️ 顺带查出一个真 bug（已修，见第五十八节）

朗读失败时代码**是有提示**的（「这台手机还没有英文语音，装一个就能读了」，
带一颗「去安装」按钮），但只有**复习页**（VocabularyDashboard）把它显示出来。
**阅读页的划词小窗（LyricEditor）和右侧生词板（RightSidebar）把 `useSpeak()`
返回的 `error` 整个丢掉了** —— 于是用户在那两处得到的是纯粹的「点了没用」，
既没声音也没有任何交代。

还有一处该一起改：`canSpeak` 现在只看**插件在不在**（`isPluginAvailable`），
不看**引擎初始化成功没有**。引擎压根不可用时喇叭图标照样画出来，点了空响。

第一条已修（第五十八节）。**第二条没做，而且不该做** —— 用户指出这个 app
本来就没有喇叭图标、点词就是发音；而且平板上有真人录音的词照样能响，
把词改成不可点会连好使的那一半一起砍掉。


## 五十八、~~朗读失败时说人话，而且在你点的那个地方说~~ ✅ 2026-09-04 已验收

补上第五十七节欠的那个 bug。分支 `speech-error-visible`。

### 三处里只有一处会说话

朗读失败时代码**本来就有提示**（「这台手机还没有英文语音，装一个就能读了」，
带一颗「去安装」），但只有**复习页**画它 ——
**阅读页的划词小窗和右侧生词板把 `useSpeak()` 返回的 `error` 整个丢掉了**。

用户平时就在阅读页长按取词，于是他得到的是纯粹的「点了没用」：
没声音，也没有任何交代。**这是第五十七节多绕好几轮的直接原因** ——
要不是打了诊断包，那句 `Not yet initialized` 他永远看不到。

抽成 `SpeechNotice`，三处共用。

### 「压根没有引擎」得单算一种，而且不能给「去安装」

`errors.ts` 里新加一条 `NO_ENGINE`，认 `Not yet initialized` /
`not available on this device`，翻成
「**这台设备没有可用的朗读引擎，只有网上查得到发音的词能读**」。

⚠️ **它必须排在「缺语音包」那条前面。** 那句原话里带着 available、language 之类的字眼，
顺序反了会被误判成缺语音包，进而给出一颗**骗人的**「去安装」——
那颗键跳的是系统的语音包安装页，而这台设备连那个页面都没有，点了什么也不会发生。
测试里专门钉了这一条。

### 关掉一次就不再拿同一句话烦人

没有引擎的机器上，**每碰到一个词典里查不到的词都会失败一次**，
每次都弹同一条，读一页书要关十几回。所以「知道了」按下去之后，
这一趟里同样的话不再出现。记在模块里而不是组件里 ——
阅读页、生词板、复习页各有一份 `useSpeak`，记在组件里就成了「每处各烦你一遍」。

### ⚠️ 我提的一个改法被用户否掉了，他是对的

我原本还想「引擎不可用时干脆别画喇叭图标」。用户回：
**「本来就没有喇叭图标，原设计就是点击单词本身就能发音。」**

查了代码，他是对的：`canSpeak` 为假时是**把词变成不可点的纯文字**，没有图标这回事。
而且顺着想下去更错 —— **平板上有真人录音的词照样能响**，
把词改成不可点会连好使的那一半一起砍掉。

**这一节最后只做了一件事：把话说出来。**

## 五十九、~~云端朗读：真人录音 → 云端合成 → 系统引擎~~ ✅ 2026-09-04 已验收

第五十七节查明平板上没有系统朗读引擎、且装不上，只能换一条不依赖设备的路。
分支 `cloud-tts`。用户开通了**火山引擎（豆包语音）**，在平板上试读成功。

### 用户定的规矩

> 「优先都走云端，引擎是实在没网了再说，**不用平板手机区分开**。」

所以是三级：**真人录音 → 云端合成 → 系统引擎**。

真人录音仍排第一，这一条是我定的，跟他说明过：人念的比任何合成都好，
而且**不花云端的调用次数** —— 他那两万次几乎只会花在句子和生造词上。

手机上引擎是好的，但也一样先走云端。两台机器行为不一致，比慢半秒更难受。

### 为什么选「按调用次数」那个服务

他开通了火山的两个语音合成，免费额度：

| | 语音合成（旧版） | 语音合成 2.0 |
|---|---|---|
| 免费额度 | **20000 次调用** | **20000 个字** |
| 怎么算 | 一句算一次，**多长都一样** | 按字数扣 |
| 音色 | 只有 2 个 | 99 个 |
| 服务集群 | `volcano_tts` | 控制台上空着 |

他读的是英文句子，一句几十个字符。按字数扣半个多月就没了，按次数扣能用近两年。
**差着四十倍**，所以用旧版。音色他实测过：标的是中文，但英文句子也念得出来。

### ⚠️ 两处是实测逼出来的，不是照文档抄的

1. **认证头是 `Bearer;<令牌>`，分号不是空格。** 火山文档特意强调过，
   按常规写成空格会被拒，而报错未必说得清是这个原因。
2. **状态码不好看时要先看正文。** 拿假令牌实测：火山回 **HTTP 401**，
   而正文里写着 `code 3001, "load grant: requested grant not found in SaaS storage"` ——
   **那句才是能拿去查的东西**。第一版我只报了「网络返回 401」，
   等于把唯一的线索扔了。而这条路的验证全靠用户把报错念给我听
   （我手上没有他的 Key），扔掉线索等于没法查。

这两条和 `fetchAudio.ts` 那个老教训是同一件事：**别信状态码，看内容。**

### 缓存键里必须带音色

云端音频进现成的发音仓库（`audioCache`），键是 `文本|cloud:<音色>`。

带音色是必须的，否则换了音色还会放出旧音色那一份。
而**存下来这件事在这边格外要紧**：词典那边重复请求只是费流量，
这边费的是**免费额度**。同一句永远只花一次。

### 没配 Key 就当这条路不存在

`CloudNotConfigured` 和普通失败分开抛，上层安静地落到系统引擎，不弹任何提示。
没开通云端的人不该被这件事打扰。

### 设置摆在哪：AI 那一栏的第二行

第一版我单开了一节「朗读」摊在主列表上，用户说**「放进 AI 那一栏」**。
两件事性质一样：都是外面的服务、都要粘一个 Key、都只存在这台手机上。

做成和 AI 那行一样的**一行、点进去单独一屏**，而不是把四个输入框摊开 ——
摊开会把设置主列表撑得很长，和旁边几栏都不一个重量。
这一行顺带显示状态：配好了写当前音色，没配写「还没设置」。

⚠️ **没有塞进现有的「AI 设置」那一屏**：那一屏底下有「保存 / 取消」两颗键，
而朗读这几格是改一下存一下的 —— 点「取消」时 AI 的改动丢了、朗读的却已经存了，
说不清楚。

### 用了多少次：app 自己数，没接官方额度

用户问「为什么火山那边没显示消耗」。两个原因叠在一起：
**官网的用量是延迟统计的**；**同一句只花一次**（读过的存下来了，再点不联网）。

他要求把额度集成进来。查下来官方那条路是 `ListResourcePackages`（费用中心 OpenAPI），
**鉴权要账号级 AK/SK + HMAC-SHA256 签名，不认朗读那个令牌**。
那套密钥能动整个火山账号 —— 往手机里放一把万能钥匙只为看一个数字，代价不对等。
给了他三条路（本地计数 / 建只读子用户 / 用主账号 AK/SK），**他选了本地计数**。

于是云端朗读那一屏显示「这台机器上已经用掉 N 次」，可归零。
**只数真发出去的**，命中缓存的不算 —— 那些压根没联网，也不扣额度。

### 电脑上验到哪一步

用**假凭证**跑通了全程：请求发出去 → vite 转发 → 火山收到并认出认证头 →
报错原样回到屏上（屏上显示的正是上面那句 `load grant`）。
**除了「凭证对不对」，别的环节都验过了** —— 那一项只有用户手上有真 Key。

用户在平板上试读成功。

## 六十、~~设置页三处小问题~~ ✅ 2026-09-04 已验收

用户报的三件，**后两件是同一个根子**。分支 `settings-scroll`。

### 一、AI 那一栏底下那段说明文字，删了

用户要求的。

### 二、滚动位置：一个根子，两个症状

这几块屏（主列表、AI 设置、云端朗读、使用说明、用户协议、开发者那两屏）
**共用同一个滚动容器**，而 `scrollTop` 从来没人管。于是：

- **「开发者那两个点进去默认滑到最下方」** —— 那两个入口就在主列表最底下，
  你得滚到底才点得着；换屏时 `scrollTop` 原样留着，看着就像它自己滑下去了
- **「从子屏返回，设置跳回顶部」** —— 子屏内容短，`scrollTop` 被浏览器夹成了小数，
  返回时那个小数还留着

定案（用户的话）：**进子屏一律从头看，退回来还他原来那个位置，关掉设置就忘掉**
——「不用把一直记着」。

⚠️ **位置必须在点下去那一刻记，不能等副作用里再读。**
换屏时内容高度一变，浏览器当场就把 `scrollTop` 夹掉了 —— 那正是这个 bug 的成因，
在副作用里读到的已经是夹过的数。所以六个入口全部包了一层 `enterSub()`。

### 实测（浏览器里量的，不靠眼睛）

主列表滚到 **469** → 进子屏 **0** → 返回 **469** → 关掉再开 **0**。

## 六十一、~~云端同步：手机和平板共用一份数据~~ ✅ 2026-09-04 已验收（省流量那一批还在分支上）

用户提的：「我两个设备还要来回备份迁移」。分支 `sync-webdav`（已并）、
`sync-quota`（**没并，等真机验**）。

### 选型：不要云端数据库，只要一个放文件的地方

他先入为主地问「这应该没免费的吧」。实测了七家，**都有免费的**，
而且他的数据本来就是**一整块 JSON**，压根不需要数据库：

| 路子 | 直连 | 要 key | 备注 |
|---|---|---|---|
| **坚果云 WebDAV** | ✅ 45ms | 应用密码 | **选了这个** |
| Gitee 私有仓库 | ✅ 36ms | 令牌 | 有版本历史，但接起来麻烦一档 |
| GitHub / Supabase / Cloudflare | ✅ | — | 境外，国内不稳 |
| 火山 TOS / 阿里云 OSS | ✅ | 账号级 AK/SK | 签名复杂，且密钥权限太大 |

坚果云胜在：注册不用实名不用绑卡、国内最快、协议就是「传一个文件／取一个文件」。

### ⚠️ 真正的难点不是存哪儿，是怎么合

**这一段值得完整读一遍，是这一节的核心。**

原计划是给每条记录加「最后改动时间」+「删除墓碑」再比时间。那要**动数据格式**，
还要去二十来个写入口挨个补时间戳 —— **补漏一处，那条记录就永远同步不出去，
而且不报错、只会悄悄不同步**。这种 bug 最难查。

定案是**三方合并**：本地留一份「上次同步完是什么样」（叫**底本**），
于是「谁改了什么」是**算出来的**，不是记出来的：

- 底本有、本地没有 → 我删了这条（**不需要墓碑**）
- 底本没有、本地有 → 我加的
- 和底本一样 → 我没动过

**数据格式一个字没改，写入口一处没碰。** 冲突规则：

| 情况 | 结果 | 为什么 |
|---|---|---|
| 只有一边动过 | 听那一边的 | 没有分歧 |
| **一边删、一边改** | **保留那次修改** | 删除还能再删一次，写过的字丢了就没了 |
| 两边都改且有 updatedAt（文档、文库） | 晚的赢 | |
| 两边都改且没时间可比（标注） | 本机赢，**并计一笔冲突报给用户** | 标注没有 updatedAt |
| 第一次同步（没有底本） | 当成空底本 → **并集** | 两台已有的数据谁也不吃掉 |

`merge.ts` 是纯函数，24 项测试，每种情形单独钉一条 ——
**判错一次就是用户的笔记没了，而且丢了不报错、过几天才发现。**

### 用户否掉了「先做整块覆盖」那个过渡版本，他是对的

我原本建议分两步走，先做「手动传、整块覆盖」跑通链路。他回：
**「第一步对我来说没什么用，我本来就能手动导出备份并覆盖，这个功能我是知道的。」**

确实。那一步等于他现有的操作，白做一轮。直接上逐条合并是对的。

### 真机上撞的三个坑，全是 Capacitor 和坚果云的脾气

1. **坚果云在「上层文件夹不存在」时回 409，不是 404。** 而我只在 404 那条路上
   去建文件夹，于是永远走不到建文件夹那一步。两个都要当成「云端还没有东西」。
2. **报错被我截断在 120 字，把唯一有用的那段切掉了。** WebDAV 报错是一整坨 XML，
   开头一百多字符全是 `<?xml ... xmlns:d="DAV:"` 这种命名空间声明，人话在后面。
   现在先抠 `<s:message>` / `<s:exception>`，抠不到才退回原文。
3. ⚠️ **CapacitorHttp 不支持 MKCOL** —— 报错原文是
   `Expected one of [OPTIONS, GET, HEAD, POST, PUT, DELETE, TRACE, PATCH] but was MKCOL`。
   所以**「app 自动建文件夹」这条路在手机上根本走不通**，别再试了。
   解法：让用户填坚果云**默认就有**的那个文件夹「我的坚果云」，或者他自己去网页上建一个。

顺带记一个我自己的坑：抠 XML 那个正则第一版写成 `[\s\S]`，而它在**模板字符串**里 ——
反斜杠被吃掉，变成了「s 或 S」，中文一个都匹配不上。**测试当场抓到，没漏到真机。**

### ⚠️ 流量：用户问对了，而且比想的更急

他问「这么高频率的对齐会不会很快用完额度」。会。量过：**八本书的库，明文 3.05 MB**；
坚果云免费额度每月 1G 上传，不压只够 **335 次**。

**而头号大户不是改笔记，是阅读进度** —— 一路往下读，滚动位置一直在存
（`LyricPage.progress`），每一次都算「数据变了」。**读一天书能把一个月额度读光。**

四道闸（`sync-quota` 分支）：

1. **只有阅读进度变了，不单独触发上传**（`onlyProgressChanged`，纯函数 + 6 项测试）。
   不是不同步进度 —— 换设备接着读那一行有用；是让它**顺路**走。手动那颗按钮不受此限
2. **先 HEAD 问一句「变了没有」，变了才 GET**。一次 HEAD 几百字节，一次 GET 好几兆
3. **gzip + base64**。实测高熵英文散文净省 **2.2 倍**（gzip 压掉三分之二，
   base64 再涨三分之一 —— 那是原生网络只收字符串的过路费）。
   ⚠️ **拆箱时明文也要认**，用户云端已经有一份没压过的
4. 两次自动同步至少隔一分钟；存底最多一小时一份（原先每次同步都写，上传量翻倍）

### 界面上的取舍（都是用户拍的）

- 入口在**设置 → 数据 → 云端同步**；导出/恢复备份那两颗上面加了并列的小标题
  **「手动同步」**，两个标题**同一档字号**（他指出原先「手动同步」小了一截，
  看着像从属关系 —— 量过，现在都是 14px 同色）
- 云端朗读和云端同步两屏都改成**保存制**（草稿 + 保存/取消，和 AI 设置对齐）。
  他的理由：「不小心修改了内容又得重新输入」
- ⚠️ **保存之后留在原地，不要顺手关掉那一屏** —— 他可能还要接着点「立刻同步一次」。
  存没存住看那颗键：保存完变「已保存」并置灰
- 同步用的是**存下来**那一份，不是草稿，所以有未保存改动时先拦一下

### 时机：不做真·实时，也不需要

用户最初要的是「一台划了词另一台即时同步」。**但真正的即时是看不见的** ——
那会儿另一台锁在兜里。他要的其实是「**拿起另一台的时候东西是新的**」。
所以只做两下：**回到前台拉一次** + **改完过几秒传一次**。
效果一样，代价小一个数量级（真·实时要一台一直连着的服务器，坚果云给不了）。

### 我验到哪一步、没验到哪一步

**验了**：合并逻辑（24 项测试）、压缩装拆箱（6 项）、报错解析（10 项）；
浏览器里用假账号跑通全程（请求 → vite 转发 → 坚果云 → 401 原样回到屏上）。

**没验**：真账号那条路 —— 我手上没有他的密码。所以照第四十九、五十七节的老办法，
**把服务器原话原样打在屏上**，让他自己一眼看出是哪一种。他两台真机上已经同步成功。

**还没验**：`sync-quota` 那四道闸的真机表现（打了包，他还没装）。

### 留个教训：他先入为主的判断，值得当场去测

「这应该没免费的吧」「即时同步做得到吗」「会不会用完额度」——
三句里**两句的直觉是错的、一句是对的**，而且对的那句比他以为的还严重。
**三句我都去量了**，没有一句是靠嘴回答的。这一节能一次做成，靠的就是这个。

## 六十二、~~正文和索引拆开存：划词从传 1.44 MB 变成传 17 KB~~ ✅ 2026-09-04 已验收

用户问的：「我在阅读的时候划词做笔记这个动作是很频繁的，那要是每次这方面的变动
都会引起同步不也会消耗很快吗？」—— **问对了，而且第六十一节那四道闸完全没治到这一种。**
分支 `sync-split`。

### 上一轮为什么没治到

那四道闸治的是**阅读进度**（不让它单独触发上传）。而**划词是货真价实的数据改动，
本来就该传** —— 于是最坏每分钟传一次整份 1.44 MB。

量出来：**一个月 1G 只够约 12 小时的密集划词。** 他天天读，一两周见底。

### 根子：97% 的流量在重传一个字没变的小说

| 内容 | 压缩后 |
|---|---|
| **正文**（八本小说） | **1.42 MB** |
| 索引 + **全部 1760 条笔记** | **17 KB** |

划一个词，改的是那 17 KB 里的一条。

### 拆法

- `data.json` —— 索引：文库、文档的**壳**（标题、所属、进度……没有正文）、全部笔记。
  正文在这里只是**一枚指纹**
- `page-<id>.json` —— 每篇正文一个，**只在编辑正文或导入新书时才动**

省 **84 倍**：密集划词从 12 小时/月 变成约 1000 小时/月。用户真机实测**一次同步 30 KB**。

### ⚠️ 四处判错就会丢正文的地方

1. **指纹必须算内容，不能用 `updatedAt`。** `savePage` 每次都刷新它，
   **连只存了滚动位置那一次也刷** —— 拿它判断「正文变没变」会把大量没变的正文
   误判成变了，等于白拆。
2. **平铺成 `page-xxx.json`，不放子目录。** CapacitorHttp 不支持 MKCOL（第六十一节），
   建不出子目录。
3. **先传正文，再写索引，顺序不能反。** 反了的话另一台会看到一篇「有壳没正文」的文档，
   而且它一比对指纹，会当成「对面把正文改成空的了」——
   **那不是慢一步，是真会把正文冲掉。**
4. **旧格式（version 1，正文就在那一整块里）必须认得出来。** 用户云端已经有一份，
   认不出等于把他同步上去的弄丢。认出来后正文直接取用，下次写回自动升成新格式，
   他什么都不用做。

另外：取不到正文时给**空字符串**而不是丢掉那篇文档 ——
宁可显示成一篇空文档（一眼看得出不对、下次同步补回来），
也不能让它从文库里消失，那会让人以为自己的书没了。

### merge.ts 一个字没动

指纹就放在索引的文档对象里，所以「正文改了」照样会被三方合并看见
（那条记录的内容变了），而合并本身完全不必碰那几兆正文。
**这是拆分能做得这么轻的原因** —— 心脏那部分连测试都不用重跑一遍。

### 测试：不测函数，测「两台设备来回同步」

新增 22 项拆分测试，外加 **7 项整段推演**（`roundtrip.test.ts`）：
用一个假云端（几个 Map，语义照真的来 —— 索引一个文件、每篇正文一个文件），
把手机平板轮流同步的整个过程跑一遍，每轮断言「两台看到的一样，正文一个字没少」。

**为什么这么测**：拆开之后同步不再是一次读写，而是「索引 + 若干篇正文」的一组读写，
中间任何一步判错都可能把正文弄丢，**而且丢了不报错**。
其中一条专钉「反复同步不会把正文磨没」—— 空内容是这类拆分最容易出的事故。

共 590 项。

### 顺带：设置里 AI 那一栏改叫「辅助笔记」

用户要的。原先那一行显示的是供应商名字（DeepSeek），和底下「云端朗读」一比
就不整齐 —— 一行说功能、一行说牌子。现在两行都说功能，
供应商和模型挪到第二行（`DeepSeek · deepseek-chat`），
「数据发给谁」这个信息一个字没丢。

## 六十三、~~⚠️ 排序被同步悄悄还原~~ ✅ 2026-09-04 已验收

**两个 bug 都是用户追问出来的**，而且都属于「不报错、只会让他哪天莫名其妙发现不对」。
分支 `sync-order`。

### 一、文库和文档的拖动排序会被还原

他问：「要是文档文库的排序变了，这个又会怎么样」。答案是**会被改回去**。

根子很隐蔽：**文库和文档的顺序是「在数组里排第几」表达的，不是存一个字段**
（`storage.ts` 的 `reorderBooks` / `reorderPages` 就是重排数组）。
而三方合并是「按 id 建表再吐出来」的，**天然不认识「位置」**——
拖动之后每条记录**内容一个字没变**，合并只会说「谁都没动过」，
然后按底本的次序吐出来。那一下白拖了。

修法：顺序单独三方合并一遍（`mergeOrder`）。取舍和别处一致 ——
只有一边拖过就听那一边的，两边都拖过本机赢。

⚠️ **判断「谁拖过」时只比两边都有的那些 id**：否则「删了一篇」「加了一篇」
都会被误判成拖动过，进而把另一边真正的拖动盖掉。

**做法上值得一提**：先写了 5 条会挂的测试把 bug 钉死，跑出来确实全挂，再动手改。

### 二、复习页卡片的排序 —— 本以为没事，测试当场抓到

他追问：「复习模式下的卡片也有排序功能，这个有考虑到吗」。

**这一种和上面不是一回事，这一点很容易记混：**

| | 顺序存在哪 | 三方合并看得见吗 |
|---|---|---|
| 文库、文档 | **数组位置** | **看不见** ← 上面那个 bug |
| 复习页卡片 | 每条记录上的 `order` **字段**（`reorderAnnotations`） | 看得见 |

所以基本情况天生就对。**但写测试时抓到一个真的**：两边都重排同一组卡片时，
**逐条合会交错** —— 本机把 A 挪到最后、对面把 B 挪到最前，各取各的，
最后两张卡可能都编号 0，排出一个**谁都没要过的乱序**。

改成按「同一文档、同一类型」**整组**合（`mergeCardOrder`，复用 `mergeOrder`），
合完**重新编号 0..n-1**，顺手把历史遗留的重复编号也抹平。

### 留个教训：两种排序机制混在一个 app 里

**别假设「排序都一样处理」。** 一种是位置、一种是字段，
对同步来说是完全相反的两件事（一个天生看不见、一个天生看得见）。
下次再加什么「顺序」，先问一句它是哪一种。

### 还有：用户连着问出了两个这类问题

划词的流量（第六十二节）和这一节的排序，**都不会报错**，
都要等他哪天自己发现不对，而那时候更难查。
两个都不是我主动会去查的地方 —— **他问「那……会怎么样」的时候，值得当真去查一遍，
而不是凭结构推理回一句「应该没问题」。** 这一节我差点就那么回了。

---

## 六十四、~~去掉手动排序，笔记一律按正文顺序~~ ✅ 2026-09-04 待验收

用户提的，一句话：**「把手动排序的功能给删掉，完全由规则排好就不能再自定义移动位置了。」**

### 拍板过的一件事

他手机和平板上**已经有的笔记，很多是他亲手拖过顺序的**。只删功能的话，
那些顺序会永远冻在当时的样子、再也调不了 —— 那不叫「完全由规则排好」。
问过他，选的是**一次性按正文顺序重排**，接受「以前拖出来的顺序全部消失」。

**结果这件事是免费的**：次序改成渲染时按坐标算，
**一个字节的数据都没动**，也没有多出一轮同步。老数据打开就是正文顺序。

### 新规则（`utils/annotationOrder.ts` 的 `compareByText`）

从上往下，前一条分不出胜负才看下一条：

| 顺位 | 规则 | 为什么 |
|---|---|---|
| 1 | **有位置的排在孤儿前面** | 孤儿（正文里那段被删了）按定义就没有位置 |
| 2 | **按起点坐标**（先行、后词） | 这就是「正文顺序」 |
| 3 | **起点相同，短的在前** | 单词 `stood` 和短语 `stood up very` 都从 L0W2 起，让单词在前 |
| 4 | **再按 createdAt，最后按 id** | 两台设备**必须**算出同一个次序，不能靠数组本来的位置 |

### 净减 445 行

删掉的东西比加的多得多，因为**从前那套复杂全是为了照顾手动顺序**：

- **`insertionOrder`（约一百行 + 13 条单测）** —— 它算的是「排在正文里紧挨着
  它前面那条**当前所在的位置**后面」。存在的唯一理由就是不冲掉用户拖出来的顺序。
  副作用是列表被拖乱之后，新卡片会落在一个看着莫名其妙的位置
- **`reorderAnnotations`、`nextAnnotationOrder`、`orderForNewAnnotation`**（storage）
- **复习页那一整套 dnd-kit**：`CardGrid`、`SortableCard`、`SortableCardInner`、
  手柄、`handleDragEnd`、`sensors`（左侧栏文库/文档的拖拽**不动**，那是另一回事）
- **`mergeCardOrder`（同步）** —— 第六十三节那个 bug 的补丁。次序不在数据里之后，
  笔记按 id 逐条合就够了，**同步这边什么都不用做**
- **`AnnotationGroup` / `annotationGroupOf`** —— 「单词和短语必须排在同一条队里」
  是编号时代的约束，按坐标排之后天然就在一起了

### `order` 字段留着没删

改成可选（`order?: number`），**谁也不再读、不再写**。

删它等于改写每一条笔记，白白换来一轮全量上传，而它又碍不着谁 ——
第六十一～六十三节反复交代过那块「判错就丢笔记」，能不碰就不碰。
新建的笔记压根不带这一格（真机验过）。

### 实测（浏览器里走真实流程）

先把示例数据的 `order` **故意打反**（stood=99、bursting=0、孤儿=-5000），
再塞进三条：一条夹在中间的、一条和 stood 同起点的短语、一条孤儿。刷新之后：

```
stood(L0W2) · stood up very(L0W2-W4) · stumble(L8W2) ·
夹在中间的(L9W1) · learned(L10W1) · bursting(L46W3) · 孤儿(无位置)
```

四条规则一条不差，**`order` 里存的数字完全不起作用**。接着：

| 验的东西 | 结果 |
|---|---|
| 编辑模式（那支笔） | 还在，输入框照常；**手柄没有了**，卡片左边也不再留那道空位 |
| 「词」和「句」两个页签 | 都没有手柄，卡片没有残留的空隙 |
| 左滑删除 | 七张卡七层 `SwipeToDelete`，一个不少 |
| 改卡片内容 | 存得进去（`order` 仍是 -999，纹丝不动 —— 确实没人写它了） |
| 右侧生词板 | 九条，次序和复习页**一模一样** |
| **新划一个词**（长按 voice，L2W3） | 落在第三位，voice 正好夹在 stood up very 与 stumble 之间 |
| 新划的记录里有没有 `order` | **没有这一格** |
| **新划一个短语**（长按 wanna + shout，L4W5-W6） | 落在 voice 与 stumble 之间，正文里那条实线也在 |
| 375px 窄屏 | 单列，卡片左边干净，没有手柄留下的空档 |

测试 593 项全过；`npx tsc -b --force`、`npm run build` 都通过。验完把测试数据清掉了。

### 顺带改了使用说明

原来两条「拖着排顺序」的说明已经不成立。现在写的是**按正文顺序、算出来的、不能手动调**，
并说清了文库复习那边是另一套（划过多次的在前，其余按字母）。

### 留个教训：**去掉一个功能，往往能顺手拆掉一整串为它服务的复杂**

这一节真正的收获不是少了个手柄，是**次序从「存下来的东西」变成了「算出来的东西」**。
一旦如此：两台设备算出来必然一致，同步不可能再把它弄乱（第六十三节那类 bug
从根上没有了），也没有「新的该插在哪」这个问题 —— 它本来就在它该在的地方。

**动手前值得先问一句：这个字段是非存不可，还是能算出来？**

---

## 六十五、~~右侧栏点重叠的短语跳不到正文~~ ✅ 2026-09-04 待验收

用户报的，描述得很准：**「点击右侧栏一些短语，它并没有跳转到文中所在位置，
往往发生这种情况的短语在划词上与划词重叠 —— 比如 all right，
单独划词 all、单独划短语 all right 都没事，但两个一起划，all right 就没法跳转了。」**

### 根因：一个字段被拿去干两件事

右侧生词板每一条只存了一个 `anchorId`，它同时是：

1. **身份** —— 删除、React 列表键、高亮当前项都认它
2. **门牌号** —— 点一下跳到正文哪里，靠 `document.getElementById(它)`

平时同一个值没问题，因为坐标既是身份也是位置。**但两条笔记撞同一个起点时就崩了**：
`all`（单词）和 `all right`（短语）起点都是 L5W2，身份不能发重号，
于是后来的那条**退回用自己的记录 id 当身份**。而那是身份证不是门牌号 ——
拿它去正文里找当然找不到，`getElementById` 返回 null，
`App.tsx` 那个 effect 就**静悄悄地什么也不做**。没有报错，没有反馈。

### ⚠️ 有一半是第六十四节带来的

这个 bug 本来就在，但**从前谁当输家取决于先划哪个**：先划单词就是短语跳不了，
先划短语就轮到单词跳不了（`insertionOrder` 会把后划的排在先划的后面）。

第六十四节定下「起点相同时单词排在短语前面」，于是**短语从此成了固定的输家**。
用户看到的「往往是短语跳不了」正是这么来的。规则本身没错，
是它把一个原本随机的老毛病变成了每次必现。

**教训：改排序规则时要想一想，有没有别处正靠着「谁排在前面」做决定。**

### 改法：身份和门牌号拆成两格

`VocabItemView` 多一格 `startAnchorId`（就是标注的真实 `start`），**跳转只认这一格**。
`anchorId` 原样不动 —— 删除那条路（`findAnnotationByKey`）本来就认得记录 id。

孤儿（正文里那段被删了）没有这一格：它本来就没地方可跳，行为不变。

改了三个文件、加了三条单测，别的一概没动。

### 实测（浏览器里走真实流程）

在示例文章里造了一组对照：短语 `stumble and` 和已有的单词 `stumble` **重叠**
（都从 L8W2 起）；短语 `wanna shout` 不重叠，作对照。
挂了个监听盯 `scrollIntoView` 到底有没有被调用：

| 点了什么 | 改之前 | 改之后 |
|---|---|---|
| 单词 `stood` | ✅ 滚到 L0W2 | ✅ |
| 短语 `wanna shout`（不重叠） | ✅ 滚到 L4W5 | ✅ scrollTop → 0 |
| **短语 `stumble and`（重叠）** | ❌ **一次都没发出** | ✅ scrollTop 2195 → **91，正文正好居中** |
| 单词 `stumble`（被重叠的那个） | ✅ | ✅ 91，没被改坏 |

测试 596 项全过；`npx tsc -b --force`、`npm run build` 都通过。验完把测试数据清掉了。

### ⚠️ 验这一条时被环境坑了半天，值得记下来

一开始怎么点都看不见正文动 —— **连普通单词也不动**，差点以为是另一个更大的 bug。

真相是 `scrollIntoView({behavior:'smooth'})` **靠 rAF 推进，而这个环境里
面板隐藏时 rAF 是冻着的**（验证表里早写着这一条，我没往这儿想）。
在 CSS 里注 `scroll-behavior:auto !important` **没用** ——
JS 里显式传的 `behavior:'smooth'` 压过 CSS。

治法是把 `Element.prototype.scrollIntoView` 临时包一层，强制 `behavior:'instant'`，
当场就看见了。**下次在这个环境里验「跳转 / 滚动」，先把平滑关掉。**

顺带一条：我中途用 `sc.scrollTop = 2195` 手工设过滚动位置，
那会被阅读进度记下来又还原，看着像「跳过去又被弹回来」—— 那是我自己造的干扰，不是 bug。

---

## 六十六、~~自动同步被闸拦下来时补一次~~ ✅ 2026-09-04 待验收

第六十五节交付之后，用户问「云端同步到底有哪些情况下会触发」。查完发现一处
**会悄悄丢改动**的地方，他当场要求改掉。

### 触发点一共三个，没有定时轮询

1. **app 启动、以及每次回到前台** —— 「拿起另一台的那一刻」正好是这一下
2. **改完东西 8 秒后**（`PUSH_DELAY_MS`，期间又改就重新计时）
3. **设置页里手动那颗「立刻同步一次」**

「什么算改动」挂在 `refreshData` 上（App.tsx），**所有写操作最后都会走它** ——
逐个写入口去挂钩子迟早漏一处。

### ⚠️ 查出来的毛病：被拦下来就地放弃，那次改动就等下一个触发点

两道省流量的闸只管自动的：**两次自动同步至少隔一分钟**，
以及**只有阅读进度变了不算变**（`onlyProgressChanged`，流量头号大户）。

问题出在第一道闸的写法上 —— 从前是 `if (...) return`，**直接放弃，不改约**。
于是：刚同步完 10 秒你划了个词，8 秒后闹钟响，一看「才 18 秒」就走了，
而且**不另约时间**。这条改动要等你切后台再回来、或者再改点别的，才会被顺路带上去。
现象就是**「我明明改了，另一台没更新」**。

顺手堵了同一个毛病的另一个口子：**已经有一轮在跑时**（`running.current`）
自动那条也是直接 return —— 而跑着的那一轮是在这次改动之前起的手，带不上它。

现在两处都改成**改约**：约到闸开的那一刻（或者 2 秒后再看一眼那轮跑完没有）。
**每分钟至多一次这个上限一点没松**，只是不再丢东西。

### 抽出一个纯函数，因为这块在真机上根本没法验

要盯着秒表、还得两台设备一起看。所以照 `shouldImmerse` 的路子，
把「这一次到底走不走」抽成 `decideAutoSync`（返回 `go` / `retry` / `skip`），
9 条单测钉住。**真身（网络、坚果云的脾气）仍然验不了，那部分只能等用户回话。**

### ⚠️ 测试自己写错了一条

「从没同步过时第一次别等一分钟」那条，我写了 `now: 1000`，
算出「还差 59 秒」判成改约，红了。**是测试错，不是代码错** ——
现实里 `Date.now()` 是万亿级，减 0 永远远大于一分钟。
（验证表里那句「检查脚本自己也会写错，对不上时先怀疑脚本」，又应验一次。）

### ⚠️⚠️ 下面这两笔账**全是错的**，留着当病历，别照抄

> **这一段的数字后来被用户用真数据推翻了两次，正确的在第六十七、六十八节。**
>
> - 「一条笔记约 10 字节」——错。真数是**单词 47、短语 85、句摘 103 字节**
>   （错误来源：拿第六十二节里**另一个数据集**的数当成了他的）
> - 「两万条以内不用管」——错。真门槛是**再划约 2900 条**
> - 「一个月 79 MB / 占 8%」——也不对，后来我又改口估成 157 MB，**还是错**，
>   高了约五倍（只有真有改动才上传）
>
> **别再算了。** 第六十八节已经改成**记真账**：
> 设置 → 开发者 → 同步数据，最上面那栏就是本月实际用掉多少。
>
> 留着这一段不删，是因为**它是「怎么错的」的完整记录** ——
> 教训写在第六十七节末尾：引用一个数之前，先问它是从哪儿量出来的。

**一、这么改会不会把额度用光。** 他的额度：每月上传 1 G、下载 3 G。

| | 一次同步上传 | 一个月 | 占上传额度 |
|---|---|---|---|
| 实际用法（一天三小时边读边划） | 约 15 KB | 约 5400 次 ≈ **79 MB** | **8%** |
| 理论上限（一天 24 小时不停地改） | 约 15 KB | 43200 次 ≈ **633 MB** | 63% |

**结论：安全。** 每分钟一次的上限本身就是天花板，改约不会把它抬高。

**二、笔记多了以后长得快不快。** 实测过的锚点：**1760 条笔记 + 索引 = 17 KB**
（压缩后），也就是**一条笔记约 10 字节**。合成数据跑了一遍确认是**线性**增长
（5000 / 1 万 / 2 万 / 5 万 / 10 万条，体积一路成正比，没有拐点）。

所以：**一万条约 100 KB，两万条约 200 KB。**

⚠️ 两个门槛，差得很远，别混起来看：

- 按**实际用法**（每月约 5400 次同步）算，data.json 到 **194 KB ≈ 两万条笔记**
  才会吃满上传额度
- 按**理论上限**（每分钟一次不停歇）算，24 KB ≈ **两千五百条**就会超 ——
  但那要求一天 24 小时不停地改东西，不会发生

**结论：两万条笔记以内不用管。** 到那时再看下面这条。

### 用户提的下一步优化：只对齐所在文档的笔记（**先不做**）

他的想法是对的方向：现在 `data.json` 里装着**全部**笔记，
在一篇文章里划一个词，整份都要重传。拆成 `notes-<文档id>.json` 之后，
一次只传那一篇的笔记，能再省一个数量级。

**但现在不划算，理由三条：**

1. **绝对值太小** —— 17 KB 拆成 1 KB，省下的 16 KB 乘以一个月也就几十兆，
   而额度用了 8%
2. **代价在三方合并上** —— 现在合并看的是**一份完整快照**，
   底本、删除、跨文档的改动都在同一个视野里。拆开之后底本也得拆，
   还要处理「一个文件传上去了、另一个没传」的中间态。
   而这块**每一处判错都是「不报错、过几天才发现笔记没了」**（第六十一～六十三节反复交代）
3. **有更省的先手** —— 已经有一道「先问一句变了没有」的闸（HEAD 几百字节），
   没变就直接结束、显示「没走流量」。日常大多数同步走的是这条路

**什么时候该做：data.json 到 150～200 KB（约两万条笔记）。**
到时候翻这一节，方案已经想清楚了。

### 留个教训：**「省流量」和「别丢改动」是两件事，别用同一个 return 兼办**

那道闸的本意是「别传太勤」，写成 `return` 之后顺带办成了「这次不传了」——
一个 return 干了两件事，而第二件没人要。**限流的正确形状是「推迟」，不是「丢弃」。**
（和第六十五节同一个味道：一个东西被拿去干了两件事。）

测试 605 项全过；`npx tsc -b --force`、`npm run build` 都通过。

---

## 六十七、~~「同步数据」读数屏~~ ✅ 2026-09-04 待验收 · ⚠️ 附一次我算错的账

第六十六节里我回答了「笔记多了体积涨得快吗」，**算错了，而且错得离谱**。
用户拿他的真数据当场推翻。这一节记的是错在哪、真数是多少，
以及为此加的那一屏读数。

### ⚠️ 我错在哪：拿别人的数当他的数

我从第六十二节里搬了「1760 条笔记 = 17 KB」，除出「**一条约 10 字节**」，
再推出「**两万条以内不用管**」。

他回：**「我大概才划了 300 个词/短语和 50 个句子，可现在每次上下就已经 30 KB 了。」**

他是对的。两个错误叠在一起：

1. **那 17 KB 是另一个数据集**（更早、笔记大多没填内容），我没说明就当成了他的
2. **只算了笔记**，而 data.json 里还装着**文档壳**和**旧模型残留**

而且他自己点破了我漏掉的那一半：「划词划短语之外还有划句，而且这些笔记
还会补中文释义、音标、语法」—— 句摘带着整句英文原文加句型加翻译，比单词重一倍。

### 真数（拿他的备份跑同步那套真代码量的）

`data.json` = **29.8 KB**，和他 app 里看到的「30 KB」对得上。

| 构成 | 大小 | 占比 |
|---|---|---|
| 笔记（322 条） | 18.3 KB | **61%** |
| **文档壳**（201 篇：标题、进度、指纹） | 8.8 KB | **29%** |
| 旧模型残留 `notes`（9 篇的老表） | 1.7 KB | 6% |
| 文库（14 个） | 0.8 KB | 3% |

每条笔记平均（压缩后）：**单词 47 字节、短语 85、句摘 103**。
边际成本（再加一条要多传多少）：**单词约 60、短语约 50、句摘约 83**。

**比我说的 10 字节大了五到八倍。**

⚠️ 还查明一件事：备份文件里那个 `sentences`（2.9 KB）是**导出时才补的**，
`replaceAllData` 不收它，所以**不进同步**。量的时候要先删掉，否则会多算 10%。

### 修正后的额度账

| | 一个月上传 | 占 1 G |
|---|---|---|
| 按一天边读边划三小时（约 180 次/天） | **157 MB** | **15%** |

**眼下安全，但门槛比我上次说的近一个数量级：**

- 要吃满上传额度，data.json 得涨到约 194 KB
- 他现在 30 KB，**再划约 2900 条笔记**就到了（不是我说的两万条）

### ⚠️ 顺带修正对「按文档拆分」的判断

他提的「不必对齐所有笔记，只对齐所在文档的笔记」——**方向对，但省不到一个数量级**。

拆开之后索引里还得留着**文档壳 8.8 KB + 文库 0.8 KB + 残留 1.7 KB ≈ 11.3 KB**，
再加当前文档那一篇的笔记（约 1 KB）。**30 KB → 约 12 KB，两倍多一点，不是十倍。**

因为**文档壳拆不掉**，而他有 201 篇（下载的书按章分篇），那 29% 是地板。
真要再往下压，得先想文档壳怎么瘦，而不是先拆笔记。

**结论不变：现在别做**（代价压在三方合并上，那里判错就是悄悄丢笔记）。
但**门槛从「两万条」改成「data.json 到 100 KB 左右、或者他自己觉得慢了」**。

### 加了什么：设置 → 开发者 → 同步数据

第四十九、五十七节那一招，第三次用上：**加一屏读数，胜过猜三轮。**

这屏显示：data.json 现在多大 · 字节花在哪儿了（按大小排） ·
每类笔记平均多重 · 照这个大小推算一个月用多少额度、还能再划多少条 ·
正文总量（单独存，平时不传）。

**数字全是真算的** —— 跑的是同步那套真代码（`toIndex` + `pack`），
不是估计值。真机上点开就能看见，往后不用再来问我，我也不会再猜。

进那一屏时才算：要把整份数据压一遍、正文还得逐字算指纹，
而这屏十次里九次不会被点开。

⚠️ 界面上写明了**各块相加不等于总数** —— gzip 是看上下文的，
几块内容互相占便宜。这不是算错，是压缩的本性。

### 两个测试自己写错的地方（一节里连中两次）

1. 第六十六节那条 `now: 1000`（现实里是万亿级时间戳）
2. 这一节：断言「笔记越多、余量越小」—— 红了。**合成数据里几百条笔记长得一模一样，
   gzip 压得越来越狠，平均每条反而更小。** 那是合成数据的特性，不是代码错。
   换成一条不依赖数据重复度的性质：别的东西占得多了，留给笔记的就少

### 留个教训：**引用一个数之前，先问它是从哪儿量出来的**

17 KB 那个数就写在自己的文档里，看着很可信 —— 但它是**另一批数据、另一个时候**量的。
我拿它当他的数用，还在结论里加了「不用管」这种确定的口气。

**用户手上有真数据，而我只有一个二手的数。** 这种时候正确的做法是
**先要数据、或者先加读数**，不是先给结论。他一句话就把结论推翻了，
而如果他没较真，这个错误会一直躺在文档里。

测试 614 项全过；`npx tsc -b --force`、`npm run build` 都通过。
量完把他的备份从工作区删干净了，浏览器里的数据也清了。

### 追加：「句摘到底走不走同步」——他追问出来的，而这里原先一条都没测过

我上面那句「备份里的 `sentences` 不进同步」把他吓着了，他追问「句摘不会被云同步吗」。

**会。** 但我原先只是**凭结构推理**就那么说了，而 `roundtrip.test.ts` 里
**一条句摘都没有**（14 条测试全是单词）。所以补了 4 条，把话说实：

- 手机划的句摘，平板拿得到，**句型和翻译一个字不少**
- 单词 / 短语 / 句摘混在一起，三种都过得去，谁也不吃掉谁
- 两台各划各的句摘，都保留，不互相盖
- 句摘的翻译改了，改动传得过去

再拿他的真备份对了一遍：`annotations` 里 `type='sentence'` 的 **42 条**，
导出时另补的 `sentences` 也是 **42 条**，**id 完全重合，差集为 0** ——
后者是前者的副本，不是另一批数据。

**两个字段的分工**（下一个人容易在这儿犯迷糊）：

| | 住在哪 | 走同步吗 | 干什么用的 |
|---|---|---|---|
| `annotations` 里 `type='sentence'` | 主库 | **走** | 这才是句摘的真身 |
| `sentences` | **只在导出的备份文件里** | 不走 | 旧模型的形状，为了退回旧版 APK 还认得；恢复备份那条路会读它（App.tsx） |

⚠️ 所以**量同步体积时必须先把 `sentences` 删掉**，否则多算 10%（他那份是 2.9 KB）。

### 又一个教训：**「顺带查明一件事」也要有证据**

那句话是我在报告里顺手带出来的，语气很确定，但**背后没有一条测试**。
用户一追问，我才发现往返测试里连一条句摘都没有。
**顺口说的结论和写在文档里的结论，标准应该一样。**

---

## 六十八、~~云端同步的三件优化~~ ✅ 2026-09-04 待验收

用户问「云端同步怎么优化比较好」。列了四个方向、给了排序，他选了**前三件一起做**。
（我提过「一次别改一大片，真机一挂分不清哪一处」，他仍要一起 ——
所以拆成**三个独立提交**放同一条分支，哪件出问题单独摘掉那一个就行。）

### 又一次先纠正自己：157 MB 那个数偏高了约五倍

第六十七节读数屏上写的「一个月约 157 MB」，是按**一天同步 180 次**估的。
但**只有真有改动时才上传** —— 回前台那种多半命中「没走流量」。
按他一天划 20～50 个词推，真实大概是**一个月 15～30 MB，占 2～3%**。

**同一个问题我估错了两次**（一次乐观十倍、一次悲观五倍）。所以第一件事不是省流量。

### 一、记真账，不再估（`sync/usage.ts`）

每次同步本来就算出了上下行字节，从前用完就扔。现在累加进 localStorage，
**按自然月**记（跟坚果云额度对齐），换月自动归零。
读数屏最上面一栏：「本月已用 上传 X / 1 G」，并标明**这是真账不是估算**。

「没走流量」那种不计入次数。记账失败一律吞掉 —— 不能因为记账把同步搞挂。

底下那两行推算改了口气：标明按最坏情况估，**以真账为准**。

### 二、旧模型残留不上云（省 6%）

那 9 篇老笔记表内容早已迁进 `annotations`，运行期**只有一次性的分词迁移会写它**
（`replacePageNotes`，唯一调用方在启动流程里）—— 冻着的，却每次都传一遍。

现在 `toIndex` 把它摘掉，**本地照样留着**当保险（恢复换代前的老备份还得靠它），
两台各留各的，反正谁也不会再改。

⚠️ **摘出去带来一条随时会踩的坑**：合并结果里没有它，忘了把本地那份带回来，
`replaceAllData` 会拿 `?? {}` 把它抹成空的，**而且不报错**。
所以 `fromIndex` 那个参数做成**必填** —— 让编译器把每个调用点逼出来，
而不是给个默认值让它悄悄变空。**这一手值得学：危险的遗漏，用类型系统挡，别靠记性。**

### 三、阅读进度拆出去单独走小文件（`sync/progress.ts`）

从前进度就在索引里，一路往下读时滚动位置一直在存，每次都算「数据变了」。
老治法是「只有进度变了就先别传」，省了流量，**代价是进度不实时** ——
手机读到第 50 页，切平板还是旧位置，得等你划个词才顺路带过去。

现在进度从索引里摘出去，走 `progress.json`（几百字节）：
**索引在读书时纹丝不动，进度自己随时能传。两个问题一起解决。**

⚠️ **路上排掉的一个坑，不排的话这件事等于白做**：存进度走的是 `savePage`，
而它**每次都刷 `updatedAt`**，那一格在索引里 —— 于是滚动一次索引就变一次。
加了 `savePageProgress`：只写进度、不碰 `updatedAt`，顺手挡掉「值没变还写一遍」。

**合并规则：按篇比时间戳，谁记得晚谁赢。**
不是「读得远的赢」—— 你翻回前面重读才是最新的意图，不该被旧记录顶回去。
平局保本地的。云端那份当**不可信输入**过一遍，形状不对的整条丢掉而不是抛错
（一条坏记录不该让所有篇的进度都用不了）。

`onlyProgressChanged` 和 `syncNow` 的 `auto` 参数一并删掉 —— 它们只为那条老规则活着。

### 实测（拿他的真备份灌进浏览器走真实流程）

| | 改之前 | 改之后 |
|---|---|---|
| `data.json` | 30 KB | **28 KB** |
| 旧模型残留 | 6%，每次都传 | **不上云**，只留本机 |
| 阅读进度 | 在索引里 | **单独小文件** |
| 一路往下读时 | 索引反复重传（老治法是干脆不传） | **索引一次都不写**，进度实时上去 |

读数屏最上面多了「本月实际用掉了多少」那一栏（塞了一笔假账验过显示）。

⚠️ 界面上把「不进 data.json」的东西**单列一栏**，没有塞进构成表里当成「占 0 字节的一块」——
那会被读成「它在里面，只是不占地方」，而事实是它压根没上云。

测试 **646 项**全过；`npx tsc -b --force`、`npm run build` 都通过。
量完把他的备份从工作区删干净了，浏览器里的数据也清了。

### 三条「去掉修复就会红」的验证

危险的那几处都专门验过测试挡不挡得住（把修复拿掉，确认变红，再还原）：

- 旧 notes 没带回来 → 2 条红
- 进度没补回来 → 3 条红
- （第六十六节）门牌号那格没填 → 2 条红

**写完测试要反过来验一次它会不会红，否则你不知道它在挡什么。**

### 没做的那一件：按文档拆笔记

第六十七节算过：只能省一半多一点（文档壳那 29% 拆不掉），
而代价压在三方合并上。**门槛：data.json 到 100 KB 左右再说。**

### 留个教训：**限流和省流量都不该用「丢弃」来实现**

这一轮和第六十六节是同一个味道：
- 第六十六节：限流闸写成 `return`，顺带把改动丢了 → 应该是**推迟**
- 这一节：省流量写成「只有进度变了就不传」，顺带把实时性丢了 → 应该是**拆小**

**「不做某事」是最容易写的实现，也最容易把别的东西一起做没了。**

## 六十九、~~设置里新增的子屏漏登记：进去不在顶部、返回键关掉整个设置~~ ✅ 2026-09-04 待验收

用户报的两件，**他自己就猜到是同一个根子，猜对了**：

> 「设置，开发者，同步数据那个窗口，点进去后那个页面的位置不是顶部，
> 应该是和设置那个窗口共用一个滚动进度了吧，这个问题似乎以前就犯过，
> 以后记住在设置里新增窗口，滚动条应该单独计算。
> 另外还是这个同步数据的窗口，我通过手机返回键退出它结果把整个设置窗口都关了。」

分支 `settings-subscreen-registry`。

### 根子：一块子屏要在**五处**手写清单里各登记一行，第六十七节只登记了三处

第六十节把滚动位置治好了（进子屏归零、返回还原）。但那次的做法是
在一串手写的判断里把当时那几屏列全，**新增一屏得记得往每一处都加一行**：

| 清单 | 干什么 | 「同步数据」当初 |
|---|---|---|
| `useEffect(open)` 里的重置 | 关掉再打开时清空 | ✅ 加了 |
| `title` 那串三元 | 顶栏写什么 | ✅ 加了 |
| `back` 那串三元 | 右上角 X / 点外面 | ✅ 加了 |
| **`inSub`** | 在不在子屏 → 决定滚不滚到顶 | ❌ **漏了** |
| **`useBackHandler`** | 手机返回键退到哪 | ❌ **漏了** |

漏掉 `inSub` → 换屏时 `scrollTop` 原样留着，进去就停在主列表那个位置；
漏掉返回键那处 → 返回键落到最后那个 `else onClose()`，一按关掉整个设置。
两个症状，**一处漏、一处漏，但同一个成因**：靠记性维护五份平行清单。

### 治法：五份清单收成一个状态 + 一张编译器管着的表

八个布尔（`editingAi`、`showDev`、`showSyncSize`……）换成**一个** `sub` 状态：

```ts
type SubScreen = 'ai' | 'cloudTts' | 'sync' | 'agreement' | 'guide' | 'syncSize' | 'speechDev' | 'dev'

const SUB_TITLE: Record<SubScreen, string> = { ai: 'AI 设置', ... }
```

于是那五处各自缩成一行：重置是 `setSub(null)`，`inSub` 是 `sub !== null`，
返回键是 `if (sub) setSub(null) else onClose()`，
标题是 `SUB_TITLE[sub]`，返回是 `() => setSub(null)`。**净减 35 行。**

⚠️ **往后加一屏，只能从 `SubScreen` 这个联合类型下手** —— 加个成员，
编译器立刻在 `SUB_TITLE` 和渲染分支上把该补的地方指出来。
这正是第六十八节那条「危险的遗漏用类型系统挡，别靠记性」的第二次应用。

### 反过来验了这道闸会不会红

按第六十八节的规矩，往联合类型里临时加一个 `'newScreen'`，`tsc` 当场报：

```
error TS2741: Property 'newScreen' is missing in type '{ ai: ...; dev: string; }'
  but required in type 'Record<SubScreen, string>'.
```

确认它真的挡得住，再还原。

### 浏览器里量的（八块子屏全跑了一遍）

临时在 `window.__back` 上挂了 `handleBackPress`（验完删掉，没进提交），
这样浏览器里也能走一遍真机那条返回键通路 —— 第五十一节说的「留个手工入口」。

主列表滚到 **741** → 进任一子屏 **0** → 按返回键 → **设置还开着**、回到 **741**；
在主列表上按返回键才关掉设置。八块子屏（AI 设置、云端朗读、云端同步、
使用说明、用户协议、系统栏参数、同步数据、朗读引擎参数）结果一致。

### 留个教训：**「新增一屏要记得在 N 个地方登记」——** N 大于 1 就已经是 bug 了

第六十节修的是**症状**（滚动位置），没动**成因**（平行清单）。
七节之后同一个坑又踩了一次，而且这次多带出一个返回键的症状。

**修完一个「忘了改另一处」的 bug，顺手问一句：还有几处是同样要手工同步的？**
能收成一处就收成一处；收不成，至少让编译器替你数。

## 七十、~~笔记栏跟着正文滚~~ ✅ 2026-09-04 待验收

用户提的：

> 「一个文档内容很长，做的笔记也可能越多，右侧栏的笔记也会很长，这时候想要在
> 右侧栏找到想要的笔记就很麻烦。我的想法是右侧栏默认跟着正文滚动……
> 如果这时候还没划词划短语，那右侧栏也可以滑动到离阅读位置最近的笔记那里。
> 句摘栏也同理。」

分支 `sidebar-follow`。

### 地基是现成的，这件事比看上去便宜

- 每条笔记的位置就是 `L第几行W第几个词`，而且**第六十四节起顺序是算出来的**
  （按正文坐标），所以「清单里第几条」和「正文里第几行」本来就是单调对应的
- 阅读器里早就有 `topVisibleLine()` —— 当初为「编辑全文」接滚动位置写的（第三十四节）

于是不必去量每张卡片的 DOM，**两个行号一比就有了**。

### 三个决定，都是用户拍的板

**一、停在哪** —— 三种停法做成网页给他拖着看（第 2 条工作方式：说不清就做成网页）：
永远贴顶 / 永远居中 / **看不见才滚**。他选了最后一种。

「怎么滚过去」最后一行代码都没写：`scrollIntoView({ block: 'nearest' })`
浏览器自己就懂「看得见就别动，看不见才滚，只滚到刚好露出来」。

**二、「最近」取前面那条，不是绝对距离最近的那条**（`followScroll.ts` 里有整段说明）。
笔记在第 12 行和第 38 行、读到第 30 行 → 落在**第 12 行**。
按绝对距离算的话，空档正中间会有一个翻页点，从第 25 行读到第 26 行竖线会一下子跳过去。

**三、当前那条左边一道细竖线**。两种状态都画 2px 边（不当前的透明），
**布局一个像素都不动** —— 只画当前那条的话，竖线一出现整列往右挪 2px。

### 暂停规则

用户选的是「手动滚过侧栏就暂停一会儿，正文再滚就恢复」。落地时的关键一处：

⚠️ **判「用户滚了侧栏」要听 `wheel` / `touchmove`，不能听 `scroll`** ——
后者分不清是谁滚的，我自己 `scrollIntoView` 滚的也会触发，一滚就把自己暂停了。
指针事件只有人能发出来。

解除暂停的四件事：打开笔记栏、切标签、换文档、**正文动了**。
另有一件能**盖过**暂停：刚亲手划下一条笔记（`savedNoteFocus`，带时间戳，
因为同一条可能连着存两次而坐标不变）。

⚠️ **AI 一键划词那一批不走这条路**：一次进来几十条，而战报条（「已划上 N 条 / 撤销」）
就挂在侧栏顶上，列表一甩就找不着撤销了。

### 算不算这个数：收起来时一次都不算

`onTopLineChange` **不给就一次也不算**。笔记栏收着（窄屏上滚正文时必然收着）
或在编辑模式，App 传 `undefined`，阅读器那边连遍历都不做。
编辑模式还有第二个理由：那时候在滚的是 textarea 自己，「第几行」这个坐标源
根本不在阅读容器里（第三十四节）。

滚动中按 **120ms 尾随节流**上报，而且行号没变就不报。

**量过**（120 行、最坏情形即滚到最底要一路扫到最后）：一次 **0.13ms**，
按 120ms 一次算占 0.11%。两千行的长章外推也就 2ms 出头，不必优化。

### ⚠️ 这一轮验证踩了三个环境坑，全是「验证环境会骗人」

这三条比功能本身更值得记：

1. **面板隐藏时 CSS 过渡不推进** —— 侧栏 class 已经是 `translate-x-0` 了，
   `getBoundingClientRect()` 读到的却还是推到屏幕外那一帧。
   我据此量了半天，量的全是一块看不见的面板。治法是注一条
   `*{transition:none!important}`（这条本来就写在文档的「能信/不能信」表里）
2. **改了 `scrollTop`，原生 `scroll` 事件一次都不发**（实测 0 次）——
   于是阅读进度和行号一起冻住，看着像功能没生效。
   治法和「模拟器不发 resize」完全一样：**手工补一个** `dispatchEvent(new Event('scroll'))`
3. **后台标签页里 `setTimeout` 被钳到 1 秒起步** —— 我按 120ms 的节流只等了 280ms，
   于是每一步量到的都是**上一步**的答案，看着像「慢一拍的 bug」。
   等待拉到 1600ms 之后，全部对上

⚠️ **三次都是环境在骗人，代码一次都没错。** 第一反应「这是个 bug」时，
先问一句：这个数是在什么条件下量出来的？

### 浏览器里量的（临时塞了 120 行正文 + 22 条词 / 6 条句摘 / 2 条孤儿，验完删掉）

| 读到第几行 | 竖线落在 | 侧栏 scrollTop | 当前那条完整可见 |
|---|---|---|---|
| 0 | 第 3 行那条 | 8 | ✓ |
| 26 | 第 26 行那条 | **8（没动）** | ✓ |
| 53 | 第 53 行那条 | 233 | ✓ |
| 74 | 第 74 行那条 | 606 | ✓ |
| 110 | 第 104 行那条 | 904 | ✓ |

第 26 行那次**侧栏一动不动** —— 那条本来就看得见，正是「看不见才滚」。

其余场景：句摘标签切过去落在第 40 行那条（读到第 53 行）✓；
手动把侧栏拉到顶后**不被拽回去**，滚一下正文立刻恢复并跟到位 ✓；
手动拉到底（1094）→ 收起 → 滚正文 → 重新打开，跳回第 12 行那条（157）✓；
读到全文最末尾时竖线停在最后一条**有坐标**的笔记上，两条孤儿在它下面 ✓。

⚠️ **「刚亲手划完一条就跟过去」这一条我没在浏览器里走通** ——
要模拟长按取词那一整套手势。真机上留意一下：划完一个新词，
侧栏应当跳到它身上，**哪怕你上一秒刚手动滚过侧栏**。

### 留个教训：**「怎么做」的那一半，先问问浏览器自己会不会**

「看得见就别动，看不见才滚，而且只滚到刚好露出来」——
这句话原样就是 `scrollIntoView({ block: 'nearest' })`。
自己算的话要处理容器内边距、卡片高度、上下两个方向、正在动画中的位置……
而这些全是像素，全是我验不了的东西。

**算得清的那一半（跟到哪一条）抽成纯函数用测试钉住，
算不清的那一半（怎么滚过去）交给浏览器。** 这一节两边都没写像素。
